---
phase: 45-win-celebration-effects-system
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/styles/global.css
  - tests/slots-win-celebration-contract.test.mjs
autonomous: true
requirements:
  - FX-70
  - FX-72

must_haves:
  truths:
    - "Win outcomes trigger a visible cyan reel glow plus amplified gold frame pulse on EN/PT Slots routes"
    - "The reel glow animates via @keyframes slots-reel-win-pulse with ~2 iterations at ~0.95s cadence"
    - "The gold frame flare peaks at 0.55+ outer glow opacity (up from 0.18)"
    - "Win visual state holds as static highlight until next spin (no timeout reset)"
    - "reduced intensity caps reel-win-pulse to 1 iteration"
    - "minimal intensity disables reel-win-pulse animation entirely, keeps box-shadow fallback"
  artifacts:
    - path: "src/styles/global.css"
      provides: "slots-reel-win-pulse keyframes + win reel-window rules + amplified slots-win-flare + extended reduced/minimal blocks"
      contains: "@keyframes slots-reel-win-pulse"
    - path: "tests/slots-win-celebration-contract.test.mjs"
      provides: "Source-grep contracts proving FX-70 and FX-72 CSS rules exist"
      contains: "FX-70"
  key_links:
    - from: "src/styles/global.css"
      to: "data-slots-anim-effect='win'"
      via: "CSS attribute selector"
      pattern: "data-slots-anim-effect='win'.*slots-stage__reel-window"
    - from: "src/styles/global.css"
      to: "data-slots-anim-intensity"
      via: "CSS attribute selector compound"
      pattern: "data-slots-anim-intensity='reduced'.*reel-win-pulse|data-slots-anim-intensity='minimal'.*reel-window"
---

<objective>
Add motion-policy-safe win celebration effects to the Slots CSS layer: a cyan neon pulse on reel windows and an amplified gold frame flare, both driven by the existing `data-slots-anim-effect='win'` dataset hook. Lock the implementation with source-grep contract tests proving FX-70 (visible celebration) and FX-72 (motion-policy respect).

Purpose: Deliver the two-tone win moment (cyan reels + gold frame) that makes wins feel casino-impactful, while staying CSS-only and presentation-only with no authority boundary crossings.

Output: Updated `global.css` with new keyframes, reel-window win rules, amplified frame flare, and extended reduced/minimal blocks. New contract test file proving CSS rules exist.
</objective>

<execution_context>
@/home/gabriel/Documents/gabriel-rodrigues/.claude/get-shit-done/workflows/execute-plan.md
@/home/gabriel/Documents/gabriel-rodrigues/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/45-win-celebration-effects-system/45-CONTEXT.md

@src/styles/global.css
@tests/compatibility-contract.test.mjs
@tests/slots-motion-accessibility-contract.test.mjs

<interfaces>
<!-- Existing CSS selectors and keyframes the executor must work with/extend -->

From src/styles/global.css (lines 1350-1541):

Existing win frame rule (line 1472-1475):
```css
#slots-shell-root[data-slots-anim-effect='win'] .slots-stage__frame {
  animation: slots-win-flare 0.95s cubic-bezier(0.16, 1, 0.3, 1) 2;
  border-color: rgba(255, 215, 9, 0.42);
}
```

Existing @keyframes slots-win-flare (line 1350-1364):
```css
@keyframes slots-win-flare {
  0%, 100% {
    box-shadow:
      inset 0 0 0 1px rgba(143, 245, 255, 0.08),
      inset 0 -20px 40px rgba(0, 0, 0, 0.35),
      0 0 0 rgba(255, 215, 9, 0);
  }
  50% {
    box-shadow:
      inset 0 0 0 1px rgba(143, 245, 255, 0.18),
      inset 0 -20px 40px rgba(0, 0, 0, 0.25),
      0 0 30px rgba(255, 215, 9, 0.18);
  }
}
```

Existing reduced win rule (line 1517-1520):
```css
#slots-shell-root[data-slots-anim-intensity='reduced'][data-slots-anim-effect='win']
  .slots-stage__frame {
  animation-iteration-count: 1;
}
```

Existing minimal animation-none block (line 1522-1529):
```css
#slots-shell-root[data-slots-anim-intensity='minimal'] .slots-stage__reel-window,
#slots-shell-root[data-slots-anim-intensity='minimal'] .slots-stage__frame,
/* ... more selectors ... */
{
  animation: none !important;
}
```

Existing minimal win box-shadow fallback (line 1536-1541):
```css
#slots-shell-root[data-slots-anim-intensity='minimal'][data-slots-anim-effect='win']
  .slots-stage__frame,
/* ... */
{
  box-shadow: inset 0 0 0 1px rgba(255, 215, 9, 0.12);
}
```

Design tokens: --color-cyan is #00e5ff (rgba: 0, 229, 255)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add win celebration CSS — reel-win-pulse keyframes, reel-window glow, amplified frame flare, and motion-policy extensions</name>
  <files>src/styles/global.css</files>
  <read_first>
    - src/styles/global.css (full file — executor must see current state of keyframes at ~line 1350, win rules at ~line 1472, reduced block at ~line 1513, minimal block at ~line 1522)
    - .planning/phases/45-win-celebration-effects-system/45-CONTEXT.md (decisions D-01 through D-07)
  </read_first>
  <behavior>
    - Test 1 (FX-70 reel glow): global.css contains `@keyframes slots-reel-win-pulse` with cyan box-shadow values using rgba(0, 229, 255, ...)
    - Test 2 (FX-70 reel selector): global.css contains `[data-slots-anim-effect='win'] .slots-stage__reel-window` with `animation: slots-reel-win-pulse`
    - Test 3 (FX-70 frame amplification): global.css `@keyframes slots-win-flare` 50% keyframe has outer glow opacity >= 0.55
    - Test 4 (FX-72 reduced): global.css contains `[data-slots-anim-intensity='reduced'][data-slots-anim-effect='win']` rule covering `.slots-stage__reel-window` with `animation-iteration-count: 1`
    - Test 5 (FX-72 minimal): The minimal `animation: none !important` block already covers `.slots-stage__reel-window` (existing selector at line 1522); verify no regression
  </behavior>
  <action>
    Modify `src/styles/global.css` with these exact changes:

    **1. Add new @keyframes (insert after the existing `slots-win-flare` block, before `@keyframes slots-shadow-drift` at ~line 1366):**

    ```css
    @keyframes slots-reel-win-pulse {
      0%, 100% {
        box-shadow:
          inset 0 1px 0 rgba(255, 255, 255, 0.04),
          inset 0 0 40px rgba(0, 0, 0, 0.35),
          0 0 0 rgba(0, 229, 255, 0);
      }
      50% {
        box-shadow:
          inset 0 1px 0 rgba(255, 255, 255, 0.06),
          inset 0 0 30px rgba(0, 0, 0, 0.25),
          0 0 28px rgba(0, 229, 255, 0.38);
      }
    }
    ```

    **2. Amplify the existing `@keyframes slots-win-flare` (modify the 50% keyframe in-place at ~line 1358):**

    Change the 50% block from:
    ```css
    50% {
      box-shadow:
        inset 0 0 0 1px rgba(143, 245, 255, 0.18),
        inset 0 -20px 40px rgba(0, 0, 0, 0.25),
        0 0 30px rgba(255, 215, 9, 0.18);
    }
    ```
    To:
    ```css
    50% {
      box-shadow:
        inset 0 0 0 1px rgba(143, 245, 255, 0.28),
        inset 0 -20px 40px rgba(0, 0, 0, 0.18),
        0 0 38px rgba(255, 215, 9, 0.58);
      filter: brightness(1.12);
    }
    ```
    This raises the gold outer glow from 0.18 to 0.58 opacity and adds a brightness flash per D-04.

    **3. Add reel-window win rule (insert immediately after the existing `[data-slots-anim-effect='win'] .slots-stage__frame` block at ~line 1475):**

    ```css
    #slots-shell-root[data-slots-anim-effect='win'] .slots-stage__reel-window {
      animation: slots-reel-win-pulse 0.95s cubic-bezier(0.16, 1, 0.3, 1) 2;
      border-color: rgba(0, 229, 255, 0.34);
    }
    ```
    This gives reel windows a cyan neon glow per D-01, animated per D-02, with 2 iterations matching the win-flare cadence. All wins get the same flat treatment per D-03. Win state persists via existing data-slots-anim-effect lifecycle per D-05.

    **4. Extend the reduced-intensity win block (insert after the existing reduced win frame rule at ~line 1520):**

    ```css
    #slots-shell-root[data-slots-anim-intensity='reduced'][data-slots-anim-effect='win']
      .slots-stage__reel-window {
      animation-iteration-count: 1;
    }
    ```
    Per D-06: reduced intensity caps reel-win-pulse to 1 iteration.

    **5. Extend the minimal win box-shadow fallback block (~line 1536-1541):**

    Add `.slots-stage__reel-window` to the existing minimal win fallback selector group. The result should be:
    ```css
    #slots-shell-root[data-slots-anim-intensity='minimal'][data-slots-anim-effect='win']
      .slots-stage__frame,
    #slots-shell-root[data-slots-anim-intensity='minimal'][data-slots-anim-effect='win']
      .slots-stage__reel-window,
    #slots-shell-root[data-slots-anim-intensity='minimal'][data-slots-anim-effect='blocked']
      .slots-stage__status-readout {
      box-shadow: inset 0 0 0 1px rgba(255, 215, 9, 0.12);
    }
    ```
    Note: The existing minimal `animation: none !important` block at ~line 1522 already covers `.slots-stage__reel-window` — no change needed there. The reel-window minimal win fallback uses the same gold box-shadow as the frame for visual consistency in minimal mode. Per D-07: no additional prefers-reduced-motion media query needed.

    **Do NOT:**
    - Add any new JS or TS files — this is CSS-only
    - Create payout-intensity scaling (D-03 explicitly forbids it)
    - Add timeout/reset logic (D-05: existing lifecycle is correct)
    - Add prefers-reduced-motion media query (D-07: already handled via motion-policy)
  </action>
  <verify>
    <automated>grep -c "slots-reel-win-pulse" src/styles/global.css | grep -q "^[3-9]" && grep "rgba(255, 215, 9, 0.5[5-9]\|0\.[6-9]" src/styles/global.css | head -1 && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - grep "slots-reel-win-pulse" src/styles/global.css returns at least 3 matches (keyframes definition, win rule animation value, reduced rule reference)
    - grep "rgba(0, 229, 255" src/styles/global.css returns at least 2 matches (keyframes 50% glow + win rule border-color)
    - grep "rgba(255, 215, 9, 0.58)" src/styles/global.css returns exactly 1 match (amplified flare 50% keyframe)
    - grep "filter: brightness(1.12)" src/styles/global.css returns at least 1 match (in win-flare 50%)
    - grep "reduced.*win" src/styles/global.css followed by grep "reel-window" finds the reduced reel-window rule
    - grep "minimal.*win" src/styles/global.css followed by grep "reel-window" finds the minimal reel-window fallback
    - The @keyframes slots-reel-win-pulse block exists between slots-win-flare and slots-shadow-drift
    - No new .ts or .js files created
  </acceptance_criteria>
  <done>
    global.css contains: (1) @keyframes slots-reel-win-pulse with cyan box-shadow animation, (2) win reel-window rule using that keyframe at 0.95s x2, (3) amplified slots-win-flare at 0.58 gold opacity + brightness flash, (4) reduced-intensity reel-window iteration cap, (5) minimal-intensity reel-window box-shadow fallback. No JS changes.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add source-grep contract tests proving FX-70 and FX-72 CSS rules exist</name>
  <files>tests/slots-win-celebration-contract.test.mjs</files>
  <read_first>
    - tests/compatibility-contract.test.mjs (lines 1-32 for file-read pattern, lines 195-210 for globalCss assertion pattern)
    - tests/slots-motion-accessibility-contract.test.mjs (for motion-policy test pattern)
    - src/styles/global.css (to confirm exact strings to assert against after Task 1)
  </read_first>
  <action>
    Create `tests/slots-win-celebration-contract.test.mjs` following the established source-grep contract pattern from `compatibility-contract.test.mjs`.

    The file must:

    1. Import `node:test`, `node:assert/strict`, `node:fs`, `node:path`
    2. Read `src/styles/global.css` into a `globalCss` string variable (same pattern as compatibility-contract.test.mjs line 17-31)
    3. Contain these test cases:

    ```javascript
    import test from 'node:test';
    import assert from 'node:assert/strict';
    import { readFileSync } from 'node:fs';
    import { resolve } from 'node:path';

    const globalCssPath = resolve(process.cwd(), 'src/styles/global.css');
    const globalCss = readFileSync(globalCssPath, 'utf8');

    test('FX-70: win outcome triggers cyan reel-window glow via slots-reel-win-pulse keyframes', () => {
      assert.match(globalCss, /@keyframes slots-reel-win-pulse/);
      assert.match(globalCss, /rgba\(0, 229, 255/);
      assert.match(globalCss, /data-slots-anim-effect='win'\] \.slots-stage__reel-window/);
      assert.match(globalCss, /animation: slots-reel-win-pulse 0\.95s/);
    });

    test('FX-70: win-flare gold frame pulse is amplified to dramatic opacity', () => {
      assert.match(globalCss, /@keyframes slots-win-flare/);
      assert.match(globalCss, /rgba\(255, 215, 9, 0\.58\)/);
      assert.match(globalCss, /filter: brightness\(1\.12\)/);
    });

    test('FX-72: reduced intensity caps reel-win-pulse to single iteration', () => {
      assert.match(
        globalCss,
        /data-slots-anim-intensity='reduced'\]\[data-slots-anim-effect='win'\]\s*\n?\s*\.slots-stage__reel-window/
      );
    });

    test('FX-72: minimal intensity disables reel-window animation and provides box-shadow fallback', () => {
      assert.match(
        globalCss,
        /data-slots-anim-intensity='minimal'\] \.slots-stage__reel-window[\s\S]*?animation: none !important/
      );
      assert.match(
        globalCss,
        /data-slots-anim-intensity='minimal'\]\[data-slots-anim-effect='win'\]\s*\n?\s*\.slots-stage__reel-window/
      );
    });
    ```

    Each test name starts with the requirement ID it proves (FX-70 or FX-72). The assertions use regex matching against the raw CSS source, consistent with the project's established source-grep contract pattern.
  </action>
  <verify>
    <automated>node --test tests/slots-win-celebration-contract.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - File exists at tests/slots-win-celebration-contract.test.mjs
    - grep "FX-70" tests/slots-win-celebration-contract.test.mjs returns at least 2 matches
    - grep "FX-72" tests/slots-win-celebration-contract.test.mjs returns at least 2 matches
    - grep "globalCss" tests/slots-win-celebration-contract.test.mjs returns at least 5 matches
    - node --test tests/slots-win-celebration-contract.test.mjs exits 0 with all 4 tests passing
  </acceptance_criteria>
  <done>
    New test file with 4 passing contract tests: 2 proving FX-70 (reel glow keyframes + amplified frame flare) and 2 proving FX-72 (reduced iteration cap + minimal fallback). All tests pass via `node --test`.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CSS presentation layer | No user input surfaces; all effects are CSS attribute-selector driven by existing runtime datasets |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-45-01 | Tampering | data-slots-anim-effect dataset | accept | Dataset is set by trusted runtime.ts; CSS only reads it. Tampering via devtools has no authority impact (presentation-only). |
| T-45-02 | Denial of Service | @keyframes slots-reel-win-pulse | accept | 2-iteration CSS animation with cubic-bezier easing; negligible GPU cost. Minimal-mode fallback disables animation entirely. |
| T-45-03 | Information Disclosure | CSS source | accept | No secrets in CSS; all values are visual presentation tokens. |
</threat_model>

<verification>
1. `grep -c "slots-reel-win-pulse" src/styles/global.css` returns >= 3
2. `grep "rgba(255, 215, 9, 0.58)" src/styles/global.css` returns match (amplified flare)
3. `grep "rgba(0, 229, 255" src/styles/global.css` returns matches (cyan reel glow)
4. `node --test tests/slots-win-celebration-contract.test.mjs` exits 0 with 4 passing tests
5. `npm run build` succeeds (no CSS syntax errors)
</verification>

<success_criteria>
- Win outcomes produce a two-tone celebration: cyan reel glow + amplified gold frame pulse
- @keyframes slots-reel-win-pulse exists with ~0.95s cadence and 2 iterations
- slots-win-flare gold outer glow at 0.58 opacity (up from 0.18) with brightness flash
- reduced intensity: reel-win-pulse capped to 1 iteration
- minimal intensity: reel-window animation disabled, box-shadow fallback present
- 4 contract tests pass proving FX-70 and FX-72
- No new JS/TS files; no authority boundary crossings; no payout-intensity scaling
</success_criteria>

<output>
After completion, create `.planning/phases/45-win-celebration-effects-system/45-01-SUMMARY.md`
</output>
