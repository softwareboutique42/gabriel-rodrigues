---
phase: 26-slots-shell-and-presentation-upgrade
plan: 01
type: execute
wave: 1
depends_on:
  - 24-01
files_modified:
  - src/pages/en/slots/index.astro
  - src/pages/pt/slots/index.astro
  - src/styles/global.css
  - src/i18n/en.json
  - src/i18n/pt.json
  - tests/slots-shell-foundation.test.mjs
  - tests/slots-i18n-parity-contract.test.mjs
  - e2e/compatibility.spec.ts
  - .planning/phases/26-slots-shell-and-presentation-upgrade/26-01-SUMMARY.md
autonomous: true
requirements:
  - VIS-20
  - VIS-21
  - VIS-22
must_haves:
  truths:
    - Slots shell hierarchy, playfield framing, and gameplay HUD become visually stronger on desktop and mobile without altering gameplay authority.
    - EN/PT Slots routes remain structurally mirrored and preserve canonical runtime IDs, datasets, and route behavior.
    - Atmosphere and cabinet-style presentation upgrades remain presentation-only and keep the non-gambling product framing visible.
  artifacts:
    - path: src/pages/en/slots/index.astro
      provides: upgraded EN Slots shell composition with stronger hierarchy, playfield framing, and stable gameplay anchors.
    - path: src/pages/pt/slots/index.astro
      provides: PT shell parity for the upgraded composition and canonical route behavior.
    - path: src/styles/global.css
      provides: shared cabinet/frame/HUD presentation primitives for the refreshed Slots shell.
    - path: tests/slots-shell-foundation.test.mjs
      provides: source-level guardrails against route drift, lifecycle regressions, and shell-structure breakage.
    - path: e2e/compatibility.spec.ts
      provides: browser-level proof that canonical EN/PT Slots flows still function after the shell refresh.
  key_links:
    - from: src/pages/en/slots/index.astro
      to: src/scripts/slots/controller.ts
      via: preserved gameplay IDs and datasets consumed by controller-owned authority updates
      pattern: slots-balance-value|slots-bet-value|slots-spin-button|slots-gameplay-status|slots-gameplay-outcome|slots-round-result
    - from: src/pages/pt/slots/index.astro
      to: src/scripts/slots/controller.ts
      via: mirrored gameplay anchors and canonical PT route shell
      pattern: same IDs and data-slots-* contract as EN page
    - from: src/pages/en/slots/index.astro
      to: src/scripts/slots/animation/runtime.ts
      via: root-level deterministic visual runtime envelope
      pattern: id="slots-shell-root" plus data-slots-* attributes retained
    - from: tests/slots-shell-foundation.test.mjs
      to: src/pages/en/slots/index.astro and src/pages/pt/slots/index.astro
      via: parity and shell-boundary assertions for upgraded structure
      pattern: shell framing|canonical routes|lifecycle hooks|disclaimer visibility
validation_commands:
  - npm run lint
  - node --test tests/slots-shell-foundation.test.mjs tests/slots-i18n-parity-contract.test.mjs tests/slots-core-determinism-contract.test.mjs
  - npx playwright test e2e/compatibility.spec.ts --project=chromium --project=mobile-chrome
  - npm run build
  - npm run lint && node --test tests/slots-shell-foundation.test.mjs tests/slots-i18n-parity-contract.test.mjs tests/slots-core-determinism-contract.test.mjs && npx playwright test e2e/compatibility.spec.ts --project=chromium --project=mobile-chrome && npm run build
---

<objective>
Upgrade the Slots shell, reel framing, and gameplay HUD presentation so the experience feels intentional, readable, and production-ready while preserving deterministic gameplay boundaries.

Purpose: satisfy VIS-20, VIS-21, and VIS-22 with presentation-only shell work and parity-safe validation.
Output: refreshed EN/PT Slots shell composition, shared presentation styling, and regression coverage for canonical runtime stability.
</objective>

<execution_context>
@/home/gabriel/Documents/gabriel-rodrigues/.claude/get-shit-done/workflows/execute-plan.md
@/home/gabriel/Documents/gabriel-rodrigues/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/26-slots-shell-and-presentation-upgrade/26-CONTEXT.md
@.planning/phases/26-slots-shell-and-presentation-upgrade/26-RESEARCH.md
@src/pages/en/slots/index.astro
@src/pages/pt/slots/index.astro
@src/scripts/slots/main.ts
@src/scripts/slots/controller.ts
@src/scripts/slots/animation/runtime.ts
@src/styles/global.css
@tests/slots-shell-foundation.test.mjs
@tests/slots-i18n-parity-contract.test.mjs
@e2e/compatibility.spec.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add red-first contracts for upgraded shell hierarchy, parity, and stable gameplay anchors</name>
  <files>tests/slots-shell-foundation.test.mjs, tests/slots-i18n-parity-contract.test.mjs</files>
  <behavior>
    - Test 1: contracts fail if the upgraded EN/PT Slots pages lose mirrored shell zones, canonical route links, or visible disclaimer framing.
    - Test 2: contracts fail if gameplay anchor IDs or root-level data-slots-* attributes used by controller/runtime are removed or renamed.
    - Test 3: contracts fail if new user-facing shell copy or labels drift across EN/PT parity.
  </behavior>
  <action>
    Strengthen source-level regression coverage before implementation:
    - Extend existing shell and i18n parity contracts to lock the upgraded structural zones and stable gameplay anchors.
    - Keep assertions objective by checking IDs, data attributes, canonical links, and required translation key usage.
    - Preserve lifecycle assertions already enforced for Astro SPA initialization.
  </action>
  <verify>
    <automated>node --test tests/slots-shell-foundation.test.mjs tests/slots-i18n-parity-contract.test.mjs</automated>
  </verify>
  <done>Shell/presentation regressions are detectable before implementation, with parity and runtime-anchor locks in place.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Recompose EN/PT Slots pages into a stronger cabinet-style shell without touching authority logic</name>
  <files>src/pages/en/slots/index.astro, src/pages/pt/slots/index.astro, src/styles/global.css, src/i18n/en.json, src/i18n/pt.json</files>
  <behavior>
    - EN/PT Slots pages expose clearer visual zones for status, playfield, economy HUD, controls, and results on desktop and mobile.
    - Existing gameplay IDs, root attributes, and controller/runtime integration points remain intact.
    - Atmosphere and framing improvements read as production-ready while disclaimers and non-gambling framing remain visible.
  </behavior>
  <action>
    Upgrade the shell composition and styling layer only:
    - Redesign the page layout around a stronger playfield frame and clearer HUD grouping.
    - Introduce shared CSS primitives for cabinet/chrome/background/readout treatments instead of scattering one-off utility strings.
    - Add EN/PT copy only where new visible labels are necessary, maintaining strict key parity.
    - Do not modify payout logic, economy transitions, or visual-event authority boundaries.
  </action>
  <verify>
    <automated>node --test tests/slots-shell-foundation.test.mjs tests/slots-i18n-parity-contract.test.mjs tests/slots-core-determinism-contract.test.mjs</automated>
  </verify>
  <done>Slots shell looks substantially more intentional and readable while all deterministic gameplay hooks remain stable.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Extend browser compatibility checks for refreshed shell parity and release evidence</name>
  <files>e2e/compatibility.spec.ts</files>
  <behavior>
    - Test 1: canonical EN/PT Slots routes still expose the runtime parity envelope after shell refresh.
    - Test 2: browser checks confirm the upgraded shell exposes the expected visual zones without route or lifecycle regressions.
    - Test 3: validation remains timing-agnostic and avoids screenshot-only assertions.
  </behavior>
  <action>
    Extend compatibility coverage to prove the shell refresh is safe to ship:
    - Add stable selector or dataset checks for the upgraded shell composition in EN/PT.
    - Keep gameplay compatibility assertions intact so presentation changes cannot hide authority regressions.
    - Use the existing release chain of lint, node contracts, compatibility E2E, and build as ship evidence for Phase 26.
  </action>
  <verify>
    <automated>npx playwright test e2e/compatibility.spec.ts --project=chromium --project=mobile-chrome</automated>
  </verify>
  <done>Canonical EN/PT Slots routes retain deterministic compatibility guarantees after the presentation refresh.</done>
</task>

</tasks>

<validation_commands>

1. npm run lint
2. node --test tests/slots-shell-foundation.test.mjs tests/slots-i18n-parity-contract.test.mjs tests/slots-core-determinism-contract.test.mjs
3. npx playwright test e2e/compatibility.spec.ts --project=chromium --project=mobile-chrome
4. npm run build
5. npm run lint && node --test tests/slots-shell-foundation.test.mjs tests/slots-i18n-parity-contract.test.mjs tests/slots-core-determinism-contract.test.mjs && npx playwright test e2e/compatibility.spec.ts --project=chromium --project=mobile-chrome && npm run build

</validation_commands>

<verification>
VIS-20, VIS-21, and VIS-22 are complete when the Slots shell is materially upgraded across EN/PT routes, gameplay anchors remain deterministic and unchanged, and the full lint/test/e2e/build chain passes.
</verification>

<success_criteria>

- Slots shell presents a stronger cabinet/playfield frame and clearer HUD grouping on desktop and mobile.
- Balance, bet, state, outcome, and result readouts remain easy to scan and continue working through the existing controller/runtime contract.
- EN/PT pages remain structurally mirrored with canonical route and disclaimer behavior intact.
- Compatibility and contract coverage catch shell regressions without relying on subjective screenshot review.
  </success_criteria>

<output>
After completion, create .planning/phases/26-slots-shell-and-presentation-upgrade/26-01-SUMMARY.md
</output>
