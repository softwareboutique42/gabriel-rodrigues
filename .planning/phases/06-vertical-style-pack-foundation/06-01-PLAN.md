---
phase: 06-vertical-style-pack-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/scripts/canvas/types.ts
  - src/scripts/canvas/style-selector.ts
  - src/scripts/canvas/config-normalization.ts
  - src/scripts/canvas/versions.ts
  - src/scripts/canvas/main.ts
  - src/scripts/canvas/export.ts
  - src/pages/en/canvas/index.astro
  - src/pages/pt/canvas/index.astro
  - src/i18n/en.json
  - src/i18n/pt.json
  - workers/company-api/src/index.ts
  - tests/animation-quality-contract.test.mjs
  - tests/style-selector-matrix-contract.test.mjs
autonomous: true
requirements:
  - PACK-01
  - PACK-02
  - PACK-03
must_haves:
  truths:
    - User can select at least 3 new vertical presets from the canvas selector in EN and PT.
    - Generated configs route deterministically to style defaults for at least 2 additional categories.
    - Existing style matrix mappings remain stable for current categories unless explicitly expanded.
    - New presets preserve seamless 12-second loops and existing easing and quality contracts.
  artifacts:
    - path: src/scripts/canvas/style-selector.ts
      provides: Deterministic routing matrix with legacy mapping safeguards and new category coverage
    - path: src/scripts/canvas/config-normalization.ts
      provides: Single normalization boundary that resolves preset plus animation style for runtime and export
    - path: src/scripts/canvas/versions.ts
      provides: Vertical preset metadata surfaced to UI while preserving existing style arrays
    - path: tests/animation-quality-contract.test.mjs
      provides: Contract-level regression coverage for matrix stability and loop/easing hooks
    - path: tests/style-selector-matrix-contract.test.mjs
      provides: Deterministic routing contract coverage including fallback behavior and tie-breakers
  key_links:
    - from: src/scripts/canvas/style-selector.ts
      to: src/scripts/canvas/config-normalization.ts
      via: selectAnimationStyle and preset resolution used during normalizeCompanyConfig
      pattern: selectAnimationStyle\(|animationStyle:\s*selectAnimationStyle
    - from: src/scripts/canvas/config-normalization.ts
      to: src/scripts/canvas/export.ts
      via: export path consumes normalized config instead of diverging style logic
      pattern: normalizeCompanyConfig|animationStyle|animationParams
    - from: src/scripts/canvas/main.ts
      to: src/pages/en/canvas/index.astro
      via: preset selector wiring and request payload plumbing
      pattern: version-select|preset|animationStyle
---

<objective>
Deliver the Phase 6 vertical style-pack foundation as a deterministic preset layer over existing animation engines.

Purpose: Satisfy PACK-01, PACK-02, and PACK-03 with low-regression risk by extending routing and selector surfaces while preserving loop and quality contracts.
Output: New preset contracts, deterministic category expansion, bilingual selector surface, worker enum parity, and matrix regression tests.
</objective>

<scope>
In scope:
- Add 3 vertical presets exposed in selector UX and version metadata.
- Extend deterministic routing to at least 2 additional categories.
- Keep existing matrix behavior stable for current categories through explicit test locks.
- Ensure runtime and export consume the same normalized style/preset resolution.
- Update worker category sanitization/schema to keep client-worker contract aligned.

Out of scope:

- New animation rendering classes or shader systems.
- New framework, state library, or external dependency.
- Export funnel UX conversion changes (Phase 7).
- Verification automation process hardening (Phase 8).
  </scope>

<assumptions>
- Conservative default A1: implement vertical packs as preset metadata and parameter overrides on top of existing styles (no new engines).
- Conservative default A2: expand categories with education and hospitality for PACK-03 rather than remapping existing categories.
- Conservative default A3: deterministic routing remains source of truth; AI-provided animationStyle continues to be best-effort only.
- If A2 is rejected during execution, fallback is explicit remap of two existing categories with equivalent regression locks.
</assumptions>

<execution_context>
@/home/gabriel/Documents/gabriel-rodrigues/.claude/get-shit-done/workflows/execute-plan.md
@/home/gabriel/Documents/gabriel-rodrigues/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-vertical-style-pack-foundation/06-RESEARCH.md
@src/scripts/canvas/style-selector.ts
@src/scripts/canvas/types.ts
@src/scripts/canvas/animations/index.ts
@src/scripts/canvas/versions.ts
@src/scripts/canvas/export.ts
@workers/company-api/src/index.ts
@tests/animation-quality-contract.test.mjs

<interfaces>
From src/scripts/canvas/style-selector.ts:
- selectAnimationStyle({ version, industryCategory, mood }) is pure and deterministic.
- V1_STYLE_MATRIX and V2_STYLE_MATRIX are current legacy defaults and must be preserved unless explicitly expanded.

From src/scripts/canvas/types.ts:

- IndustryCategory currently includes tech, finance, health, retail, creative, food, other.
- CompanyConfig carries mood, industryCategory, animationStyle, animationParams, visualElements.

From src/scripts/canvas/versions.ts:

- VERSIONS is the UI source for version labels and style availability.

From workers/company-api/src/index.ts:

- VALID_INDUSTRY_CATEGORIES and sanitizeIndustryCategory enforce worker-side enum parity.

From tests/animation-quality-contract.test.mjs:

- Existing contract tests assert selector matrix presence and loop/easing hooks via string patterns.
  </interfaces>
  </context>

<dependencies>
Task 1 -> Task 2 -> Task 3
- Task 2 depends on new contracts and deterministic routing from Task 1.
- Task 3 depends on completed wiring from Tasks 1 and 2.
</dependencies>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define preset and deterministic routing contracts with legacy mapping safeguards</name>
  <files>src/scripts/canvas/types.ts, src/scripts/canvas/style-selector.ts, src/scripts/canvas/versions.ts, workers/company-api/src/index.ts, tests/style-selector-matrix-contract.test.mjs, tests/animation-quality-contract.test.mjs</files>
  <behavior>
    - Test 1: existing v1/v2 mappings for current categories remain unchanged unless intentionally expanded.
    - Test 2: at least two additional categories route deterministically to new preset defaults.
    - Test 3: worker category sanitization accepts new categories and still falls back deterministically.
  </behavior>
  <action>
    Implement PACK-03 contract layer first: add preset identifiers and category expansion in shared types, then extend deterministic selector matrices and tie-breakers while preserving all existing category outputs per stability locks.
    Add version/preset metadata for at least three vertical presets to satisfy PACK-01 surface contracts.
    Update worker enum/schema sanitization in the same task to avoid client/worker drift.
    Add a new matrix contract test file and extend existing animation-quality contract tests with explicit legacy mapping locks.
    Do not introduce new animation engines or external dependencies.
  </action>
  <verify>
    <automated>node --test tests/style-selector-matrix-contract.test.mjs tests/animation-quality-contract.test.mjs</automated>
  </verify>
  <done>
    New preset and category contracts exist, deterministic routing is extended, legacy mappings are locked by tests, and worker enums match client contracts.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire normalized preset resolution through runtime, selector UX, and export path</name>
  <files>src/scripts/canvas/config-normalization.ts, src/scripts/canvas/main.ts, src/scripts/canvas/export.ts, src/pages/en/canvas/index.astro, src/pages/pt/canvas/index.astro, src/i18n/en.json, src/i18n/pt.json</files>
  <behavior>
    - Test 1: preset selection or deterministic default resolves to one normalized animationStyle/config pair used in both runtime and export.
    - Test 2: EN and PT pages expose equivalent selector controls and labels for new presets.
    - Test 3: unknown preset/category input falls back to deterministic safe defaults without breaking render/export.
  </behavior>
  <action>
    Implement PACK-01 and PACK-02 wiring: normalize preset + animation style at a single boundary, then ensure runtime render flow and export flow both consume that normalized result.
    Add selector UI plumbing for EN/PT pages and i18n keys so at least three vertical presets are user-selectable while preserving current style selector behavior.
    Keep loop duration at 12 seconds and preserve existing easing/quality profile hooks by only applying bounded parameter overrides from preset metadata.
    Add guard logic to prevent export-specific style divergence from normalized runtime selection.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>
    Preset selection is visible and functional in EN/PT, runtime/export use aligned normalized config, and loop/easing contracts remain unchanged.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Finalize regression net, execute validation checkpoints, and document release-safe rollback path</name>
  <files>tests/animation-quality-contract.test.mjs, tests/style-selector-matrix-contract.test.mjs, .planning/phases/06-vertical-style-pack-foundation/06-01-SUMMARY.md</files>
  <behavior>
    - Test 1: contract suite catches any change to locked existing mappings.
    - Test 2: contract suite confirms new preset/category routes and fallback determinism.
    - Test 3: full build still succeeds after preset and routing changes.
  </behavior>
  <action>
    Run automated checkpoints in order and only proceed when all pass.
    Produce summary notes with a mapping-diff checklist and rollback trigger conditions.
    Include explicit rollback procedure: revert selector matrix + type/category additions + worker enum updates as one atomic rollback if regression appears in existing categories.
    Record any assumptions validated or rejected during execution in the summary.
  </action>
  <verify>
    <automated>node --test tests/animation-quality-contract.test.mjs tests/style-selector-matrix-contract.test.mjs && npm run build</automated>
  </verify>
  <done>
    Validation checkpoints pass, rollback path is documented, and Phase 6 Plan 01 is ready for execution handoff.
  </done>
</task>

</tasks>

<test_strategy>
Exact tests to update/add:

- Update tests/animation-quality-contract.test.mjs
  - Add PACK-01 assertions for new preset identifiers surfacing in version/style contracts.
  - Add PACK-03 legacy-lock assertions for existing matrix mappings.
  - Add PACK-02 assertions confirming loop/easing hook usage remains intact.
- Add tests/style-selector-matrix-contract.test.mjs
  - Assert deterministic outputs for added categories and mood tie-break paths.
  - Assert deterministic fallback for invalid/unknown category or mood.
- Keep existing command surface:
  - node --test tests/animation-quality-contract.test.mjs tests/style-selector-matrix-contract.test.mjs
  - npm run build
    </test_strategy>

<validation_checkpoints>
Checkpoint 1 (contract gate): selector matrix and quality contracts pass before UI/export wiring finalization.
Checkpoint 2 (build gate): full Astro build passes after runtime/export/UI integration.
Checkpoint 3 (regression gate): legacy mapping locks pass with new category routes enabled.
</validation_checkpoints>

<risks>
- Risk R1: accidental change to legacy mapping matrix for existing categories.
  Mitigation: lock legacy outputs in tests and fail fast in Checkpoint 1.
- Risk R2: runtime/export config drift due to duplicated style logic.
  Mitigation: enforce normalization boundary and verify both paths consume normalized result.
- Risk R3: worker/client category enum mismatch.
  Mitigation: update both in Task 1 and include enum/sanitizer assertions.
- Risk R4: loop seam regression from aggressive preset params.
  Mitigation: keep LOOP_DURATION unchanged and bound overrides to existing tested ranges.
</risks>

<rollback_strategy>
Trigger rollback if any legacy mapping lock fails or if runtime/export resolved style diverges for same input.
Rollback steps:

1. Revert matrix and tie-breaker edits in src/scripts/canvas/style-selector.ts.
2. Revert category/preset type expansions in src/scripts/canvas/types.ts and worker enum changes in workers/company-api/src/index.ts.
3. Revert selector UI/preset wiring in src/scripts/canvas/main.ts and canvas pages while keeping unrelated fixes.
4. Re-run node --test tests/animation-quality-contract.test.mjs and npm run build to confirm restored baseline.
   </rollback_strategy>

<conventional_commit_suggestions>

1. test(phase-06): add deterministic style-selector matrix contract and legacy mapping locks
2. feat(canvas): add vertical preset contracts and deterministic category routing for education and hospitality
3. feat(canvas): wire preset selector through normalization, EN/PT UI, and export parity
4. chore(worker): align industry category sanitization with Phase 6 preset routing contracts
   </conventional_commit_suggestions>

<verification>
- node --test tests/animation-quality-contract.test.mjs tests/style-selector-matrix-contract.test.mjs
- npm run build
- Manual smoke: npm run dev then verify preset selector behavior in /en/canvas/ and /pt/canvas/ with deterministic defaults.
</verification>

<success_criteria>

- PACK-01: 3+ vertical presets are visible/selectable in EN/PT selector and render successfully.
- PACK-02: presets preserve 12-second seam-safe looping and existing easing/quality behavior.
- PACK-03: deterministic routing includes at least 2 additional categories while legacy mappings remain stable.
- No new framework or dependency introduced; Astro static architecture remains intact.
  </success_criteria>

<output>
After completion, create .planning/phases/06-vertical-style-pack-foundation/06-01-SUMMARY.md
</output>
