---
phase: 33-bridge-and-authority-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/scripts/slots/main.ts
  - src/scripts/casinocraftz/tutorial/main.ts
  - src/scripts/casinocraftz/tutorial/types.ts
  - tests/casinocraftz-tutorial-contract.test.mjs
  - tests/compatibility-contract.test.mjs
  - tests/slots-i18n-parity-contract.test.mjs
  - e2e/compatibility.spec.ts
  - .planning/debug/bridge-validation-report.json
  - .planning/phases/33-bridge-and-authority-hardening/33-VALIDATION.md
  - .planning/phases/33-bridge-and-authority-hardening/33-VERIFICATION.md
  - .planning/phases/33-bridge-and-authority-hardening/33-01-SUMMARY.md
autonomous: true
requirements:
  - BRG-50
  - BRG-51
validation_commands:
  - node --test tests/casinocraftz-tutorial-contract.test.mjs tests/compatibility-contract.test.mjs tests/slots-i18n-parity-contract.test.mjs
  - npx playwright test e2e/compatibility.spec.ts --project=chromium --workers=1 --grep "casinocraftz embeds slots module with canonical EN/PT host parity|slots runtime compatibility keeps machine-readable gameplay state in EN/PT|casinocraftz tutorial advances to probability reveal after three spins in EN/PT"
  - npx playwright test e2e/compatibility.spec.ts --project=chromium --workers=1 --grep "casinocraftz" --reporter=json > .planning/debug/bridge-validation-report.json
  - npm run lint
  - npm run build
must_haves:
  truths:
    - Bridge contract is explicitly versioned (v1) and accepts legacy unversioned payloads without mutation.
    - Unknown bridge versions and malformed events are silently ignored without mutating tutorial or Slots authority state.
    - Tutorial progression remains deterministic and is triggered only by accepted bridge events (no spontaneous UI-driven transitions).
    - Tutorial and starter-card layers do not import or mutate Slots authoritative state (RNG, payout, economy).
    - Bridge behavior is deterministic across EN/PT routes and embedded/standalone host modes.
    - No net-new tutorial steps, card mechanics, or gameplay effects are introduced in this phase.
  artifacts:
    - path: src/scripts/slots/main.ts
      provides: versioned bridge event sender with backward-compatible payload envelope
      exports: initSlotsShell() function emits versioned ccz:spin-settled events
    - path: src/scripts/casinocraftz/tutorial/main.ts
      provides: tolerant bridge event receiver with safe parser and fail-safe validation
      exports: mountTutorial() function accepts versioned and legacy ccz:spin-settled events
    - path: src/scripts/casinocraftz/tutorial/types.ts
      provides: explicit bridge event types to anchor authority boundary contracts
      exports: types for BridgeEvent, SpinSettledPayload, etc.
    - path: tests/casinocraftz-tutorial-contract.test.mjs
      provides: source-level bridge versioning and fail-safe validation contracts
      min_lines: 50
    - path: tests/compatibility-contract.test.mjs
      provides: canonical route authority-isolation and anti-monetization contracts
      min_lines: 40
    - path: tests/slots-i18n-parity-contract.test.mjs
      provides: EN/PT parity and host-mode invariant contracts
      min_lines: 40
    - path: e2e/compatibility.spec.ts
      provides: Chromium checks for embedded/standalone bridge determinism and parity
      min_lines: 200
    - path: .planning/debug/bridge-validation-report.json
      provides: machine-readable Playwright evidence for bridge validation
    - path: .planning/phases/33-bridge-and-authority-hardening/33-VALIDATION.md
      provides: requirement-to-command validation architecture for BRG-50, BRG-51
    - path: .planning/phases/33-bridge-and-authority-hardening/33-VERIFICATION.md
      provides: executed validation chain outcomes for BRG-50/BRG-51
    - path: .planning/phases/33-bridge-and-authority-hardening/33-01-SUMMARY.md
      provides: final bridge hardening implementation and evidence summary
  key_links:
    - from: src/scripts/slots/main.ts
      to: src/scripts/casinocraftz/tutorial/main.ts
      via: versioned ccz:spin-settled bridge event envelope
      pattern: 'postMessage.*ccz:spin-settled|addEventListener.*message'
    - from: src/scripts/slots/main.ts
      to: e2e/compatibility.spec.ts
      via: deterministic bridge emission across embedded host parity and EN/PT modes
      pattern: 'data-slots-state|data-slots-host|data-slots-seed|data-slots-outcome'
    - from: src/scripts/casinocraftz/tutorial/main.ts
      to: tests/casinocraftz-tutorial-contract.test.mjs
      via: safe bridge parser behavior for versioned, legacy, and malformed payloads
      pattern: 'onSpinMessage|recordSpin|unlockCards|data-casinocraftz-tutorial-step'
    - from: src/scripts/casinocraftz/tutorial/types.ts
      to: src/scripts/casinocraftz/tutorial/main.ts
      via: explicit type boundaries ensure tutorial does not mutate Slots authority
      pattern: 'import.*types|TutorialState|UtilityCardId'
    - from: src/scripts/casinocraftz/tutorial/engine.ts
      to: src/scripts/slots/main.ts
      via: tutorial engine remains read-only consumer of bridge events (no feedback)
      pattern: 'recordSpin|advanceTutorialStep|export'
---

<objective>
Harden the deterministic Slots-to-tutorial bridge event contract with explicit versioning, backward compatibility, and fail-safe validation while maintaining strict authority boundaries between tutorial/card presentation and Slots gameplay authority.

Purpose: Implement BRG-50 (versioned bridge with backward compatibility) and BRG-51 (presentation-only authority boundary isolation) through contract evolution, safe parsing, and deterministic tests across EN/PT and host modes without adding net-new gameplay features.

Output: Versioned bridge envelope in sender and receiver, safe parser with fail-safe validation, updated source and browser contracts, and release evidence artifacts (`.planning/debug/bridge-validation-report.json`, `33-VALIDATION.md`, `33-VERIFICATION.md`, `33-01-SUMMARY.md`).
</objective>

<execution_context>
@/home/gabriel/Documents/gabriel-rodrigues/.claude/get-shit-done/workflows/execute-plan.md
@/home/gabriel/Documents/gabriel-rodrigues/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/33-bridge-and-authority-hardening/33-CONTEXT.md
@.planning/phases/33-bridge-and-authority-hardening/33-RESEARCH.md
@.planning/phases/32-casinocraftz-integration-confidence-lock/32-01-SUMMARY.md
@src/scripts/slots/main.ts
@src/scripts/casinocraftz/tutorial/main.ts
@src/scripts/casinocraftz/tutorial/types.ts
@src/scripts/casinocraftz/tutorial/engine.ts
@src/scripts/casinocraftz/tutorial/cards.ts
@tests/casinocraftz-tutorial-contract.test.mjs
@tests/compatibility-contract.test.mjs
@tests/slots-i18n-parity-contract.test.mjs
@e2e/compatibility.spec.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define and evolve bridge event types with explicit versioning and add safe parser implementation</name>
  <files>src/scripts/casinocraftz/tutorial/types.ts, src/scripts/slots/main.ts, src/scripts/casinocraftz/tutorial/main.ts</files>
  <action>
    1. In `src/scripts/casinocraftz/tutorial/types.ts`, add explicit TypeScript types for versioned bridge envelope:
       - Define `BridgeEvent` base type with required `type` field
       - Define `SpinSettledV1Payload` (v1) with `spinIndex: number`
       - Define `SpinSettledLegacyPayload` (unversioned) with `spinIndex: number` for backward compatibility
       - Define `CczSpinSettledEvent` discriminated union accepting v1 with `version: 1` or legacy without version
       - Export all types for contract reference

    2. In `src/scripts/slots/main.ts` (line 44–48), evolve the sender to emit versioned envelope:
       - Change `window.parent.postMessage({ type: 'ccz:spin-settled', spinIndex: event.spinIndex }, '*')`
       - To: `window.parent.postMessage({ type: 'ccz:spin-settled', version: 1, payload: { spinIndex: event.spinIndex } }, '*')`
       - Keep try/catch for cross-origin failures (existing)
       - No other behavior changes

    3. In `src/scripts/casinocraftz/tutorial/main.ts`, implement safe parser function:
       - Create `parseSpinSettledBridgeEvent(data: unknown): { spinIndex: number } | null`
       - Accept v1 versioned payload: if `data?.type === 'ccz:spin-settled'` and `data?.version === 1`, extract `data?.payload?.spinIndex`
       - Accept legacy unversioned payload: if `data?.type === 'ccz:spin-settled'` and `data?.version` is undefined, extract `data?.spinIndex` (assumption: implicit v0)
       - Validate spinIndex is an integer >= 0; return null if invalid
       - Ignore unknown versions >= 2 (return null, no error thrown)
       - Return null for non-object or missing type
       - Update receiver (onSpinMessage, line 228) to call parser and return early if null (fail-safe: no state mutation on invalid events)
       - All changes inline, no new imports, keep deterministic behavior

    Reference: Per BRG-50 (research.md: Pattern 1), sender and receiver must support versioned envelope while accepting legacy shape without breaking existing flows.

  </action>
  <verify>
    <automated>npm run lint -- src/scripts/slots/main.ts src/scripts/casinocraftz/tutorial/main.ts src/scripts/casinocraftz/tutorial/types.ts</automated>
  </verify>
  <done>
    - `src/scripts/casinocraftz/tutorial/types.ts` exports CczSpinSettledEvent and related types.
    - `src/scripts/slots/main.ts` emits versioned envelope with `version: 1` and `payload` field.
    - `src/scripts/casinocraftz/tutorial/main.ts` has safe parser that accepts v1, legacy, and rejects unknown/malformed (return null).
    - ESLint passes with no errors.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Harden source-level bridge versioning and authority-isolation contracts with fail-safe validation tests</name>
  <files>tests/casinocraftz-tutorial-contract.test.mjs, tests/compatibility-contract.test.mjs, tests/slots-i18n-parity-contract.test.mjs</files>
  <behavior>
    - Bridge versioning contract asserts v1 receiver accepts v1 versioned payload (version: 1, payload: { spinIndex }).
    - Bridge backward-compatibility contract asserts v1 receiver accepts legacy unversioned payload (no version, spinIndex).
    - Bridge fail-safe contract asserts unknown versions (version: 2+) are silently ignored without state mutation.
    - Bridge fail-safe contract asserts malformed events (non-object, missing type, invalid spinIndex) are silently ignored.
    - Authority isolation contract asserts tutorial/card modules do not import Slots authority internals (RNG, payout, economy modules).
    - Authority isolation contract asserts machine-readable Slots authority outputs (data-slots-seed, data-slots-outcome, data-slots-state) remain read-only from tutorial perspective.
    - EN/PT parity contract asserts bridge event handling determinism is identical across EN and PT locale fixtures.
    - Host-mode invariant contract asserts bridge behavior is deterministic in embedded (casinocraftz host) and standalone modes.
  </behavior>
  <action>
    1. Update `tests/casinocraftz-tutorial-contract.test.mjs`:
       - Add test suite "Bridge Versioning and Backward Compatibility":
         - Test: parseSpinSettledBridgeEvent accepts v1 (version: 1, payload: { spinIndex: N })
         - Test: parseSpinSettledBridgeEvent accepts legacy (no version, spinIndex: N)
         - Test: parseSpinSettledBridgeEvent returns null for unknown version (version: 2)
         - Test: parseSpinSettledBridgeEvent returns null for malformed (null, undefined, non-object, missing type)
         - Test: onSpinMessage does not mutate state when parser returns null (fail-safe)
       - Add test suite "Authority Isolation":
         - Test: assert tutorial/engine.ts does not import slots/* modules
         - Test: assert tutorial/cards.ts does not import slots/* modules
         - Test: assert tutorial/main.ts does not call or reference Slots RNG/payout/economy functions
    2. Update `tests/compatibility-contract.test.mjs`:
       - Add/enhance test: canonical routes /en/casinocraftz/ and /pt/casinocraftz/ are reachable
       - Add test: data-casinocraftz-* attributes remain present and projectable from tutorial state
    3. Update `tests/slots-i18n-parity-contract.test.mjs`:
       - Add test: EN/PT routes emit and receive bridge events deterministically (no locale-specific branch)
       - Add test: embedded and standalone host modes do not have divergent bridge handling logic
    Use `assert()` and `test()` from node:test. Keep mature/existing matcher style. Reuse fixture setup from Phase 32 contracts if available.
    
    Reference: Per BRG-50 and BRG-51 (CONTEXT.md), bridge validation must cover versioning, backward compatibility, fail-safe, and authority isolation deterministically.
  </action>
  <verify>
    <automated>node --test tests/casinocraftz-tutorial-contract.test.mjs tests/compatibility-contract.test.mjs tests/slots-i18n-parity-contract.test.mjs</automated>
  </verify>
  <done>
    - All three contract suites run green.
    - casinocraftz-tutorial-contract covers versioning, backward compatibility, fail-safe payloads, and authority isolation.
    - compatibility-contract covers canonical routes and authority projection.
    - slots-i18n-parity-contract covers EN/PT and embedded/standalone determinism.
    - No production behavior changes outside typed bridge envelope.
  </done>
</task>

<task type="auto">
  <name>Task 3: Lock EN/PT browser parity and deterministic host-mode bridge validation through Playwright compatibility suite</name>
  <files>e2e/compatibility.spec.ts</files>
  <behavior>
    - Chromium checks assert embedded casinocraftz host mode emits and receives versioned ccz:spin-settled events in both EN and PT.
    - Chromium checks assert standalone Slots mode remains unaffected by bridge versioning and maintains deterministic machine-readable state.
    - Chromium checks assert tutorial progression advances deterministically from versioned bridge events in EN/PT (e.g., prob-reveal at spin 3).
    - Chromium checks assert no console errors or page crashes occur during bridge event exchanges in either mode or locale.
    - Chromium checks assert data-slots-* and data-casinocraftz-* machine-readable attributes remain stable markers for parity validation.
  </behavior>
  <action>
    Refine existing targeted compatibility specs in `e2e/compatibility.spec.ts` (do not add gameplay features) to deterministically assert bridge versioning and authority parity:
    - Enhance or add spec: "casinocraftz embeds slots module with versioned bridge event parity in EN/PT"
      - Test embedded /en/casinocraftz/ and /pt/casinocraftz/ routes
      - Trigger spin and validate bridge event is received (machine-readable markers after spin-settled)
      - Assert tutorial state transitions deterministically from spin-settled events
    - Enhance or add spec: "slots runtime compatibility keeps machine-readable gameplay state stable and read-only from embedded host"
      - Test embedded mode with host=casinocraftz
      - Verify data-slots-seed, data-slots-outcome, data-slots-state remain readable
      - Verify tutorial cannot mutate these attributes (read-only from tutorial perspective)
    - Reuse existing test structure and assertions. Keep deterministic focus (no flaky timing, no UI screenshot comparisons).
    - Reference Phase 32 Chromium suite pattern for consistency.
  </action>
  <verify>
    <automated>npx playwright test e2e/compatibility.spec.ts --project=chromium --workers=1 --grep "casinocraftz embeds slots module with canonical EN/PT host parity|slots runtime compatibility keeps machine-readable gameplay state in EN/PT|casinocraftz tutorial advances to probability reveal after three spins in EN/PT"</automated>
  </verify>
  <done>
    - Targeted Chromium specs pass for embedded and standalone host modes in EN/PT.
    - Bridge event determinism is verified through machine-readable dataset assertions.
    - Tutorial progression parity is confirmed across locales.
    - No new gameplay features or card mechanics are introduced.
  </done>
</task>

<task type="auto">
  <name>Task 4: Execute release evidence chain and publish Phase 33 bridge hardening artifacts</name>
  <files>.planning/debug/bridge-validation-report.json, .planning/phases/33-bridge-and-authority-hardening/33-VALIDATION.md, .planning/phases/33-bridge-and-authority-hardening/33-VERIFICATION.md, .planning/phases/33-bridge-and-authority-hardening/33-01-SUMMARY.md</files>
  <action>
    Run the full bridge hardening validation chain in this order:
    1. Node contract tests: `node --test tests/casinocraftz-tutorial-contract.test.mjs tests/compatibility-contract.test.mjs tests/slots-i18n-parity-contract.test.mjs`
    2. Targeted Playwright parity checks: `npx playwright test e2e/compatibility.spec.ts --project=chromium --workers=1 --grep "casinocraftz embeds slots module with canonical EN/PT host parity|slots runtime compatibility keeps machine-readable gameplay state in EN/PT|casinocraftz tutorial advances to probability reveal after three spins in EN/PT"`
    3. Playwright JSON evidence export: `npx playwright test e2e/compatibility.spec.ts --project=chromium --workers=1 --grep "casinocraftz" --reporter=json > .planning/debug/bridge-validation-report.json`
    4. Lint: `npm run lint`
    5. Production build: `npm run build`
    
    Create `33-VALIDATION.md`:
    - Map BRG-50 to contract assertions and commands
    - Map BRG-51 to contract assertions and commands
    - Include deterministic EN/PT and host-mode verification checkpoints
    
    Create `33-VERIFICATION.md`:
    - Record exact commands executed
    - Log pass/fail results
    - Reference artifact paths (.planning/debug/bridge-validation-report.json, test output)
    
    Create `33-01-SUMMARY.md`:
    - Requirement closure evidence for BRG-50 (versioned bridge with backward compatibility)
    - Requirement closure evidence for BRG-51 (authority boundary isolation)
    - Production build artifact hash and status
    - Any scoped non-blocking warnings or notes
    
    Keep all artifacts aligned to Phase 32 summary pattern for consistency.
  </action>
  <verify>
    <automated>node --test tests/casinocraftz-tutorial-contract.test.mjs tests/compatibility-contract.test.mjs tests/slots-i18n-parity-contract.test.mjs && npx playwright test e2e/compatibility.spec.ts --project=chromium --workers=1 --grep "casinocraftz embeds slots module with canonical EN/PT host parity|slots runtime compatibility keeps machine-readable gameplay state in EN/PT|casinocraftz tutorial advances to probability reveal after three spins in EN/PT" && npx playwright test e2e/compatibility.spec.ts --project=chromium --workers=1 --grep "casinocraftz" --reporter=json > .planning/debug/bridge-validation-report.json && npm run lint && npm run build</automated>
  </verify>
  <done>
    - All validation commands complete successfully.
    - `.planning/debug/bridge-validation-report.json` exists and is non-empty.
    - `33-VALIDATION.md` maps BRG-50, BRG-51 to deterministic automated commands with EN/PT and host-mode checkpoints.
    - `33-VERIFICATION.md` logs exact executed commands and outcomes.
    - `33-01-SUMMARY.md` presents requirement closure evidence and production build status.
    - No production feature files modified outside versioned bridge envelope and validation scope.
  </done>
</task>

</tasks>

<verification>
- Validate all `validation_commands` complete successfully in one run.
- Confirm `.planning/debug/bridge-validation-report.json` is generated and machine-readable (JSON format).
- Confirm `33-VALIDATION.md` maps BRG-50 (versioned, backward-compatible bridge) and BRG-51 (authority boundary) to deterministic assertions.
- Confirm `33-VERIFICATION.md` references exact command lines, pass/fail status, and artifact paths.
- Confirm EN/PT parity checks pass deterministically (no locale-specific divergence).
- Confirm embedded and standalone host-mode checks pass deterministically (bridge versioning does not break standalone).
- Confirm no production feature files are modified outside bridge envelope and contract scope (Task 1-3).
- Confirm `src/scripts/slots/main.ts` emits versioned envelope; `src/scripts/casinocraftz/tutorial/main.ts` accepts versioned and legacy with fail-safe.
</verification>

<success_criteria>

- **BRG-50:** Deterministic evidence proves versioned (v1) and legacy ccz:spin-settled payloads are both accepted by receiver without state mutation on unknown/malformed events. Contract tests and Playwright checks confirm determinism across EN/PT and embedded/standalone host modes.
- **BRG-51:** Authority isolation is explicit and testable: tutorial/card modules do not import or call Slots RNG/payout/economy functions. Machine-readable Slots state (data-slots-\*) remains read-only from tutorial perspective. Source contracts and browser checks assert no authority drift.
- **Release Evidence:** All validation commands are reproducible, lint and build pass, `.planning/debug/bridge-validation-report.json` is captured, and `33-VALIDATION.md` / `33-VERIFICATION.md` / `33-01-SUMMARY.md` document requirement closure.
- **No Feature Expansion:** No net-new tutorial steps, card mechanics, or gameplay effects are introduced; only bridge contract hardening and validation.

</success_criteria>

<output>
After completion, create `.planning/phases/33-bridge-and-authority-hardening/33-01-SUMMARY.md`.
</output>
