---
phase: 11-slots-shell-foundation
plan: 01
type: execute
wave: 3
depends_on:
  - 10-01
files_modified:
  - src/pages/en/slots/index.astro
  - src/pages/pt/slots/index.astro
  - src/scripts/slots/main.ts
  - src/i18n/en.json
  - src/i18n/pt.json
  - tests/slots-shell-foundation.test.mjs
autonomous: true
requirements:
  - SLOT-01
  - SLOT-02
  - SLOT-03
must_haves:
  truths:
    - EN and PT Slots shell pages exist at /en/slots/ and /pt/slots/ with mirrored structure and explicit in-development messaging above the fold (SLOT-01).
    - Both locale pages visibly render direct non-gambling and no-real-money disclaimer copy as body content, not only metadata or footer text (SLOT-02).
    - Slots shell bootstrap executes through astro:page-load and remains idempotent across SPA navigations through init and cleanup guards (SLOT-03).
    - Canonical routing remains intact with no /projects/slots or other alias-route links introduced from Slots shell CTAs (SLOT-01).
    - Automated contract tests fail if shell scope expands into gameplay or monetization terms, if disclaimer parity drifts, or if lifecycle wiring regresses (SLOT-01, SLOT-02, SLOT-03).
  artifacts:
    - path: src/pages/en/slots/index.astro
      provides: EN shell page with in-development messaging, visible disclaimer block, canonical return CTA, and SPA bootstrap trigger
    - path: src/pages/pt/slots/index.astro
      provides: PT shell page mirroring EN shell structure and disclaimer behavior
    - path: src/scripts/slots/main.ts
      provides: Idempotent slots shell bootstrap with AbortController-based cleanup contract
    - path: src/i18n/en.json
      provides: EN slots shell copy keys for status and disclaimer text used by page content
    - path: src/i18n/pt.json
      provides: PT slots shell copy keys with strict parity to EN keys
    - path: tests/slots-shell-foundation.test.mjs
      provides: Node contract coverage for SLOT-01, SLOT-02, SLOT-03 and shell-only scope lock
  key_links:
    - from: src/pages/en/slots/index.astro
      to: src/scripts/slots/main.ts
      via: astro:page-load listener that guards by slots shell root element before init
      pattern: document.addEventListener('astro:page-load'...) plus DOM root guard
    - from: src/pages/pt/slots/index.astro
      to: src/scripts/slots/main.ts
      via: mirrored SPA bootstrap trigger and root guard
      pattern: same lifecycle contract as EN page
    - from: src/pages/en/slots/index.astro
      to: /en/projects/
      via: primary return CTA href
      pattern: canonical locale route only, no /en/projects/slots alias
    - from: src/pages/pt/slots/index.astro
      to: /pt/projects/
      via: primary return CTA href
      pattern: canonical locale route only, no /pt/projects/slots alias
    - from: src/scripts/slots/main.ts
      to: Astro SPA transitions
      via: AbortController teardown and astro:before-swap cleanup
      pattern: previous controller abort at init start plus cleanup hook
    - from: tests/slots-shell-foundation.test.mjs
      to: src/pages/en/slots/index.astro and src/pages/pt/slots/index.astro
      via: static source assertions for disclaimer visibility, parity, and canonical links
      pattern: SLOT-01 and SLOT-02 contracts
---

<objective>
Deliver Phase 11 shell-only Slots foundation with bilingual EN/PT routes, explicit compliance-forward disclaimer behavior, and SPA-safe bootstrap lifecycle wiring.

Purpose: Satisfy SLOT-01, SLOT-02, and SLOT-03 while strictly preventing gameplay, monetization, and alias-route expansion in this milestone.
Output: EN/PT slots shell pages, minimal lifecycle-safe client bootstrap module, parity-matched locale copy, and dedicated contract tests.
</objective>

<execution_context>
@/home/gabriel/Documents/gabriel-rodrigues/.claude/get-shit-done/workflows/execute-plan.md
@/home/gabriel/Documents/gabriel-rodrigues/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-slots-shell-foundation/11-CONTEXT.md
@.planning/phases/11-slots-shell-foundation/11-RESEARCH.md
@.planning/phases/09-navigation-and-i18n-primitives/09-01-SUMMARY.md
@.planning/phases/10-projects-hub-delivery/10-01-SUMMARY.md

@src/pages/en/projects/index.astro
@src/pages/pt/projects/index.astro
@src/components/LanguageSwitcher.astro
@src/i18n/utils.ts
@src/scripts/canvas/main.ts
@src/i18n/en.json
@src/i18n/pt.json
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create red-first Slots shell contract suite for scope, disclaimer, canonical route, and lifecycle guarantees</name>
  <files>tests/slots-shell-foundation.test.mjs</files>
  <behavior>
    - SLOT-01: test fails when either locale slots page is missing, lacks in-development shell messaging, or breaks EN/PT structural parity.
    - SLOT-02: test fails when disclaimer text is absent, not visible in page body, or lacks explicit non-gambling and no-real-money semantics in either locale.
    - SLOT-03: test fails when astro:page-load trigger, DOM root guard, AbortController abort-at-init, or astro:before-swap cleanup contract is missing.
    - Scope lock: test fails if slots pages introduce gameplay, RNG, payout, wager, or monetization semantics.
    - Canonical lock: test fails if /projects/slots alias links appear in EN/PT slots pages.
  </behavior>
  <action>Add a focused node:test contract file that statically inspects EN/PT slots pages, slots bootstrap module, and locale dictionaries. Write explicit assertions for shell-only boundaries, disclaimer semantics, and SPA lifecycle wiring. Keep this suite limited to Phase 11 contracts and do not add broader E2E matrix work reserved for Phase 12.</action>
  <verify>
    <automated>node --test tests/slots-shell-foundation.test.mjs</automated>
  </verify>
  <done>Dedicated Slots shell contract suite exists and captures failing conditions before implementation for SLOT-01, SLOT-02, SLOT-03 plus scope lock constraints.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement EN/PT slots shell pages and locale parity copy with explicit compliance-forward disclaimer behavior (per D-01, D-02, D-04)</name>
  <files>src/pages/en/slots/index.astro, src/pages/pt/slots/index.astro, src/i18n/en.json, src/i18n/pt.json</files>
  <behavior>
    - EN and PT slots pages render mirrored shell sections: title, in-development status, concise description, visible disclaimer block, and primary return-to-projects CTA.
    - Disclaimer copy in both locales explicitly states not gambling and no real-money wagering.
    - All newly introduced slots shell keys exist with strict EN/PT parity.
    - Links remain canonical locale routes and avoid any /projects/slots alias routing.
  </behavior>
  <action>Build bilingual slots shell pages using repository page conventions, with visible in-development messaging above the fold and a compliance-forward disclaimer block in body content. Update locale dictionaries in the same change for parity. Keep shell informational only and explicitly avoid gameplay controls, RNG terms, payout displays, payment hooks, analytics instrumentation, and alias routes.</action>
  <verify>
    <automated>node --test tests/slots-shell-foundation.test.mjs --test-name-pattern "SLOT-01|SLOT-02|scope|canonical"</automated>
  </verify>
  <done>EN/PT slots shell pages and locale copy satisfy SLOT-01 and SLOT-02 with strict shell-only scope and canonical route behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement SPA-safe slots bootstrap lifecycle contract and complete full phase contract verification (per D-03)</name>
  <files>src/scripts/slots/main.ts, src/pages/en/slots/index.astro, src/pages/pt/slots/index.astro</files>
  <behavior>
    - Slots bootstrap is initialized through astro:page-load from both locale pages.
    - Initialization is idempotent by aborting any previous controller before attaching listeners.
    - Cleanup path on astro:before-swap is present to prevent duplicate listeners across client navigation.
    - DOM root guard prevents initialization when slots shell root is absent.
  </behavior>
  <action>Create minimal slots bootstrap implementation in src/scripts/slots/main.ts aligned with existing canvas lifecycle discipline. Wire both locale pages to trigger init only when slots shell root exists. Keep behavior minimal and lifecycle-focused only; do not add gameplay logic, animation systems, or monetization interactions in this phase.</action>
  <verify>
    <automated>node --test tests/nav-i18n-primitives.test.mjs tests/projects-hub-delivery.test.mjs tests/slots-shell-foundation.test.mjs tests/export-contract.test.mjs && npm run build</automated>
  </verify>
  <done>SLOT-03 lifecycle contract is implemented and all relevant node contract suites plus production build succeed.</done>
</task>

</tasks>

<verification>
- node --test tests/slots-shell-foundation.test.mjs
- node --test tests/nav-i18n-primitives.test.mjs tests/projects-hub-delivery.test.mjs tests/slots-shell-foundation.test.mjs tests/export-contract.test.mjs
- npm run build
</verification>

<success_criteria>

- /en/slots/ and /pt/slots/ ship as shell-only foundation pages with mirrored structure and explicit in-development messaging.
- Both slots pages display direct non-gambling and no-real-money disclaimer copy in visible body content with EN/PT parity.
- Slots bootstrap contract uses astro:page-load trigger, DOM root guard, AbortController idempotence, and astro:before-swap cleanup.
- Canonical route behavior remains intact with locale-correct return links and no alias routes.
- Gameplay, RNG, payout, wagering, payment, and analytics expansion remains out of scope and blocked by automated contracts.
  </success_criteria>

<output>
After completion, create .planning/phases/11-slots-shell-foundation/11-01-SUMMARY.md
</output>
