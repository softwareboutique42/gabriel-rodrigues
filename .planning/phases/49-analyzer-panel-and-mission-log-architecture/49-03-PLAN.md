---
phase: 49-analyzer-panel-and-mission-log-architecture
plan: 03
type: execute
wave: 2
depends_on:
  - 49-01
files_modified:
  - src/pages/pt/casinocraftz/index.astro
autonomous: true
requirements:
  - DTL-01
  - DTL-02

must_haves:
  truths:
    - "PT lobby has the same two-column grid structure as the EN lobby (Plan 02)"
    - "PT lobby uses Portuguese i18n keys for all new labels (PAINEL ANALISADOR, REGISTRO DE MISSÃO, etc.)"
    - "The 780px iframe is removed from the PT lobby"
    - "All data-casinocraftz-* attributes on card and tutorial elements are preserved, lang param is 'pt'"
  artifacts:
    - path: "src/pages/pt/casinocraftz/index.astro"
      provides: "Restructured PT lobby — structural mirror of EN version with PT i18n keys and lang=pt"
      contains: "data-ccz-mission-log-toggle, data-ccz-analyzer-toggle, class=\"ccz-lobby-grid\""
  key_links:
    - from: "src/pages/pt/casinocraftz/index.astro"
      to: "src/i18n/pt.json"
      via: "t('lobby.analyzerPanel.label') and other new keys"
---

<objective>
Mirror the EN lobby restructure (Plan 02) in the PT route. The markup structure is identical; only i18n keys and data-casinocraftz-lang differ.

Purpose: DTL-02 requires EN/PT parity for Analyzer Panel and Mission Log behavior. PT route must have the same structural changes.
Output: PT astro page restructured identically to EN, using PT translation keys.
</objective>

<execution_context>
@/home/gabriel/Documents/gabriel-rodrigues/.claude/get-shit-done/workflows/execute-plan.md
@/home/gabriel/Documents/gabriel-rodrigues/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/49-analyzer-panel-and-mission-log-architecture/49-CONTEXT.md
@.planning/phases/49-analyzer-panel-and-mission-log-architecture/49-UI-SPEC.md
@.planning/phases/49-analyzer-panel-and-mission-log-architecture/49-01-SUMMARY.md
@.planning/phases/49-analyzer-panel-and-mission-log-architecture/49-02-SUMMARY.md

<interfaces>
<!-- REFERENCE: The completed EN restructure is the authoritative layout blueprint. -->
<!-- Read src/pages/en/casinocraftz/index.astro (as restructured by Plan 02) FIRST. -->
<!-- Then apply identical structural changes to src/pages/pt/casinocraftz/index.astro. -->

<!-- Key differences between EN and PT versions: -->
<!--
  EN: data-casinocraftz-lang="en"  →  PT: data-casinocraftz-lang="pt"
  EN: href="/en/slots/"            →  PT: href="/pt/slots/"
  EN: href="/en/projects/"         →  PT: href="/pt/projects/"
  EN: iframe src="/en/slots/..."   →  PT: (iframe REMOVED in both)
  EN: const t = useTranslations('en')  →  PT: const t = useTranslations('pt')
  EN: import ... from '../../../i18n/utils'  →  PT: same (relative path may differ by one dir level)
  All t('key') calls: same keys, different locale outputs
-->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Apply EN layout restructure to PT lobby</name>
  <files>src/pages/pt/casinocraftz/index.astro</files>
  <read_first>
    - src/pages/en/casinocraftz/index.astro (read the fully restructured EN version — this is the source of truth for the layout changes)
    - src/pages/pt/casinocraftz/index.astro (read the current PT version to understand what differs: locale, href paths, lang attribute)
    - src/i18n/pt.json (lines 65-82 to confirm new PT keys, e.g. "PAINEL ANALISADOR ▼")
  </read_first>
  <action>
Apply the IDENTICAL structural changes from the EN lobby (src/pages/en/casinocraftz/index.astro) to the PT lobby (src/pages/pt/casinocraftz/index.astro).

The structure is a mirror — same class names, same data attributes, same element hierarchy. The only differences are:
- `data-casinocraftz-lang="pt"` (already set in PT file — do not change)
- `const t = useTranslations('pt')` (already set — do not change)
- All `href` attributes already use `/pt/` paths (keep as-is)
- `t('key')` calls produce PT strings automatically via the locale

**Apply the same changes as Plan 02 Task 1:**

1. Wrap article content (excluding deposit modal) in `<div class="ccz-lobby-grid">`.
2. Create `<aside class="ccz-analyzer-panel" data-casinocraftz-zone="analyzer-panel">`:
   - Desktop label `p` using `{t('lobby.analyzerPanel.label')}`
   - Mobile toggle `button` with `data-ccz-analyzer-toggle`, `data-label-open={t('lobby.analyzerPanel.toggle.open')}`, `data-label-close={t('lobby.analyzerPanel.toggle.close')}`
   - Drawer `div` with `id="ccz-analyzer-drawer" data-ccz-analyzer-drawer hidden` containing the 3 card divs (vertical stack, no grid wrapper)
3. Create `<div class="min-w-0">` main column with:
   - Foundation header (unchanged)
   - Games section with section label changed to `{t('lobby.chambers.label')}`
   - Mission Log section with toggle button `data-ccz-mission-log-toggle`, `data-label-open={t('lobby.missionLog.toggle.open')}`, `data-label-close={t('lobby.missionLog.toggle.close')}`, and content div `data-ccz-mission-log-content hidden` containing curriculum + tutorial sections
   - **Remove the 780px iframe block** from the tutorial section (same as EN)
   - Footer actions div (unchanged)
4. Keep `<div data-ccz-deposit-modal hidden>` outside `.ccz-lobby-grid`.

The `<script>` block (mountLobby + mountTutorial calls, lang='pt') stays UNCHANGED — lobby.ts already handles both locales since DOM queries are locale-agnostic.
  </action>
  <acceptance_criteria>
    - grep -c "ccz-lobby-grid" src/pages/pt/casinocraftz/index.astro returns 1
    - grep -c "ccz-analyzer-drawer" src/pages/pt/casinocraftz/index.astro returns 1
    - grep -c "ccz-mission-log-toggle" src/pages/pt/casinocraftz/index.astro returns 1
    - grep -c "data-casinocraftz-slots-embed\|iframe" src/pages/pt/casinocraftz/index.astro returns 0 (iframe removed)
    - grep "lobby.chambers.label" src/pages/pt/casinocraftz/index.astro confirms section label uses the i18n key
    - grep -c "data-casinocraftz-card=" src/pages/pt/casinocraftz/index.astro returns 3
    - grep "data-casinocraftz-lang" src/pages/pt/casinocraftz/index.astro outputs: pt
    - grep -c "mountLobby\|mountTutorial" src/pages/pt/casinocraftz/index.astro returns 2 (script block unchanged)
  </acceptance_criteria>
  <verify>grep -n "ccz-lobby-grid\|ccz-analyzer-drawer\|ccz-mission-log-toggle\|data-casinocraftz-slots-embed\|iframe" src/pages/pt/casinocraftz/index.astro</verify>
  <done>PT lobby structurally mirrors the EN lobby. Two-column grid, Analyzer Panel sidebar, Mission Log accordion, no iframe. PT i18n keys used throughout. data-casinocraftz-lang="pt" preserved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Build-time markup | Static Astro template — no runtime user input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-49-05 | Tampering | src/pages/pt/casinocraftz/index.astro | accept | Build-time static output; no user input crosses this boundary |
</threat_model>

<verification>
- PT lobby has `.ccz-lobby-grid` wrapper
- PT lobby has `data-ccz-analyzer-drawer` sidebar
- PT lobby has `data-ccz-mission-log-toggle` and `data-ccz-mission-log-content`
- No iframe in PT lobby source
- `data-casinocraftz-lang="pt"` preserved
</verification>

<success_criteria>
PT lobby mirrors EN layout exactly. Both routes deliver DTL-01/DTL-02 parity.
</success_criteria>

<output>
After completion, create `.planning/phases/49-analyzer-panel-and-mission-log-architecture/49-03-SUMMARY.md`
</output>
