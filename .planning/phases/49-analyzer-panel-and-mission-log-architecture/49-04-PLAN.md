---
phase: 49-analyzer-panel-and-mission-log-architecture
plan: 04
type: execute
wave: 3
depends_on:
  - 49-02
  - 49-03
files_modified: []
autonomous: false
requirements:
  - DTL-01
  - DTL-02

must_haves:
  truths:
    - "npm run build exits 0 — no TypeScript errors, no missing i18n keys, no broken Astro imports"
    - "EN lobby at /en/casinocraftz/ shows Utility Cards in a left sidebar on desktop"
    - "PT lobby at /pt/casinocraftz/ shows equivalent layout with PT labels"
    - "Mission Log toggle expands and collapses the curriculum/tutorial section in both locales"
    - "No 780px iframe visible on either route"
  artifacts:
    - path: ".planning/phases/49-analyzer-panel-and-mission-log-architecture/49-04-SUMMARY.md"
      provides: "Phase completion evidence for plan-summary parity"
  key_links:
    - from: "EN /en/casinocraftz/ route"
      to: "two-column grid with sticky left Analyzer Panel"
      via: ".ccz-lobby-grid and .ccz-analyzer-panel CSS classes"
---

<objective>
Verify the build succeeds and the new Casinocraftz lobby layout works correctly in both EN and PT locales.

Purpose: Final gate before phase is marked complete. Confirms no regressions from the structural markup changes.
Output: Build clean + human visual/functional confirmation.
</objective>

<execution_context>
@/home/gabriel/Documents/gabriel-rodrigues/.claude/get-shit-done/workflows/execute-plan.md
@/home/gabriel/Documents/gabriel-rodrigues/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/49-analyzer-panel-and-mission-log-architecture/49-CONTEXT.md
@.planning/phases/49-analyzer-panel-and-mission-log-architecture/49-02-SUMMARY.md
@.planning/phases/49-analyzer-panel-and-mission-log-architecture/49-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build verification</name>
  <files></files>
  <read_first>
    - None — this task runs the build command only
  </read_first>
  <action>
Run the production build to verify no TypeScript errors, no missing i18n key references, and no broken Astro component imports:

```bash
npm run build
```

If the build fails:
- TypeScript error in lobby.ts → fix the type error in lobby.ts (likely a missing type annotation on the new functions)
- Missing i18n key → confirm en.json and pt.json both have the key (Plan 01 task)
- Astro import error → check the restructured pages for broken relative import paths
  </action>
  <acceptance_criteria>
    - npm run build exits with code 0
    - No TypeScript errors in output
    - Build output contains en/casinocraftz/index.html and pt/casinocraftz/index.html
  </acceptance_criteria>
  <verify>npm run build 2>&1 | tail -20</verify>
  <done>Build exits 0. No TS errors, no missing keys reported.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Phase 49 layout restructure — both EN and PT Casinocraftz lobbies now have:
- Two-column desktop grid: sticky Analyzer Panel (left, 260px) + main content (right)
- Analyzer Panel contains 3 Utility Cards stacked vertically
- Mission Log toggle expands/collapses curriculum + tutorial sections accordion-style
- No 780px iframe — Slots accessed via chamber card CTA
- Mobile: Analyzer Panel collapses to a toggle (ANALYZER PANEL ▼)
- sessionStorage persists open/closed state for both toggles
  </what-built>
  <how-to-verify>
Run `npm run dev` then test the following:

**EN Desktop (http://localhost:4321/en/casinocraftz/):**
1. Page shows a two-column layout with Analyzer Panel on the left — Utility Cards (Probability Seer, Dopamine Dampener, House Edge) visible immediately
2. Scroll down — the Analyzer Panel stays visible (sticky) while the chamber grid and Mission Log scroll
3. Section label above game cards reads "SIMULATION CHAMBERS" (not "Game Lobby")
4. "MISSION LOG ▼" button is visible below the Simulation Chambers grid — click it → curriculum + tutorial dialogue expand below it, label changes to "MISSION LOG ▲"
5. Click again → Mission Log collapses, label reverts to "MISSION LOG ▼"
6. No 780px Slots iframe visible anywhere on the page
7. Reload page after opening Mission Log → Mission Log should still be open (sessionStorage)
8. Wallet HUD and deposit button still work (click "+ Deposit" → modal opens)

**PT Desktop (http://localhost:4321/pt/casinocraftz/):**
9. Same two-column layout visible with "PAINEL ANALISADOR" header on the left panel
10. Section label reads "CÂMARAS DE SIMULAÇÃO"
11. Mission Log toggle shows "REGISTRO DE MISSÃO ▼"
12. No iframe visible

**Mobile (resize to < 640px or DevTools):**
13. Layout collapses to single column — "ANALYZER PANEL ▼" toggle button visible at the top
14. Cards are hidden by default
15. Click toggle → cards expand below the button
  </how-to-verify>
  <resume-signal>Type "approved" if all checks pass, or describe any issue found</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Build output | Static HTML/JS generated at build time |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-49-06 | Information Disclosure | Build output | accept | Static site; no secrets in HTML output. Build verification only. |
</threat_model>

<verification>
- `npm run build` exits 0
- Human confirms EN and PT lobby layout changes are visually correct
- Human confirms Mission Log accordion and mobile Analyzer Panel drawer work
</verification>

<success_criteria>
Build clean. Both EN/PT lobbies show Analyzer Panel sidebar + Mission Log accordion. No iframe. Mobile drawer works. sessionStorage state persists.
</success_criteria>

<output>
After completion, create `.planning/phases/49-analyzer-panel-and-mission-log-architecture/49-04-SUMMARY.md`
</output>
