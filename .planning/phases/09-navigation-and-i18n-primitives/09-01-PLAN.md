---
phase: 09-navigation-and-i18n-primitives
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/Header.astro
  - src/i18n/en.json
  - src/i18n/pt.json
  - tests/nav-i18n-primitives.test.mjs
autonomous: true
requirements:
  - HUB-01
  - I18N-01
must_haves:
  truths:
    - Header navigation shows Projects instead of Canvas while Canvas and Slots remain discoverable through Projects-active surfaces (HUB-01).
    - Projects nav active-state is true for /projects, /canvas, and /slots under both /en and /pt locale paths (HUB-01).
    - Navigation, projects, and slots primitives exist in both EN/PT dictionaries with strict key parity and playful tone (I18N-01).
    - Regression checks fail if nav labels drift or Projects active-state path coverage regresses (HUB-01, I18N-01).
  artifacts:
    - path: src/components/Header.astro
      provides: Top navigation entry swap plus locale-aware active-state rules for projects/canvas/slots
    - path: src/i18n/en.json
      provides: EN nav/projects/slots primitive keys with playful short copy
    - path: src/i18n/pt.json
      provides: PT nav/projects/slots primitive keys mirroring EN key set
    - path: tests/nav-i18n-primitives.test.mjs
      provides: Automated assertions for label presence/parity and active-state behavior
  key_links:
    - from: src/components/Header.astro
      to: src/i18n/en.json
      via: translation key lookup for nav/projects/slots labels
      pattern: nav|projects|slots keys consumed by header
    - from: src/components/Header.astro
      to: src/i18n/pt.json
      via: translation key lookup for nav/projects/slots labels
      pattern: EN/PT parity on same key paths
    - from: tests/nav-i18n-primitives.test.mjs
      to: src/components/Header.astro
      via: active-state matcher assertions for /en/* and /pt/* paths
      pattern: /projects|/canvas|/slots active coverage
---

<objective>
Deliver Phase 9 primitives for navigation and localization only: replace Canvas top-nav with Projects, define locale-aware active-state behavior for Projects across projects/canvas/slots routes, and lock regressions with narrow automated checks.

Purpose: Implement the IA pivot foundation without expanding into Phase 10/11 page delivery work.
Output: Updated header navigation behavior, EN/PT primitive string keys with strict parity, and focused regression tests for nav labels and active-state rules.
</objective>

<execution_context>
@/home/gabriel/Documents/gabriel-rodrigues/.claude/get-shit-done/workflows/execute-plan.md
@/home/gabriel/Documents/gabriel-rodrigues/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/09-navigation-and-i18n-primitives/09-CONTEXT.md

@src/components/Header.astro
@src/i18n/en.json
@src/i18n/pt.json
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Replace Canvas nav item with Projects and enforce active-state route set (per D-01 and D-02)</name>
  <files>src/components/Header.astro</files>
  <behavior>
    - Visiting /en/projects/, /en/canvas/, and /en/slots/ marks Projects nav item active.
    - Visiting /pt/projects/, /pt/canvas/, and /pt/slots/ marks Projects nav item active.
    - Header no longer renders Canvas as a top-level nav entry.
  </behavior>
  <action>Update header navigation to remove Canvas top-menu entry and render Projects as the canonical discovery entry (per D-01). Implement centralized active-state matching so Projects is active for /projects, /canvas, and /slots across both locale trees /en/* and /pt/* (per D-02). Keep changes minimal to nav behavior only; do not introduce Projects or Slots page content changes in this phase.</action>
  <verify>
    <automated>node --test tests/nav-i18n-primitives.test.mjs --test-name-pattern "active-state"</automated>
  </verify>
  <done>Header nav shows Projects instead of Canvas, and Projects active-state is correctly applied for /projects, /canvas, and /slots in EN/PT paths.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add EN/PT primitive i18n keys for nav/projects/slots with strict parity (per D-03)</name>
  <files>src/i18n/en.json, src/i18n/pt.json</files>
  <behavior>
    - nav/projects/slots key groups exist in EN and PT with identical key paths.
    - Labels/descriptors remain concise and playful in both locales.
    - Missing key in either locale fails parity assertions.
  </behavior>
  <action>Add primitive key groups under nav.*, projects.*, and slots.* in both dictionaries with strict same-commit parity (per D-03). Keep tone playful/experimental and concise, while preserving predictable grouped keys. Do not add unrelated copy outside navigation/projects/slots primitives.</action>
  <verify>
    <automated>node --test tests/nav-i18n-primitives.test.mjs --test-name-pattern "i18n parity|nav labels"</automated>
  </verify>
  <done>Both locale files expose matching nav/projects/slots primitive keys, with no key drift between EN and PT.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add focused regression safeguards for nav labels and active-state only</name>
  <files>tests/nav-i18n-primitives.test.mjs</files>
  <behavior>
    - Tests assert Projects label is present and Canvas top-level label is absent in header nav contract.
    - Tests assert active-state coverage for /projects, /canvas, /slots in both /en and /pt route shapes.
    - Tests assert EN/PT key-path parity for nav/projects/slots primitives only.
  </behavior>
  <action>Create or update a dedicated test module with narrow coverage: nav label contract and active-state route behavior only. Keep assertions deterministic and independent from unrelated UI flows or full E2E discovery journeys. Avoid adding broad compatibility or route-switching tests that belong to later phases.</action>
  <verify>
    <automated>node --test tests/nav-i18n-primitives.test.mjs</automated>
  </verify>
  <done>Regression suite protects against label and active-state regressions for Phase 9 primitives without covering out-of-scope functionality.</done>
</task>

</tasks>

<verification>
- node --test tests/nav-i18n-primitives.test.mjs
- npm run lint
</verification>

<success_criteria>

- Projects is the top-level header entry in place of Canvas.
- Projects active-state covers /projects, /canvas, and /slots for both EN and PT paths.
- nav/projects/slots i18n primitives exist in en.json and pt.json with strict key parity.
- Automated tests fail on nav-label or active-state regressions and pass after implementation.
  </success_criteria>

<output>
After completion, create .planning/phases/09-navigation-and-i18n-primitives/09-01-SUMMARY.md
</output>
