---
phase: 19-sprite-atlas-integration-and-ui-motion
plan: 01
type: execute
wave: 1
depends_on:
  - 18-01
files_modified:
  - src/scripts/slots/animation/atlas-registry.ts
  - src/scripts/slots/animation/symbol-frame-map.ts
  - src/scripts/slots/animation/idle-motion.ts
  - src/scripts/slots/animation/runtime.ts
  - src/scripts/slots/main.ts
  - tests/slots-sprite-atlas-contract.test.mjs
  - e2e/compatibility.spec.ts
  - .planning/phases/19-sprite-atlas-integration-and-ui-motion/19-01-SUMMARY.md
autonomous: true
requirements:
  - SPRITE-10
  - ANIM-12
must_haves:
  truths:
    - Symbol presentation uses deterministic SlotSymbol-to-frame mapping that does not alter payout, balance, or authoritative round state.
    - Idle/UI transitions are subtle and non-blocking across EN/PT routes.
    - Runtime behavior remains SPA-safe with no duplicate listener side effects after navigation.
  artifacts:
    - path: src/scripts/slots/animation/atlas-registry.ts
      provides: Atlas schema/frame availability validation and deterministic readiness status.
    - path: src/scripts/slots/animation/symbol-frame-map.ts
      provides: Stable domain-symbol to frame-key mapping boundary.
    - path: tests/slots-sprite-atlas-contract.test.mjs
      provides: Contract coverage for mapping determinism and presentation-only boundaries.
  key_links:
    - from: src/scripts/slots/engine/types.ts
      to: src/scripts/slots/animation/symbol-frame-map.ts
      via: SlotSymbol mapping adapter with no engine type mutation
      pattern: SlotSymbol|symbol.*frame
    - from: src/scripts/slots/animation/runtime.ts
      to: src/scripts/slots/animation/atlas-registry.ts
      via: atlas readiness initialization and data-slots-anim-* observability updates
      pattern: atlas|data-slots-anim-
    - from: e2e/compatibility.spec.ts
      to: data-slots-anim-* hooks
      via: stable EN/PT assertions without frame-perfect timing checks
      pattern: data-slots-anim-
validation_commands:
  - npm run lint
  - node --test tests/slots-core-determinism-contract.test.mjs tests/slots-interaction-guards-contract.test.mjs tests/slots-i18n-parity-contract.test.mjs tests/slots-animation-event-sequencing-contract.test.mjs tests/slots-sprite-atlas-contract.test.mjs
  - npx playwright test e2e/compatibility.spec.ts --project=chromium
  - npm run build
  - npm run lint && node --test tests/slots-core-determinism-contract.test.mjs tests/slots-interaction-guards-contract.test.mjs tests/slots-i18n-parity-contract.test.mjs tests/slots-animation-event-sequencing-contract.test.mjs tests/slots-sprite-atlas-contract.test.mjs && npx playwright test e2e/compatibility.spec.ts --project=chromium && npm run build
---

<objective>
Implement Phase 19 sprite atlas integration and subtle UI/idle motion polish on top of Phase 18 runtime foundations while preserving deterministic authority boundaries.

Purpose: satisfy SPRITE-10 and ANIM-12 with presentation-only enhancements.
Output: atlas contracts, symbol mapping layer, idle motion wiring, deterministic tests, and compatibility assertions.
</objective>

<execution_context>
@/home/gabriel/Documents/gabriel-rodrigues/.claude/get-shit-done/workflows/execute-plan.md
@/home/gabriel/Documents/gabriel-rodrigues/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/19-sprite-atlas-integration-and-ui-motion/19-CONTEXT.md
@.planning/phases/19-sprite-atlas-integration-and-ui-motion/19-RESEARCH.md
@.planning/phases/19-sprite-atlas-integration-and-ui-motion/19-VALIDATION.md
@.planning/phases/18-slots-animation-runtime-foundation/18-01-SUMMARY.md
@.planning/phases/18-slots-animation-runtime-foundation/18-01-VERIFICATION.md
@src/scripts/slots/engine/types.ts
@src/scripts/slots/animation/events.ts
@src/scripts/slots/animation/runtime.ts
@src/scripts/slots/main.ts
@tests/slots-animation-event-sequencing-contract.test.mjs
@tests/slots-i18n-parity-contract.test.mjs
@e2e/compatibility.spec.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add deterministic atlas registry and symbol-frame mapping boundary</name>
  <read_first>
    - .planning/phases/19-sprite-atlas-integration-and-ui-motion/19-CONTEXT.md
    - .planning/phases/19-sprite-atlas-integration-and-ui-motion/19-RESEARCH.md
    - src/scripts/slots/engine/types.ts
    - src/scripts/slots/animation/runtime.ts
  </read_first>
  <files>src/scripts/slots/animation/atlas-registry.ts, src/scripts/slots/animation/symbol-frame-map.ts</files>
  <action>
    Implement SPRITE-10 contract foundations:
    - Add atlas registry utilities that validate required frame keys and expose deterministic readiness state.
    - Add explicit SlotSymbol-to-frame mapping adapter (`A`..`E` -> atlas frame keys) in presentation layer only.
    - Ensure missing frame handling is deterministic and non-authoritative (no engine/economy writes).
  </action>
  <acceptance_criteria>
    - Atlas readiness can be evaluated deterministically at runtime.
    - Domain symbols remain independent from raw atlas key naming.
    - No gameplay authority module imports are introduced into atlas/mapping modules.
  </acceptance_criteria>
  <verify>
    <automated>npm run lint && node --test tests/slots-core-determinism-contract.test.mjs tests/slots-interaction-guards-contract.test.mjs</automated>
  </verify>
  <done>Atlas and mapping boundaries exist and preserve deterministic authority separation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire atlas readiness and subtle idle/UI motion into runtime lifecycle</name>
  <read_first>
    - src/scripts/slots/animation/runtime.ts
    - src/scripts/slots/main.ts
    - .planning/phases/19-sprite-atlas-integration-and-ui-motion/19-RESEARCH.md
  </read_first>
  <files>src/scripts/slots/animation/idle-motion.ts, src/scripts/slots/animation/runtime.ts, src/scripts/slots/main.ts</files>
  <action>
    Implement ANIM-12 runtime wiring:
    - Add idle/UI motion model that derives from existing visual event/state flow and remains non-blocking.
    - Integrate atlas readiness and motion snapshots into runtime mount/dispose flow.
    - Extend presentation-only observability hooks (`data-slots-anim-*`) for atlas and idle state visibility.
    - Preserve SPA safety and idempotent cleanup on route swaps.
  </action>
  <acceptance_criteria>
    - Idle/UI transitions are subtle, deterministic-state-driven, and do not block interactions.
    - Runtime remains idempotent across Astro SPA navigation.
    - EN/PT behavior remains functionally equivalent while visual hooks are active.
  </acceptance_criteria>
  <verify>
    <automated>node --test tests/slots-core-determinism-contract.test.mjs tests/slots-i18n-parity-contract.test.mjs && npm run build</automated>
  </verify>
  <done>Runtime atlas/motion integration is presentation-only and SPA-safe.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add atlas/motion contracts and compatibility assertions</name>
  <read_first>
    - tests/slots-animation-event-sequencing-contract.test.mjs
    - tests/slots-i18n-parity-contract.test.mjs
    - e2e/compatibility.spec.ts
    - .planning/phases/19-sprite-atlas-integration-and-ui-motion/19-VALIDATION.md
  </read_first>
  <files>tests/slots-sprite-atlas-contract.test.mjs, e2e/compatibility.spec.ts</files>
  <action>
    Close Phase 19 validation coverage:
    - Add `tests/slots-sprite-atlas-contract.test.mjs` for deterministic symbol-frame mapping, atlas readiness, and no-authority-mutation constraints.
    - Extend compatibility E2E with stable assertions for atlas/idle observability hooks in EN/PT runtime flows.
    - Keep checks deterministic and avoid frame-perfect timing assertions.
  </action>
  <acceptance_criteria>
    - Contract tests fail on mapping drift or authority-coupling regressions.
    - E2E validates atlas/motion observability through stable hooks.
    - Existing determinism, guards, and i18n parity tests remain green.
  </acceptance_criteria>
  <verify>
    <automated>node --test tests/slots-core-determinism-contract.test.mjs tests/slots-interaction-guards-contract.test.mjs tests/slots-i18n-parity-contract.test.mjs tests/slots-animation-event-sequencing-contract.test.mjs tests/slots-sprite-atlas-contract.test.mjs</automated>
  </verify>
  <done>Phase 19 has deterministic atlas/motion contract and compatibility coverage.</done>
</task>

</tasks>

<validation_commands>

1. npm run lint
2. node --test tests/slots-core-determinism-contract.test.mjs tests/slots-interaction-guards-contract.test.mjs tests/slots-i18n-parity-contract.test.mjs tests/slots-animation-event-sequencing-contract.test.mjs tests/slots-sprite-atlas-contract.test.mjs
3. npx playwright test e2e/compatibility.spec.ts --project=chromium
4. npm run build
5. npm run lint && node --test tests/slots-core-determinism-contract.test.mjs tests/slots-interaction-guards-contract.test.mjs tests/slots-i18n-parity-contract.test.mjs tests/slots-animation-event-sequencing-contract.test.mjs tests/slots-sprite-atlas-contract.test.mjs && npx playwright test e2e/compatibility.spec.ts --project=chromium && npm run build

</validation_commands>

<verification>
Phase 19 is complete when SPRITE-10 and ANIM-12 are satisfied via presentation-only atlas/motion wiring and all validation commands pass without gameplay authority regressions.
</verification>

<success_criteria>

- Symbol rendering pipeline is atlas-backed through deterministic symbol-frame mapping.
- Idle/UI transitions are subtle, non-blocking, and EN/PT consistent.
- Runtime remains SPA-safe and listener-clean across route transitions.
- Determinism, guard, i18n parity, and compatibility validation remain green.
  </success_criteria>

<output>
After completion, create .planning/phases/19-sprite-atlas-integration-and-ui-motion/19-01-SUMMARY.md
</output>
