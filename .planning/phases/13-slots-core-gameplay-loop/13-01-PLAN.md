---
phase: 13-slots-core-gameplay-loop
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/scripts/slots/main.ts
  - src/scripts/slots/engine/types.ts
  - src/scripts/slots/engine/rng.ts
  - src/scripts/slots/engine/state-machine.ts
  - src/scripts/slots/engine/spin-resolver.ts
  - src/scripts/slots/engine/paylines.ts
  - src/scripts/slots/engine/payout.ts
  - src/scripts/slots/engine/round.ts
  - src/scripts/slots/controller.ts
  - src/pages/en/slots/index.astro
  - src/pages/pt/slots/index.astro
  - tests/slots-core-determinism-contract.test.mjs
  - tests/slots-payline-evaluation-contract.test.mjs
  - .planning/phases/13-slots-core-gameplay-loop/13-VALIDATION.md
autonomous: true
requirements:
  - SLOT-10
  - SLOT-11
must_haves:
  truths:
    - User can trigger exactly one round at a time with an explicit lifecycle of idle -> spinning -> result, and duplicate spin triggers while spinning are blocked (SLOT-10).
    - For identical seed and inputs, reel stops and round outcome are identical across runs in Node contract tests without browser timing dependence (SLOT-10).
    - Payline evaluation and payout are computed from resolved reel symbols and produce correct win and loss results with explicit metadata (SLOT-11).
    - Slots gameplay loop integrates into existing EN/PT canonical pages without introducing alias routes or breaking SPA lifecycle safety (SLOT-10, SLOT-11).
  artifacts:
    - path: src/scripts/slots/engine/types.ts
      provides: serializable contracts for round state, resolved reels, paylines, and payout result payloads
    - path: src/scripts/slots/engine/state-machine.ts
      provides: deterministic transition guards for idle/spinning/result and single-active-round invariant
    - path: src/scripts/slots/engine/round.ts
      provides: pure deterministic orchestration from seed/config to resolved round result
    - path: src/scripts/slots/controller.ts
      provides: UI adapter that dispatches spin actions and renders state/result from engine snapshots
    - path: tests/slots-core-determinism-contract.test.mjs
      provides: SLOT-10 determinism and transition contract coverage
    - path: tests/slots-payline-evaluation-contract.test.mjs
      provides: SLOT-11 payline and payout contract coverage for win/loss matrices
  key_links:
    - from: src/scripts/slots/main.ts
      to: src/scripts/slots/controller.ts
      via: initSlotsShell mounts controller on #slots-shell-root under astro:page-load lifecycle
      pattern: guarded initialization with AbortController cleanup on astro:before-swap
    - from: src/scripts/slots/controller.ts
      to: src/scripts/slots/engine/round.ts
      via: controller triggers deterministic round resolution using injected seed and spin index
      pattern: no UI/DOM text dependence in result calculation path
    - from: src/scripts/slots/engine/round.ts
      to: src/scripts/slots/engine/paylines.ts and src/scripts/slots/engine/payout.ts
      via: resolved matrix -> payline matches -> payout metadata
      pattern: loss returns zero payout with explicit outcome field
    - from: tests/slots-core-determinism-contract.test.mjs
      to: src/scripts/slots/engine/state-machine.ts and src/scripts/slots/engine/round.ts
      via: repeatable seed-based assertions and invalid-transition checks
      pattern: identical input yields identical output
    - from: tests/slots-payline-evaluation-contract.test.mjs
      to: src/scripts/slots/engine/paylines.ts and src/scripts/slots/engine/payout.ts
      via: fixture matrix assertions for win/loss payout correctness
      pattern: payout derives strictly from resolved symbols
---

<objective>
Implement the first fully deterministic Slots gameplay round loop for Phase 13, limited to SLOT-10 and SLOT-11.

Purpose: Establish a reliable core gameplay foundation before economy and broader UX expansion.
Output: Deterministic round engine modules, shell integration wiring, and contract tests that lock lifecycle and payout correctness.

In scope:

- SLOT-10 deterministic lifecycle/state machine and reel resolution contract.
- SLOT-11 payline evaluation and payout calculation for win/loss correctness.
- Integration into existing canonical routes /en/slots/ and /pt/slots/ via existing SPA-safe shell bootstrap.

Out of scope:

- SLOT-12 economy, credits, bets, and insufficient-credit flows (deferred to Phase 14).
- I18N-10 gameplay copy expansion and UX-10 broad interaction matrix (deferred to Phase 14).
- COMP-10 and QA-10 broad compatibility/e2e matrices (deferred to Phase 15).
- Alias routes such as /projects/slots/\* and any real-money behavior.
  </objective>

<execution_context>
@/home/gabriel/Documents/gabriel-rodrigues/.claude/get-shit-done/workflows/execute-plan.md
@/home/gabriel/Documents/gabriel-rodrigues/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/13-slots-core-gameplay-loop/13-CONTEXT.md
@.planning/phases/13-slots-core-gameplay-loop/13-RESEARCH.md
@.planning/phases/11-slots-shell-foundation/11-01-SUMMARY.md
@.planning/phases/12-compatibility-and-qa-hardening/12-01-SUMMARY.md

@src/scripts/slots/main.ts
@src/pages/en/slots/index.astro
@src/pages/pt/slots/index.astro
@tests/slots-shell-foundation.test.mjs
@tests/compatibility-contract.test.mjs
</context>

<interfaces>
From src/scripts/slots/main.ts:
- export function initSlotsShell(): void

Required new contracts for this plan:

- Round lifecycle enum/union: idle | spinning | result
- Serializable round result payload with: seed, spinIndex, stops, matrix, winLines, totalPayoutUnits, outcome
- Controller surface that mounts against #slots-shell-root and unmounts through AbortController-safe lifecycle
  </interfaces>

<files_to_touch>
Required existing files:

- src/scripts/slots/main.ts
- src/pages/en/slots/index.astro
- src/pages/pt/slots/index.astro

Likely new files:

- src/scripts/slots/controller.ts
- src/scripts/slots/engine/types.ts
- src/scripts/slots/engine/rng.ts
- src/scripts/slots/engine/state-machine.ts
- src/scripts/slots/engine/spin-resolver.ts
- src/scripts/slots/engine/paylines.ts
- src/scripts/slots/engine/payout.ts
- src/scripts/slots/engine/round.ts
- tests/slots-core-determinism-contract.test.mjs
- tests/slots-payline-evaluation-contract.test.mjs
- .planning/phases/13-slots-core-gameplay-loop/13-VALIDATION.md
  </files_to_touch>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create SLOT-10/11 contract tests first, then build deterministic engine pipeline (implements SLOT-10 and SLOT-11 core logic)</name>
  <files>tests/slots-core-determinism-contract.test.mjs, tests/slots-payline-evaluation-contract.test.mjs, src/scripts/slots/engine/types.ts, src/scripts/slots/engine/rng.ts, src/scripts/slots/engine/state-machine.ts, src/scripts/slots/engine/spin-resolver.ts, src/scripts/slots/engine/paylines.ts, src/scripts/slots/engine/payout.ts, src/scripts/slots/engine/round.ts</files>
  <behavior>
    - SLOT-10: state transitions are explicit and invalid transitions are rejected/ignored deterministically.
    - SLOT-10: same seed + same config + same spinIndex yields identical stops, matrix, and outcome.
    - SLOT-11: loss path returns outcome=loss with totalPayoutUnits=0 and explicit metadata.
    - SLOT-11: win path returns outcome=win with deterministic line metadata and payout units from paytable.
  </behavior>
  <action>Author failing deterministic contract tests first for lifecycle transitions and payline win/loss fixtures, then implement pure deterministic modules in engine/ to make the tests pass. Keep random source injected and independent from browser timing per locked determinism decisions in 13-CONTEXT. Keep baseline payline scope intentionally narrow (single center row) to maximize correctness and readability. Do not introduce credits, bets, economy arithmetic, or locale-driven logic in this task.</action>
  <verify>
    <automated>node --test tests/slots-core-determinism-contract.test.mjs tests/slots-payline-evaluation-contract.test.mjs</automated>
  </verify>
  <done>Engine modules exist and can deterministically resolve and evaluate one round using pure functions, with no dependency on DOM or timing APIs.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Integrate engine into slots shell controller and keep canonical EN/PT + SPA lifecycle constraints (depends on Task 1)</name>
  <files>src/scripts/slots/controller.ts, src/scripts/slots/main.ts, src/pages/en/slots/index.astro, src/pages/pt/slots/index.astro</files>
  <behavior>
    - Spin action is blocked while state is spinning (single active round invariant).
    - Result render path exposes deterministic result metadata for verification without coupling evaluation to UI text.
    - initSlotsShell remains astro:page-load driven and cleanup-safe via AbortController and astro:before-swap semantics.
    - Canonical routes remain /en/slots/ and /pt/slots/ only.
  </behavior>
  <action>Create a thin DOM controller that mounts under #slots-shell-root, dispatches spin actions, and renders state from engine snapshots. Keep shell routing untouched and preserve SPA lifecycle pattern already established in main.ts. Add only minimal EN/PT shell markup hooks required for state/result display and deterministic assertions. Explicitly avoid economy controls, credits/bet UI, and alias-route additions.</action>
  <verify>
    <automated>node --test tests/slots-shell-foundation.test.mjs tests/compatibility-contract.test.mjs</automated>
  </verify>
  <done>Core loop is wired into both locale shell pages without breaking existing lifecycle and canonical-route contracts.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Finalize phase validation artifact and run phase gate commands (depends on Tasks 1-2)</name>
  <files>.planning/phases/13-slots-core-gameplay-loop/13-VALIDATION.md</files>
  <behavior>
    - SLOT-10 contracts fail on non-determinism or invalid transition leakage.
    - SLOT-11 contracts fail on incorrect win/loss payoff evaluation.
    - Existing shell/compatibility contracts continue passing after gameplay integration.
  </behavior>
  <action>Create `13-VALIDATION.md` mapping SLOT-10 and SLOT-11 to deterministic contract and integration gates, then run phase validation in fail-fast order (new contracts first, then existing compatibility guards, then build). Do not add broad Playwright matrices in this phase.</action>
  <verify>
    <automated>node --test tests/slots-core-determinism-contract.test.mjs && node --test tests/slots-payline-evaluation-contract.test.mjs && node --test tests/slots-shell-foundation.test.mjs tests/compatibility-contract.test.mjs && npm run build</automated>
  </verify>
  <done>SLOT-10 and SLOT-11 are regression-locked by dedicated deterministic contracts, and existing route/lifecycle guardrails remain green.</done>
</task>

</tasks>

<validation_commands>

- node --test tests/slots-core-determinism-contract.test.mjs
- node --test tests/slots-payline-evaluation-contract.test.mjs
- node --test tests/slots-shell-foundation.test.mjs tests/compatibility-contract.test.mjs
- npm run build
  </validation_commands>

<rollback>
- If deterministic integration causes instability, rollback by removing new engine/controller imports from src/scripts/slots/main.ts and restoring shell-only status behavior.
- If EN/PT shell pages diverge or route constraints break, revert gameplay-specific markup additions in both locale pages together to keep parity.
- If payout logic fails contracts, temporarily pin to loss-only fallback path (outcome=loss, totalPayoutUnits=0) while preserving deterministic stop generation, then re-introduce win evaluation behind green contract tests.
- Keep rollback limited to Phase 13 files; do not alter prior phase compatibility tests except to restore passing baseline.
</rollback>

<verification>
- Execute commands in <validation_commands> order.
- Confirm deterministic output stability by re-running the SLOT-10 command twice and comparing pass/fail consistency.
</verification>

<success_criteria>

- SLOT-10 and SLOT-11 are fully covered by deterministic Node contracts with explicit lifecycle and payout assertions.
- Round engine remains pure and serializable, and UI integration is adapter-only.
- Canonical slots routes and SPA lifecycle constraints remain intact on EN/PT pages.
- Economy/credits/bets and broader E2E matrix work are not introduced and remain deferred to Phases 14/15.
  </success_criteria>

<output>
After completion, create .planning/phases/13-slots-core-gameplay-loop/13-01-SUMMARY.md
</output>
