---
phase: 14-economy-ux-and-i18n-parity
plan: 01
type: execute
wave: 1
depends_on:
  - 13-01
files_modified:
  - src/scripts/slots/controller.ts
  - src/scripts/slots/main.ts
  - src/pages/en/slots/index.astro
  - src/pages/pt/slots/index.astro
  - src/i18n/en.json
  - src/i18n/pt.json
  - tests/slots-economy-contract.test.mjs
  - tests/slots-interaction-guards-contract.test.mjs
  - tests/slots-i18n-parity-contract.test.mjs
autonomous: true
requirements:
  - SLOT-12
  - I18N-10
  - UX-10
must_haves:
  truths:
    - User can place a valid bet and run rounds while balance remains non-negative.
    - Spin is blocked during spinning and when credits are insufficient, with explicit feedback.
    - All new gameplay economy/UX copy renders from EN/PT translation keys with parity.
  artifacts:
    - path: src/scripts/slots/controller.ts
      provides: Session economy loop with guard-first round flow and deterministic payout settlement.
      contains:
        - pre-spin debit balance -= bet
        - post-result credit balance += payoutUnits * bet
        - insufficient-credit guard pathway
    - path: src/pages/en/slots/index.astro
      provides: EN Slots economy/UX controls and translated status rendering hooks.
    - path: src/pages/pt/slots/index.astro
      provides: PT Slots economy/UX controls and translated status rendering hooks.
    - path: src/i18n/en.json
      provides: EN gameplay keys for economy/UX states.
    - path: src/i18n/pt.json
      provides: PT gameplay keys matching EN keyset.
    - path: tests/slots-economy-contract.test.mjs
      provides: SLOT-12 arithmetic and non-negative invariant contract coverage.
    - path: tests/slots-interaction-guards-contract.test.mjs
      provides: UX-10 invalid-action and insufficient-credit guard contracts.
    - path: tests/slots-i18n-parity-contract.test.mjs
      provides: I18N-10 key parity and non-empty localized values checks.
  key_links:
    - from: src/scripts/slots/controller.ts
      to: src/scripts/slots/engine/round.ts
      via: resolveRound() outcome consumed as payout units source of truth
      pattern: resolveRound\(
    - from: src/scripts/slots/main.ts
      to: src/scripts/slots/controller.ts
      via: initSlotsShell() under astro:page-load with AbortController cleanup
      pattern: astro:page-load|AbortController|initSlotsShell
    - from: src/pages/en/slots/index.astro
      to: src/i18n/en.json
      via: useTranslations() keys for gameplay economy/UX labels
      pattern: slots\.gameplay\.
    - from: src/pages/pt/slots/index.astro
      to: src/i18n/pt.json
      via: useTranslations() keys for gameplay economy/UX labels
      pattern: slots\.gameplay\.
---

<objective>
Implement Phase 14 economy loop, interaction guardrails, and EN/PT gameplay copy parity as an executable vertical slice.

Purpose: Deliver SLOT-12, I18N-10, and UX-10 without route churn or broad compatibility hardening.
Output: Session-safe credits/bet loop, guardrailed controls, bilingual gameplay copy parity tests, and deterministic contracts.
</objective>

<scope>
In scope:
- SLOT-12 economy loop: in-memory session balance/bet flow with deterministic settlement arithmetic.
- UX-10 interaction guardrails: clear invalid-action handling for spinning and insufficient-credit states.
- I18N-10 parity: all new gameplay economy/UX copy in EN/PT with parity contract checks.
- Preservation of canonical routes and SPA lifecycle pattern.

Out of scope:

- COMP-10 and QA-10 broad compatibility/regression hardening (deferred to Phase 15).
- Alias routes (for example /projects/slots/\*), route reshaping, or navigation behavior changes.
- Persistence, wallet/accounts, real-money flows, or gambling compliance features.
- Broad Playwright matrix expansion beyond focused contracts needed for this phase.
  </scope>

<execution_context>
@/home/gabriel/Documents/gabriel-rodrigues/.claude/get-shit-done/workflows/execute-plan.md
@/home/gabriel/Documents/gabriel-rodrigues/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-economy-ux-and-i18n-parity/14-CONTEXT.md
@.planning/phases/14-economy-ux-and-i18n-parity/14-RESEARCH.md
@.planning/phases/13-slots-core-gameplay-loop/13-01-SUMMARY.md
@src/scripts/slots/controller.ts
@src/scripts/slots/main.ts
@src/scripts/slots/engine/round.ts
@src/pages/en/slots/index.astro
@src/pages/pt/slots/index.astro
@src/i18n/en.json
@src/i18n/pt.json

<interfaces>
From src/scripts/slots/main.ts:
- initSlotsShell() must remain mounted via astro:page-load lifecycle and cleaned on astro:before-swap.

From src/scripts/slots/controller.ts:

- initSlotsShell(root, options) is the integration boundary for UI events, rendering, and state guards.
- Deterministic round resolution must continue to call engine modules rather than duplicating payout logic.

From src/scripts/slots/engine/round.ts:

- resolveRound(...) returns deterministic round output including totalPayoutUnits used for economy settlement.

From src/pages/en/slots/index.astro and src/pages/pt/slots/index.astro:

- Slots page shell remains canonical per locale route, with gameplay UI mounted in-page.

From src/i18n/en.json and src/i18n/pt.json:

- Locale dictionaries are the source for all user-facing strings introduced in this phase.
  </interfaces>
  </context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add failing contract tests for economy arithmetic, interaction guards, and EN/PT key parity</name>
  <files>tests/slots-economy-contract.test.mjs, tests/slots-interaction-guards-contract.test.mjs, tests/slots-i18n-parity-contract.test.mjs</files>
  <behavior>
    - Test 1: For a valid spin, balance is debited once before spin and then credited by payoutUnits * bet on result (SLOT-12).
    - Test 2: Balance never goes negative through allowed UI/controller actions, including insufficient-credit attempts (SLOT-12, UX-10).
    - Test 3: Spin requests are rejected while spinning and when balance < bet, with explicit insufficient/invalid feedback markers (UX-10).
    - Test 4: All new slots.gameplay economy/UX keys exist and are non-empty in both EN/PT locale files (I18N-10).
  </behavior>
  <action>Create three focused node:test contract files that fail against current behavior first, using deterministic seeds and machine-readable dataset/status assertions per D-01 economy and guard constraints in 14-CONTEXT.</action>
  <verify>
    <automated>node --test tests/slots-economy-contract.test.mjs tests/slots-interaction-guards-contract.test.mjs tests/slots-i18n-parity-contract.test.mjs</automated>
  </verify>
  <done>Contract suites exist and express SLOT-12, UX-10, and I18N-10 invariants clearly enough to drive implementation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement session economy loop and interaction guardrails in controller + slots pages</name>
  <files>src/scripts/slots/controller.ts, src/scripts/slots/main.ts, src/pages/en/slots/index.astro, src/pages/pt/slots/index.astro</files>
  <behavior>
    - Test 1: Guard-first flow blocks invalid spin attempts without mutating balance or round index.
    - Test 2: Valid round flow performs pre-spin debit and post-result payout settlement using totalPayoutUnits from resolveRound().
    - Test 3: UI exposes deterministic state markers for idle/spinning/result/insufficient and spin availability.
  </behavior>
  <action>
    Implement SLOT-12 + UX-10 only:
    - Add in-memory session economy snapshot (balance, bet, bet constraints) inside controller boundary.
    - Enforce arithmetic contract exactly: pre-spin balance -= bet, post-result balance += totalPayoutUnits * bet.
    - Add insufficient-credit and invalid-action feedback path while preserving deterministic engine source-of-truth and no duplicated payout math.
    - Update EN/PT slots page gameplay wiring to surface balance/bet/status data attributes and controls needed by contracts.
    - Preserve canonical locale routes and keep SPA lifecycle mount/cleanup pattern (`astro:page-load`, `AbortController`, `astro:before-swap`) unchanged.
  </action>
  <verify>
    <automated>node --test tests/slots-economy-contract.test.mjs tests/slots-interaction-guards-contract.test.mjs tests/slots-core-determinism-contract.test.mjs tests/slots-payline-evaluation-contract.test.mjs tests/slots-shell-foundation.test.mjs</automated>
  </verify>
  <done>Economy and guardrail contracts pass with deterministic engine tests still green and no canonical route/lifecycle regressions introduced.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add EN/PT gameplay copy parity wiring and phase-level validation gate</name>
  <files>src/i18n/en.json, src/i18n/pt.json, src/pages/en/slots/index.astro, src/pages/pt/slots/index.astro, src/scripts/slots/controller.ts</files>
  <behavior>
    - Test 1: Newly introduced gameplay labels/statuses render from translation keys, not hardcoded literals.
    - Test 2: EN/PT locale dictionaries contain mirrored non-empty keyset for all Phase 14 gameplay additions.
    - Test 3: Slots runtime presents localized copy for idle/spinning/result/insufficient feedback states.
  </behavior>
  <action>
    Complete I18N-10 integration:
    - Add and align slots.gameplay key families in both locale JSON files.
    - Replace any new/edited hardcoded economy/UX strings with translation lookups.
    - Ensure controller/page status messaging uses translation-driven text while keeping existing canonical route behavior.
    - Keep COMP-10/QA-10 broad hardening deferred; only run focused validation commands required to prove Phase 14 scope.
  </action>
  <verify>
    <automated>node --test tests/slots-i18n-parity-contract.test.mjs tests/slots-economy-contract.test.mjs tests/slots-interaction-guards-contract.test.mjs tests/slots-core-determinism-contract.test.mjs tests/slots-payline-evaluation-contract.test.mjs tests/slots-shell-foundation.test.mjs && npm run build</automated>
  </verify>
  <done>Phase 14 scoped requirements (SLOT-12, I18N-10, UX-10) are validated with deterministic contracts and bilingual parity checks.</done>
</task>

</tasks>

<required_likely_files>
Required:

- src/scripts/slots/controller.ts
- src/pages/en/slots/index.astro
- src/pages/pt/slots/index.astro
- src/i18n/en.json
- src/i18n/pt.json
- tests/slots-economy-contract.test.mjs
- tests/slots-interaction-guards-contract.test.mjs
- tests/slots-i18n-parity-contract.test.mjs

Likely (if lifecycle/data-attribute wiring needs adjustment):

- src/scripts/slots/main.ts
- tests/slots-shell-foundation.test.mjs
  </required_likely_files>

<validation_commands>

1. node --test tests/slots-economy-contract.test.mjs tests/slots-interaction-guards-contract.test.mjs tests/slots-i18n-parity-contract.test.mjs
2. node --test tests/slots-economy-contract.test.mjs tests/slots-interaction-guards-contract.test.mjs tests/slots-core-determinism-contract.test.mjs tests/slots-payline-evaluation-contract.test.mjs tests/slots-shell-foundation.test.mjs
3. node --test tests/slots-i18n-parity-contract.test.mjs tests/slots-economy-contract.test.mjs tests/slots-interaction-guards-contract.test.mjs tests/slots-core-determinism-contract.test.mjs tests/slots-payline-evaluation-contract.test.mjs tests/slots-shell-foundation.test.mjs
4. npm run build
   </validation_commands>

<verification>
- All Task 1-3 automated checks pass without editing canonical route map.
- No new alias routes are introduced.
- Slots shell remains SPA-safe across navigation with no duplicate listener behavior.
- Scope remains strictly SLOT-12, I18N-10, UX-10.
</verification>

<rollback_notes>

- Revert Phase 14 economy/UX additions by removing controller economy guards and page-level balance/bet/status controls, restoring Phase 13 deterministic round rendering behavior.
- Remove newly added phase-specific test files if rollback returns to pre-Phase-14 scope.
- Keep engine modules (`src/scripts/slots/engine/*`) and canonical routes untouched during rollback.
- If regression appears in SPA lifecycle, restore `src/scripts/slots/main.ts` to last known good Phase 13 wiring using `astro:page-load` + AbortController cleanup.
  </rollback_notes>

<success_criteria>

- SLOT-12: Session credits/bet loop updates balance deterministically and never below zero via supported actions.
- UX-10: Invalid actions are blocked with explicit feedback for spinning and insufficient-credit states.
- I18N-10: EN/PT gameplay keys are complete, non-empty, and used by runtime for new/edited Phase 14 economy/UX copy.
- COMP-10 and QA-10 broad hardening are not implemented in this plan and remain queued for Phase 15.
  </success_criteria>

<output>
After completion, create `.planning/phases/14-economy-ux-and-i18n-parity/14-01-SUMMARY.md`.
</output>
