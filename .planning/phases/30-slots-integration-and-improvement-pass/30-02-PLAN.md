---
phase: 30-slots-integration-and-improvement-pass
plan: 02
type: execute
wave: 2
depends_on:
  - 30-01
files_modified:
  - e2e/compatibility.spec.ts
  - tests/compatibility-contract.test.mjs
  - public/sw.js
  - .planning/phases/30-slots-integration-and-improvement-pass/30-02-SUMMARY.md
autonomous: true
requirements:
  - SLOT-40
  - SLOT-41
gap_closure: true
must_haves:
  truths:
    - Embedded Slots in `/en/casinocraftz/` and `/pt/casinocraftz/` accepts spin interaction and reaches deterministic result state.
    - Embedded iframe runtime remains console-clean and pageerror-clean during spin interaction in EN/PT flows.
    - Canonical standalone `/en/slots/` and `/pt/slots/` routes preserve working spin behavior while non-canonical `/slots` drift is explicitly guarded.
    - Host-mode and standalone-mode visibility constraints remain explicit: house-edge lesson visible only in embed host mode and hidden in standalone mode.
  artifacts:
    - path: e2e/compatibility.spec.ts
      provides: browser-level regression checks for embedded iframe spin interaction, console/page errors, and standalone canonical safeguards.
    - path: tests/compatibility-contract.test.mjs
      provides: source-level canonical route and alias drift guards for standalone Slots parity.
    - path: public/sw.js
      provides: cache-versioned service worker baseline to reduce stale-client runtime mismatch risk.
  key_links:
    - from: src/pages/en/casinocraftz/index.astro
      to: src/pages/en/slots/index.astro
      via: iframe src `/en/slots/?host=casinocraftz` plus embedded spin assertions
      pattern: data-casinocraftz-slots-embed
    - from: src/pages/pt/casinocraftz/index.astro
      to: src/pages/pt/slots/index.astro
      via: iframe src `/pt/slots/?host=casinocraftz` plus embedded spin assertions
      pattern: data-casinocraftz-slots-embed
    - from: e2e/compatibility.spec.ts
      to: src/scripts/slots/controller.ts
      via: spin click -> state transition `idle|result` to `spinning` to `result`
      pattern: '#slots-spin-button'
validation_commands:
  - node --test tests/compatibility-contract.test.mjs
  - npx playwright test e2e/compatibility.spec.ts --project=chromium --workers=1 --grep "casinocraftz embeds slots module with canonical EN/PT host parity|slots runtime compatibility keeps machine-readable gameplay state in EN/PT"
  - npm run lint
  - npm run build
  - npm run lint && node --test tests/compatibility-contract.test.mjs && npx playwright test e2e/compatibility.spec.ts --project=chromium --workers=1 --grep "casinocraftz embeds slots module with canonical EN/PT host parity|slots runtime compatibility keeps machine-readable gameplay state in EN/PT" && npm run build
---

<objective>
Close diagnosed Phase 30 UAT gaps by adding deterministic, EN/PT-parity regression guards for embedded and standalone spin behavior.

Purpose: eliminate ambiguous spin-failure reports by enforcing interaction, route-canonical, and error-surface assertions.
Output: compatibility contracts and browser checks that fail fast on embedded iframe interaction regressions, standalone route drift, or runtime console/page errors.
</objective>

<execution_context>
@/home/gabriel/Documents/gabriel-rodrigues/.claude/get-shit-done/workflows/execute-plan.md
@/home/gabriel/Documents/gabriel-rodrigues/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-slots-integration-and-improvement-pass/30-UAT.md
@.planning/debug/30-embedded-spin-failure.md
@.planning/debug/30-standalone-spin-failure.md
@.planning/phases/30-slots-integration-and-improvement-pass/30-01-SUMMARY.md
@src/pages/en/casinocraftz/index.astro
@src/pages/pt/casinocraftz/index.astro
@src/pages/en/slots/index.astro
@src/pages/pt/slots/index.astro
@src/scripts/slots/controller.ts
@e2e/compatibility.spec.ts
@tests/compatibility-contract.test.mjs
@public/sw.js
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add embedded iframe spin and error-surface regression coverage for EN/PT Casinocraftz</name>
  <files>e2e/compatibility.spec.ts</files>
  <behavior>
    - Test 1: EN and PT embedded runtimes in Casinocraftz accept `#slots-spin-button` clicks and transition to deterministic `result` state.
    - Test 2: Embedded flows fail if iframe or page emits `console.error` or `pageerror` during interaction.
    - Test 3: Coverage includes both direct canonical load (`/en/casinocraftz/`, `/pt/casinocraftz/`) and SPA journey entry from projects page to guard lifecycle drift.
  </behavior>
  <action>
    Extend existing `casinocraftz embeds slots module with canonical EN/PT host parity` compatibility scenario to execute spin inside iframe and assert machine-readable post-spin state/outcome/seed snapshots for both languages.
    Add per-test error collectors for parent page and embedded frame console/page errors and fail test when error-level logs are captured.
    Keep deterministic assertions explicit by checking `data-slots-host=casinocraftz`, spin state transitions, and locale-specific seed progression.
  </action>
  <verify>
    <automated>npx playwright test e2e/compatibility.spec.ts --project=chromium --workers=1 --grep "casinocraftz embeds slots module with canonical EN/PT host parity"</automated>
  </verify>
  <done>Embedded spin interaction failures and console/page-error regressions are machine-detected for EN/PT host-mode paths.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Harden standalone canonical route safeguards and EN/PT spin parity assertions</name>
  <files>tests/compatibility-contract.test.mjs, e2e/compatibility.spec.ts</files>
  <behavior>
    - Test 1: Contracts fail when non-canonical `/slots` path or locale alias drift appears in source-level routing/content.
    - Test 2: Browser checks explicitly assert `/en/slots/` and `/pt/slots/` as canonical standalone routes with working spin interactions.
    - Test 3: Standalone checks fail if host mode leaves `standalone`, if house-edge lesson becomes visible in standalone mode, or if console/page errors occur during spin.
  </behavior>
  <action>
    Add contract-level guardrails that keep standalone routes locale-prefixed only and reject `/slots` alias reintroduction.
    Extend standalone compatibility journey to assert host `standalone`, hidden house-edge lesson, successful spin progression, and clean console/page error surface in both locales.
    Keep assertions selector/data-attribute driven and deterministic; do not add screenshot-based checks.
  </action>
  <verify>
    <automated>node --test tests/compatibility-contract.test.mjs && npx playwright test e2e/compatibility.spec.ts --project=chromium --workers=1 --grep "slots runtime compatibility keeps machine-readable gameplay state in EN/PT"</automated>
  </verify>
  <done>Standalone canonical behavior is guarded against route drift and spin regressions with explicit EN/PT parity checks.</done>
</task>

<task type="auto">
  <name>Task 3: Reduce stale-client drift risk with service-worker cache key versioning</name>
  <files>public/sw.js</files>
  <action>
    Bump service-worker cache identifier to a new deterministic version and preserve existing install/activate cleanup flow so stale cached slot scripts are invalidated after deploy.
    Keep behavior backward-safe by retaining current precache list and fetch strategy while ensuring older cache namespaces are purged on activation.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>New deployments invalidate old slot runtime assets, reducing stale-environment mismatch reports without changing gameplay authority logic.</done>
</task>

</tasks>

<validation_commands>

1. node --test tests/compatibility-contract.test.mjs
2. npx playwright test e2e/compatibility.spec.ts --project=chromium --workers=1 --grep "casinocraftz embeds slots module with canonical EN/PT host parity|slots runtime compatibility keeps machine-readable gameplay state in EN/PT"
3. npm run lint
4. npm run build
5. npm run lint && node --test tests/compatibility-contract.test.mjs && npx playwright test e2e/compatibility.spec.ts --project=chromium --workers=1 --grep "casinocraftz embeds slots module with canonical EN/PT host parity|slots runtime compatibility keeps machine-readable gameplay state in EN/PT" && npm run build

</validation_commands>

<verification>
SLOT-40 and SLOT-41 remain closed when embedded and standalone spin journeys are regression-guarded with explicit EN/PT deterministic assertions and error-surface checks, while cache versioning reduces stale-client false regressions.
</verification>

<success_criteria>

- Embedded EN/PT Casinocraftz iframe tests fail on broken spin interaction or any error-level console/page exception.
- Standalone EN/PT slots tests fail on canonical route drift, host-mode drift, lesson-visibility drift, or broken spin transitions.
- Deterministic seeds/states remain asserted for both locales in machine-readable checks.
- Lint, targeted contracts, targeted compatibility checks, and production build pass.

</success_criteria>

<output>
After completion, create .planning/phases/30-slots-integration-and-improvement-pass/30-02-SUMMARY.md
</output>
