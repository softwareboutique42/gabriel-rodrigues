---
phase: 12-compatibility-and-qa-hardening
plan: 01
type: execute
wave: 4
depends_on:
  - 11-01
files_modified:
  - tests/compatibility-contract.test.mjs
  - e2e/compatibility.spec.ts
autonomous: true
requirements:
  - COMP-01
must_haves:
  truths:
    - Discovery journeys from Projects to Canvas and Slots remain functional in both locales with exact canonical URL outcomes: /en/projects/ -> /en/canvas|slots and /pt/projects/ -> /pt/canvas|slots (COMP-01).
    - Language switching on /projects/, /canvas/, and /slots/ always lands on the exact EN/PT counterpart path and never falls back to home or mismatched locale routes (COMP-01).
    - Canonical route policy remains enforced: /en|pt/projects/, /en|pt/canvas/, and /en|pt/slots/ are valid surfaces, while /projects/canvas/* and /projects/slots/* aliases are treated as regression (COMP-01).
    - Regression checks provide fast-to-slower signal cadence: source contracts fail first, then focused compatibility E2E, then full E2E suite gate (COMP-01).
  artifacts:
    - path: tests/compatibility-contract.test.mjs
      provides: fast Node contract matrix for canonical routes, counterpart switching invariants, and alias deny-list enforcement
    - path: e2e/compatibility.spec.ts
      provides: runtime EN/PT compatibility journeys for Projects discovery flow and language-switch counterpart behavior
  key_links:
    - from: tests/compatibility-contract.test.mjs
      to: src/i18n/utils.ts
      via: getLocalizedPath counterpart matrix assertions for /projects/, /canvas/, and /slots/
      pattern: explicit EN/PT input-output pairs with exact path equality checks
    - from: tests/compatibility-contract.test.mjs
      to: src/pages/en/projects/index.astro and src/pages/pt/projects/index.astro
      via: canonical CTA href assertions and alias-route negative assertions
      pattern: fail if /projects/canvas/ or /projects/slots/ appear
    - from: e2e/compatibility.spec.ts
      to: src/pages/en/projects/index.astro and src/pages/pt/projects/index.astro
      via: click-through discovery journeys with strict URL assertions
      pattern: /{lang}/projects/ CTA click resolves to /{lang}/canvas/ and /{lang}/slots/
    - from: e2e/compatibility.spec.ts
      to: src/components/LanguageSwitcher.astro
      via: language-switch interactions on canonical surfaces
      pattern: exact counterpart URL checks for projects/canvas/slots surfaces
---

<objective>
Harden compatibility and QA coverage for the v1.2 IA by locking navigation, route, and locale behavior through deterministic contracts plus focused runtime regression flows.

Purpose: Fulfill COMP-01 with regression-first safety for canonical routing and EN/PT counterpart switching, without introducing new features or visual/UI redesign.
Output: New Node contract suite and dedicated Playwright compatibility suite that guard Projects discovery flow, canonical routes, and language-switch counterpart behavior.
</objective>

<execution_context>
@/home/gabriel/Documents/gabriel-rodrigues/.claude/get-shit-done/workflows/execute-plan.md
@/home/gabriel/Documents/gabriel-rodrigues/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/12-compatibility-and-qa-hardening/12-CONTEXT.md
@.planning/phases/12-compatibility-and-qa-hardening/12-RESEARCH.md
@.planning/phases/09-navigation-and-i18n-primitives/09-01-SUMMARY.md
@.planning/phases/10-projects-hub-delivery/10-01-SUMMARY.md
@.planning/phases/11-slots-shell-foundation/11-01-SUMMARY.md

@tests/nav-i18n-primitives.test.mjs
@tests/projects-hub-delivery.test.mjs
@tests/slots-shell-foundation.test.mjs
@e2e/navigation.spec.ts
@e2e/i18n.spec.ts
@src/i18n/utils.ts
@src/components/LanguageSwitcher.astro
@src/pages/en/projects/index.astro
@src/pages/pt/projects/index.astro
@src/pages/en/canvas/index.astro
@src/pages/pt/canvas/index.astro
@src/pages/en/slots/index.astro
@src/pages/pt/slots/index.astro
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add fast contract guardrails for canonical routes and EN/PT counterpart mapping (hardening only, per locked canonical and i18n decisions)</name>
  <files>tests/compatibility-contract.test.mjs</files>
  <behavior>
    - COMP-01: test fails when getLocalizedPath does not map exact counterparts for /en|pt/projects/, /en|pt/canvas/, and /en|pt/slots/.
    - COMP-01: test fails when canonical discovery targets drift away from /{lang}/canvas/ and /{lang}/slots/.
    - COMP-01: test fails if forbidden aliases /projects/canvas/* or /projects/slots/* appear in route targets.
    - Scope lock: test suite remains compatibility-only and does not introduce product feature assertions.
  </behavior>
  <action>Create a dedicated Node contract suite focused on route/i18n invariants and alias-route deny-list checks. Reuse established test style from Phases 9-11. Keep assertions deterministic and path-exact. Do not edit production UI/content except minimal testability support only if strictly required to stabilize selectors.</action>
  <verify>
    <automated>node --test tests/compatibility-contract.test.mjs</automated>
  </verify>
  <done>Fast contract gate exists and fails immediately on counterpart mismatch, canonical route drift, or alias-route introduction for projects/canvas/slots surfaces.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add focused Playwright compatibility journeys for Projects discovery and locale counterpart switching in EN/PT (per COMP-01)</name>
  <files>e2e/compatibility.spec.ts</files>
  <behavior>
    - Discovery journey checks pass only when:
      - /en/projects/ -> Canvas CTA -> /en/canvas/
      - /en/projects/ -> Slots CTA -> /en/slots/
      - /pt/projects/ -> Canvas CTA -> /pt/canvas/
      - /pt/projects/ -> Slots CTA -> /pt/slots/
    - Language switching checks pass only when /projects/, /canvas/, and /slots/ switch to exact EN/PT counterpart paths.
    - Tests fail on fallback-to-home behavior or wrong-locale destination.
  </behavior>
  <action>Create a dedicated Playwright compatibility spec for runtime regression locking. Use strict URL assertions over fuzzy text-only checks. Keep scope to compatibility and QA hardening; do not add new UI or feature behavior beyond minimal selector/testability support if unavoidable.</action>
  <verify>
    <automated>npx playwright test e2e/compatibility.spec.ts --project=chromium</automated>
  </verify>
  <done>Runtime compatibility coverage locks COMP-01 user journeys and counterpart switching behavior with explicit EN/PT matrix assertions.</done>
</task>

<task type="auto">
  <name>Task 3: Enforce fast-to-slower regression cadence and phase gate verification</name>
  <files>tests/compatibility-contract.test.mjs, e2e/compatibility.spec.ts</files>
  <action>Run verification in escalating cost order: fast contract gate first, focused compatibility E2E second, then full Playwright suite. Keep this phase strictly hardening-only and avoid shipping any non-test feature/UI changes unless required for test stability.</action>
  <verify>
    <automated>node --test tests/compatibility-contract.test.mjs && npx playwright test e2e/compatibility.spec.ts --project=chromium && npm run test</automated>
  </verify>
  <done>Compatibility hardening is enforced by a repeatable CI-friendly cadence that catches canonical/locale regressions early and confirms no suite-level breakage.</done>
</task>

</tasks>

<verification>
- node --test tests/compatibility-contract.test.mjs
- npx playwright test e2e/compatibility.spec.ts --project=chromium
- npm run test
</verification>

<success_criteria>

- COMP-01 is covered by both deterministic Node contracts and focused Playwright E2E runtime journeys.
- Canonical route surfaces for projects/canvas/slots are guarded, and forbidden /projects/canvas/_ and /projects/slots/_ alias drift triggers failures.
- EN/PT counterpart switching is explicitly locked for /projects/, /canvas/, and /slots/ with exact-path assertions.
- Verification workflow follows fast-to-slower cadence to maximize fail-fast diagnostics.
- No feature or visual redesign work is introduced beyond minimal testability support needed to keep compatibility checks stable.
  </success_criteria>

<output>
After completion, create .planning/phases/12-compatibility-and-qa-hardening/12-01-SUMMARY.md
</output>
