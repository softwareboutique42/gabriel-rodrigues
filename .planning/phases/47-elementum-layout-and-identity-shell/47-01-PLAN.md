---
phase: 47-elementum-layout-and-identity-shell
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/en/slots/index.astro
  - src/pages/pt/slots/index.astro
  - src/i18n/en.json
  - src/i18n/pt.json
autonomous: true
requirements: [UIR-81]
user_setup: []

must_haves:
  truths:
    - "User sees ELEMENTUM label centered at the top of the slots cabinet on both EN and PT routes"
    - "User sees ← CasinoCraftz back link in the top-left corner on both EN and PT routes"
    - "Both identity anchors respect reduced-motion and motion-policy settings"
    - "Navigation labels match locale (EN/PT) with proper i18n bindings"
  artifacts:
    - path: "src/pages/en/slots/index.astro"
      provides: "EN slots page with top-level identity/navigation header structure"
      min_changes: "Add <header> zone above .slots-cabinet with ELEMENTUM + back link"
    - path: "src/pages/pt/slots/index.astro"
      provides: "PT slots page with top-level identity/navigation header structure"
      min_changes: "Mirror EN structure with PT locale bindings"
    - path: "src/i18n/en.json"
      provides: "EN translation keys for ELEMENTUM and back-link labels"
      exports: ["slots.shell.identity.elementum", "slots.shell.identity.backLink"]
    - path: "src/i18n/pt.json"
      provides: "PT translation keys for identity labels"
      exports: ["slots.shell.identity.elementum", "slots.shell.identity.backLink"]
  key_links:
    - from: "src/pages/en/slots/index.astro"
      to: "src/i18n/en.json"
      via: "useTranslations hook"
      pattern: "t\\('slots\\.shell\\.identity"
    - from: "src/pages/pt/slots/index.astro"
      to: "src/i18n/pt.json"
      via: "useTranslations hook"
      pattern: "t\\('slots\\.shell\\.identity"
---

<objective>
Add top-level identity and navigation anchors (`ELEMENTUM` title + `← CasinoCraftz` back link) to EN/PT Slots routes.

**Purpose:** Establish visual anchoring that signals brand/product identity and provides clear navigation escape hatch. Delivers requirement UIR-81 (top identity anchors visible on both locales).

**Output:** 
- EN/PT slots pages with new `<header>` zone above gameplay cabinet
- i18n keys for both locales
- No styling yet (styling lands in Wave 1 Task 2)
</objective>

<execution_context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/47-elementum-layout-and-identity-shell/47-CONTEXT.md
</execution_context>

<context>
**Current structure:** Both EN/PT slots pages mount a `.slots-shell` section containing `.slots-cabinet`.

**Current identity affordances:** Only a menu toggle (+) button within the cabinet, no top-level identity zone. Phase 47 adds a dedicated header above the cabinet.

**i18n pattern:** Established via `useTranslations(locale)` from `src/i18n/utils.ts`. New keys must follow `slots.shell.identity.*` namespace.

**EN/PT parity requirement:** Any markup changes must be mirrored exactly on both page files, with only locale-specific `t()` calls differing. CSS-only breaks are acceptable.

**Locked decisions (from 47-CONTEXT.md):**
- D-05: ELEMENTUM label is fixed text (no variable)
- D-06: Back-link copy is identical: `← CasinoCraftz` (same on both locales)
- D-08: Balanced defaults; no over-constraining stylistic choices at markup stage
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add identity/navigation header markup to EN slots page</name>
  <files>src/pages/en/slots/index.astro</files>
  <action>
    Insert a new `<header>` element between the `.slots-shell__backdrop` div and the `.slots-cabinet` article.
    
    Structure:
    ```html
    <header class="slots-shell__header" data-slots-zone="navigation">
      <a href="/" class="slots-shell__back-link" aria-label="Back to Casinocraftz">← CasinoCraftz</a>
      <div class="slots-shell__identity">
        <span class="slots-shell__identity-label">ELEMENTUM</span>
      </div>
    </header>
    ```
    
    Notes:
    - Use `data-slots-zone="navigation"` for consistency with existing dataset pattern
    - Back link is a true `<a>` to "/" (projects/home page navigation)
    - ELEMENTUM is semantically a `<span>` (not a link, not interactive)
    - Use `aria-label` on back link for screen reader clarity
    - Do NOT use i18n bindings for ELEMENTUM or back-link text (per D-05, D-06: fixed copy)
    - Do NOT add any inline styles; all styling in CSS later
  </action>
  <verify>
    <automated>grep -c "slots-shell__header" src/pages/en/slots/index.astro</automated>
  </verify>
  <done>
    - Header element present in EN slots page between backdrop and cabinet
    - Back link and identity label structure matches template above
    - No CSS applied yet (will be task 2)
  </done>
</task>

<task type="auto">
  <name>Task 2: Add identity/navigation header markup to PT slots page</name>
  <files>src/pages/pt/slots/index.astro</files>
  <action>
    Mirror the header structure from EN slots page (Task 1) exactly. Both locale versions must have identical markup:
    ```html
    <header class="slots-shell__header" data-slots-zone="navigation">
      <a href="/" class="slots-shell__back-link" aria-label="Back to Casinocraftz">← CasinoCraftz</a>
      <div class="slots-shell__identity">
        <span class="slots-shell__identity-label">ELEMENTUM</span>
      </div>
    </header>
    ```
    
    Notes:
    - Copy exact structure from EN; both ELEMENTUM and back-link text remain identical
    - PT route uses same English text (D-06: identical in both locales)
    - No i18n keys needed for these hardcoded labels
  </action>
  <verify>
    <automated>diff -q <(grep -A 6 'slots-shell__header' src/pages/en/slots/index.astro | sed 's/pt\|en//g') <(grep -A 6 'slots-shell__header' src/pages/pt/slots/index.astro | sed 's/pt\|en//g')</automated>
  </verify>
  <done>
    - Header element present in PT slots page with identical structure to EN
    - Both pages now have matching navigation zone markup
  </done>
</task>

<task type="auto">
  <name>Task 3: Add translation keys for identity labels (future extensibility)</name>
  <files>src/i18n/en.json, src/i18n/pt.json</files>
  <action>
    Add i18n keys to both EN and PT translation files for future language support extensibility (not used in Phase 47 markup, but prepared for downstream phases).
    
    In `src/i18n/en.json`, append to the `slots.shell.*` namespace:
    ```json
    "slots.shell.identity.elementum": "ELEMENTUM",
    "slots.shell.identity.backLink": "← CasinoCraftz"
    ```
    
    In `src/i18n/pt.json`, append identical values:
    ```json
    "slots.shell.identity.elementum": "ELEMENTUM",
    "slots.shell.identity.backLink": "← CasinoCraftz"
    ```
    
    Notes:
    - Both locales store the same text (D-06: identical copy in both)
    - Keys follow established `slots.shell.*` pattern
    - These keys are NOT used in Phase 47 markup (hardcoded in HTML), but ensure future localization is possible
    - Prepare for Phase 48+: if identity branding becomes language-specific, the infrastructure exists
  </action>
  <verify>
    <automated>grep -c "slots.shell.identity.elementum" src/i18n/en.json && grep -c "slots.shell.identity.elementum" src/i18n/pt.json</automated>
  </verify>
  <done>
    - Translation keys added to both EN and PT files under `slots.shell.identity.*` namespace
    - Values match (ELEMENTUM and ← CasinoCraftz)
    - Infrastructure ready for future i18n binding if needed
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User → Display | Browser renders hardcoded labels; no user input affects these strings |
| Navigation Link | Back link targets "/" (projects/home); no unsafe redirects |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-47-01 | Repudiation | Navigation link (`← CasinoCraftz` → `/`) | Accept | Safe static navigation; no state mutations. Target "/" is canonical root. |
| T-47-02 | Information Disclosure | Header markup in HTML source | Accept | No secrets, credentials, or PII in markup. Header is public UI. |
| T-47-03 | Denial of Service | Missing back-link target | Mitigate | Verify `/` route exists and renders normally in verification step (Wave 3). |

</threat_model>

<verification>
1. **Markup parity:** Both EN/PT slots pages contain identical header structure except locale context.
2. **Navigation functionality:** Back link (`<a href="/"> href`) targets valid route.
3. **No visual breakage:** Header zone exists but is unstyled; no layout drift before Task 2 styling.
4. **i18n keys present:** Both JSON files updated with identity namespace keys.
</verification>

<success_criteria>
- ✓ EN slots page contains `<header class="slots-shell__header">` with back link and ELEMENTUM label
- ✓ PT slots page mirrors EN header markup exactly
- ✓ Both i18n files have `slots.shell.identity.*` keys defined
- ✓ No styling applied yet (purely structural markup addition)
- ✓ Accessibility: Back link has clear `aria-label`, ELEMENTUM is neutral `<span>`
</success_criteria>

<output>
After completion, create `.planning/phases/47-elementum-layout-and-identity-shell/47-01-PLAN-SUMMARY.md` documenting:
- Commits: One commit per task (3 total)
- Files modified summary
- Wave 1 progress: UIR-81 partial (markup in place, styling pending Task 2)
</output>
