---
phase: 47-elementum-layout-and-identity-shell
plan: 05
type: execute
wave: 3
depends_on: [47-01, 47-02, 47-03, 47-04]
files_modified: []
autonomous: true
requirements: [UIR-80, UIR-81]
user_setup: []

must_haves:
  truths:
    - "EN and PT slots pages render identical visual layout and styling (CSS is shared, markup is mirrored)"
    - "EN labels in i18n and page markup are correctly spelled and grammatically sound"
    - "PT labels in i18n and page markup are correctly spelled and grammatically sound"
    - "No EN/PT asymmetry in header positioning, HUD alignment, or responsive behavior"
    - "ELEMENTUM title and ← CasinoCraftz back-link appear identically on both locales"
    - "Reduced-motion preference disables fade-in animation on identity label, preserves HUD functionality"
  artifacts:
    - path: "src/pages/en/slots/index.astro AND src/pages/pt/slots/index.astro"
      provides: "Verification that both files have identical structure and parity"
      min_check: "Header and HUD markup match exactly; only i18n bindings differ by locale"
    - path: "src/i18n/en.json AND src/i18n/pt.json"
      provides: "Verification that translation keys are present and correct"
      min_check: "Both files have same keys under slots.shell.* namespace"
    - path: "src/styles/global.css"
      provides: "Final CSS verification: no EN/PT-specific selectors, all shared"
      min_check: "All slots-* classes are locale-agnostic"
  key_links:
    - from: "src/pages/en/slots/index.astro"
      to: "src/pages/pt/slots/index.astro"
      via: "Expected markup parity (EN/PT, excluding useTranslations locale param)"
      pattern: "slots-shell__header|data-slots-zone"
    - from: "src/i18n/en.json"
      to: "src/i18n/pt.json"
      via: "Translation key parity check"
      pattern: "slots\\.shell\\.identity\\.(elementum|backLink)"
---

<objective>
Audit EN/PT parity across markup, translations, and styling. Verify no locale-specific visual asymmetry, and confirm that accessibility (reduced-motion, color contrast, aria labels) meets standards.

**Purpose:** Ensure UIR-80 and UIR-81 deliver the same experience on both locales. Prevent layout drift or label mismatches between EN and PT routes.

**Output:** 
- Confirmation that EN/PT markup and styling are perfectly mirrored
- Translation keys present and grammatically sound on both locales
- Accessibility checklist complete (reduced-motion, contrast, labels)
</objective>

<execution_context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/47-elementum-layout-and-identity-shell/47-CONTEXT.md
</execution_context>

<context>
**EN/PT parity contract (from CLAUDE.md):**
- All pages live under `src/pages/en/` and `src/pages/pt/` — duplicated routes with shared styling
- Markup must be identical except for `useTranslations(locale)` locale parameter
- CSS is shared in `src/styles/global.css` — no locale-specific selectors
- Translations live in `src/i18n/en.json` and `src/i18n/pt.json`

**Phase 47 parity requirements:**
- ELEMENTUM label is identical on both locales (D-05: fixed text, not translated)
- ← CasinoCraftz back-link is identical on both locales (D-06: fixed text, not translated)
- Header positioning, HUD layout, and responsive behavior must be optical duplicates
- No CSS `@media` or selectors target EN/PT specifically

**Accessibility requirements:**
- Color contrast: `#b8ff2c` on `#0e0e0e` ≥ 8:1 WCAG AAA ✓ (already verified in Task 47-02)
- Reduced-motion: fade-in animation skips under `prefers-reduced-motion: reduce`
- Aria labels: Back link has `aria-label="Back to Casinocraftz"`
- Screen reader: ELEMENTUM label is semantic (`<span>`, no aria-hidden)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit markup parity between EN/PT slots pages</name>
  <files>src/pages/en/slots/index.astro, src/pages/pt/slots/index.astro</files>
  <action>
    Compare the markup structure of both EN and PT slots pages to ensure identical layout except for locale context.
    
    Steps:
    1. Extract the header section from both files:
       ```bash
       grep -A 6 "slots-shell__header" src/pages/en/slots/index.astro > /tmp/en-header.txt
       grep -A 6 "slots-shell__header" src/pages/pt/slots/index.astro > /tmp/pt-header.txt
       ```
    
    2. Extract the HUD section from both files:
       ```bash
       grep -A 20 "slots-stage__hud--minimal" src/pages/en/slots/index.astro > /tmp/en-hud.txt
       grep -A 20 "slots-stage__hud--minimal" src/pages/pt/slots/index.astro > /tmp/pt-hud.txt
       ```
    
    3. Compare: should be identical (no differences in class names, attributes, or structure)
       ```bash
       diff /tmp/en-header.txt /tmp/pt-header.txt
       diff /tmp/en-hud.txt /tmp/pt-hud.txt
       ```
    
    Expected: No diff output (files identical). If any differences exist, investigate and fix:
    - If difference is in a translation key (e.g., `t('slots.gameplay.label.balance')`), that's expected ✓
    - If difference is in class names or structure, fix to restore parity
    - If difference is in the header or HUD structure itself, this is a parity violation ✗
    
    Do NOT change class attributes or classes between EN/PT versions.
  </action>
  <verify>
    <automated>diff <(grep -A 6 "slots-shell__header" src/pages/en/slots/index.astro | sed '/^$/d') <(grep -A 6 "slots-shell__header" src/pages/pt/slots/index.astro | sed '/^$/d') | grep -c "^<\|^>" || echo "0 differences"</automated>
  </verify>
  <done>
    - Header markup identical on EN/PT
    - HUD markup identical on EN/PT
    - No parity violations detected
  </done>
</task>

<task type="auto">
  <name>Task 2: Audit translation key parity in i18n JSON files</name>
  <files>src/i18n/en.json, src/i18n/pt.json</files>
  <action>
    Verify that all translation keys related to Phase 47 (shell identity) exist in both EN and PT files and have matching structure.
    
    Steps:
    1. Extract all `slots.shell.*` keys from EN:
       ```bash
       grep "slots.shell" src/i18n/en.json | head -20
       ```
    
    2. Extract all `slots.shell.*` keys from PT:
       ```bash
       grep "slots.shell" src/i18n/pt.json | head -20
       ```
    
    3. Verify that both files have:
       - `"slots.shell.identity.elementum"` (both EN and PT)
       - `"slots.shell.identity.backLink"` (both EN and PT)
       - Values should be identical (ELEMENTUM and ← CasinoCraftz)
    
    4. Verify no typos in keys:
       - Should be `slots.shell.identity.elementum` (not `elementum.shell` or `identity.elementum`)
       - No extra spaces or punctuation
    
    Expected: Both files have matching keys. Values are identical (per D-05, D-06).
  </action>
  <verify>
    <automated>grep "slots.shell.identity" src/i18n/en.json && grep "slots.shell.identity" src/i18n/pt.json && echo "Keys present in both files"</automated>
  </verify>
  <done>
    - Translation keys present in both EN and PT files
    - Values match (ELEMENTUM and ← CasinoCraftz)
    - No key-name mismatches
  </done>
</task>

<task type="auto">
  <name>Task 3: Verify no EN/PT-specific CSS selectors or media queries</name>
  <files>src/styles/global.css</files>
  <action>
    Search for any CSS selectors that target EN/PT locales specifically. This would be a CSS anti-pattern (locales should be handled in markup/i18n, not CSS).
    
    Commands:
    ```bash
    grep -n "en-" src/styles/global.css || echo "No 'en-' prefixes found"
    grep -n "pt-" src/styles/global.css || echo "No 'pt-' prefixes found"
    grep -n "\[lang\|data-lang" src/styles/global.css || echo "No lang attribute selectors found"
    ```
    
    Expected: Zero matches. All CSS is locale-agnostic and applies to both EN and PT equally.
    
    Notes:
    - If locale-specific styling is needed (rare), it should be in the Astro component, not CSS
    - Current design requires no such separation; all styling is shared
  </action>
  <verify>
    <automated>grep -c "en-\|pt-\|\[lang\|\[data-lang" src/styles/global.css || echo "0"</automated>
  </verify>
  <done>
    - No EN/PT-specific CSS selectors found
    - All styling is locale-agnostic and shared
  </done>
</task>

<task type="auto">
  <name>Task 4: Audit accessibility: reduced-motion, color contrast, aria labels</name>
  <files>src/pages/en/slots/index.astro, src/pages/pt/slots/index.astro, src/styles/global.css</files>
  <action>
    Perform accessibility audit on Phase 47 changes:
    
    **1. Reduced-motion check:**
    ```bash
    grep -n "prefers-reduced-motion" src/styles/global.css | grep -i elementum
    ```
    
    Expected: Found. Animation `elementum-fadein` should have a `@media (prefers-reduced-motion: reduce)` block that disables it.
    
    **2. Color contrast check (manual math):**
    - Back-link color: `#b8ff2c` (lime) on `#0e0e0e` (dark bg)
      - Relative luminance: `#b8ff2c` ≈ 0.65, `#0e0e0e` ≈ 0.004
      - Contrast ratio = (0.65 + 0.05) / (0.004 + 0.05) ≈ 12.5 (WCAG AAA ✓)
    - Identity label color: `#8ff5ff` (cyan) on `#0e0e0e`
      - Relative luminance: `#8ff5ff` ≈ 0.43, `#0e0e0e` ≈ 0.004
      - Contrast ratio ≈ 8.5 (WCAG AAA ✓)
    
    **3. Aria label check:**
    ```bash
    grep -n "aria-label" src/pages/en/slots/index.astro | grep back-link
    grep -n "aria-label" src/pages/pt/slots/index.astro | grep back-link
    ```
    
    Expected: Both files have `aria-label="Back to Casinocraftz"` on the back-link.
    
    **4. Screen reader markup check:**
    ```bash
    grep -n "aria-hidden" src/pages/en/slots/index.astro | grep elementum
    ```
    
    Expected: ELEMENTUM label should NOT have `aria-hidden`. It's semantic content (a `<span>`, not decorative).
  </action>
  <verify>
    <automated>grep -c "prefers-reduced-motion" src/styles/global.css && grep -c "aria-label.*Back" src/pages/en/slots/index.astro && echo "Accessibility checks passed"</automated>
  </verify>
  <done>
    - Reduced-motion animation disabled under preference
    - Color contrast ratios meet WCAG AAA standards
    - Aria labels present on interactive elements
    - Screen reader markup correct (ELEMENTUM is semantic)
  </done>
</task>

<task type="auto">
  <name>Task 5: Run ESLint and Prettier checks on modified files</name>
  <files>src/pages/en/slots/index.astro, src/pages/pt/slots/index.astro, src/styles/global.css, src/i18n/en.json, src/i18n/pt.json</files>
  <action>
    Run project linting and formatting tools to ensure code quality:
    
    ```bash
    npm run lint -- --fix src/pages/en/slots/index.astro src/pages/pt/slots/index.astro
    npm run format -- --check src/styles/global.css src/i18n/en.json src/i18n/pt.json
    ```
    
    Expected: No errors or warnings. If `--fix` makes changes, note them; if formatting warnings appear, run:
    ```bash
    npm run format src/styles/global.css src/i18n/en.json src/i18n/pt.json
    ```
    
    Commit any lint/format fixes separately (may happen automatically via husky pre-commit hook).
  </action>
  <verify>
    <automated>npm run lint -- src/pages/en/slots/index.astro 2>&1 | grep -c "error:" || echo "0 lint errors"</automated>
  </verify>
  <done>
    - ESLint checks pass
    - Prettier formatting compliant
    - No code quality warnings
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|-----------|-------------|
| EN/PT parity | Markup structure must be identical to prevent locale-specific bugs |
| Accessibility compliance | Contrast ratios and aria labels affect screen reader users |
| Linting | Code quality enforced; violations could indicate hidden issues |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-47-11 | Information Disclosure | Different visual treatment on EN vs PT | Mitigate | Audit tasks 1, 2, 3 verify parity; no locale-specific styling. |
| T-47-12 | Accessibility | Screen reader users miss content | Mitigate | Aria-labels and semantic markup verified in Task 4. Contrast ratios > 8:1. |
| T-47-13 | Code Quality | Linting violations indicate hidden bugs | Mitigate | ESLint and Prettier run in Task 5. |

</threat_model>

<verification>
1. **Markup parity:** diff output shows no differences between EN/PT header/HUD structures (excluding i18n keys).
2. **Translation keys:** Both EN and PT i18n files have `slots.shell.identity.elementum` and `slots.shell.identity.backLink` with identical values.
3. **No locale-specific CSS:** grep returns 0 matches for `en-`, `pt-`, `[lang`, or `[data-lang` selectors.
4. **Accessibility:** Reduced-motion rule found, color contrast ratios verified, aria-labels present, screen reader markup correct.
5. **Linting:** npm run lint returns no errors; npm run format returns no warnings.
</verification>

<success_criteria>
- ✓ EN and PT markup identical (header + HUD structure mirrored)
- ✓ Translation keys present in both i18n files with correct naming
- ✓ No EN/PT-specific CSS selectors (all styling is shared)
- ✓ Reduced-motion animation disabled under `prefers-reduced-motion: reduce`
- ✓ Color contrast ratios: both lime and cyan ≥ 8:1 (WCAG AAA)
- ✓ Aria labels present on back-link
- ✓ ELEMENTUM label is semantic (not aria-hidden)
- ✓ ESLint passes; Prettier formatting compliant
- ✓ UIR-80 and UIR-81 verified parity across both locales
</success_criteria>

<output>
After completion, create `.planning/phases/47-elementum-layout-and-identity-shell/47-05-PLAN-SUMMARY.md` documenting:
- Commits: None — this is a verification-only task (no code changes from this plan)
- Audit results: All checks passed, parity confirmed
- Wave 3 ready for final commit
</output>
