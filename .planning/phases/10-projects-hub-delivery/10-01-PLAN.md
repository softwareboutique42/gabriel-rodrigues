---
phase: 10-projects-hub-delivery
plan: 01
type: execute
wave: 2
depends_on:
  - 09-01
files_modified:
  - src/pages/en/projects/index.astro
  - src/pages/pt/projects/index.astro
  - src/components/LanguageSwitcher.astro
  - src/i18n/en.json
  - src/i18n/pt.json
  - tests/projects-hub-delivery.test.mjs
autonomous: true
requirements:
  - HUB-02
  - HUB-03
  - I18N-02
must_haves:
  truths:
    - EN and PT projects hubs exist at /en/projects/ and /pt/projects/ with exactly two primary project cards (Canvas and Slots) and visible status labels (HUB-02).
    - Each projects card exposes one canonical CTA only, linking Canvas and Slots to locale-matched canonical routes without /projects/* aliases (HUB-03).
    - Language switch on projects routes keeps users on the projects counterpart route (en<->pt) instead of dropping context to home or project detail routes (I18N-02).
    - Regression tests fail on EN/PT projects route parity drift, canonical-link drift, or language-switch context loss (HUB-02, HUB-03, I18N-02).
  artifacts:
    - path: src/pages/en/projects/index.astro
      provides: EN projects hub page with two-card hero split and single-CTA medium-detail cards
    - path: src/pages/pt/projects/index.astro
      provides: PT projects hub page mirroring EN structure and canonical CTA behavior
    - path: src/components/LanguageSwitcher.astro
      provides: Locale switch link behavior that preserves projects route context
    - path: src/i18n/en.json
      provides: EN projects-hub labels and status copy consumed by projects cards
    - path: src/i18n/pt.json
      provides: PT projects-hub labels and status copy with EN key parity
    - path: tests/projects-hub-delivery.test.mjs
      provides: Focused automated regression checks for projects parity, canonical links, and switch behavior
  key_links:
    - from: src/pages/en/projects/index.astro
      to: /en/canvas/ and /en/slots/
      via: card CTA hrefs
      pattern: no /en/projects/canvas or /en/projects/slots aliases
    - from: src/pages/pt/projects/index.astro
      to: /pt/canvas/ and /pt/slots/
      via: card CTA hrefs
      pattern: no /pt/projects/canvas or /pt/projects/slots aliases
    - from: src/components/LanguageSwitcher.astro
      to: src/i18n/utils.ts:getLocalizedPath
      via: targetPath derivation from current pathname
      pattern: /en/projects/ <-> /pt/projects/ mapping remains context-preserving
    - from: tests/projects-hub-delivery.test.mjs
      to: src/pages/en/projects/index.astro and src/pages/pt/projects/index.astro
      via: static contract assertions
      pattern: route parity + canonical hrefs + no alias links
---

<objective>
Deliver Phase 10 Projects hub pages as the bilingual discovery layer for Canvas and Slots, while preserving canonical Canvas/Slots routing and route-context language switching.

Purpose: Satisfy HUB-02, HUB-03, and I18N-02 without pulling deferred alias routing, gameplay work, or Phase 12 hardening into this plan.
Output: EN/PT projects pages, locale-parity copy updates, language-switch context safeguard, and focused regression tests.
</objective>

<execution_context>
@/home/gabriel/Documents/gabriel-rodrigues/.claude/get-shit-done/workflows/execute-plan.md
@/home/gabriel/Documents/gabriel-rodrigues/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/10-projects-hub-delivery/10-CONTEXT.md
@.planning/phases/10-projects-hub-delivery/10-RESEARCH.md
@.planning/phases/09-navigation-and-i18n-primitives/09-01-SUMMARY.md

@src/pages/en/index.astro
@src/pages/pt/index.astro
@src/components/Header.astro
@src/components/LanguageSwitcher.astro
@src/i18n/utils.ts
@src/i18n/en.json
@src/i18n/pt.json
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build EN/PT projects hubs with two-card hero split and medium-detail single-CTA cards (per D-01 and D-02)</name>
  <files>src/pages/en/projects/index.astro, src/pages/pt/projects/index.astro, src/i18n/en.json, src/i18n/pt.json</files>
  <behavior>
    - /en/projects/ and /pt/projects/ each render one Canvas card and one Slots card in a two-card hero split.
    - Each card includes title, status, concise description, and one primary CTA only.
    - Canvas status is visible as live/active; Slots status is visible as foundation/in-development.
  </behavior>
  <action>Create locale-specific projects index pages with mirrored structural hierarchy and medium-detail card content. Add or adjust translation keys only as needed and keep strict EN/PT key parity in the same task. Keep implementation scoped to discovery-layer layout and copy only; do not add slots gameplay UI, nested project routes, or broad compatibility hardening.</action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>Both projects pages compile and render with two-card split, status labels, and single CTA per card using locale-parity strings.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire canonical CTA routing and preserve projects-context language switching (per D-03 and D-04)</name>
  <files>src/pages/en/projects/index.astro, src/pages/pt/projects/index.astro, src/components/LanguageSwitcher.astro</files>
  <behavior>
    - EN projects CTAs point only to /en/canvas/ and /en/slots/.
    - PT projects CTAs point only to /pt/canvas/ and /pt/slots/.
    - Switching language from /en/projects/ resolves to /pt/projects/ and vice versa.
  </behavior>
  <action>Set project-card CTAs to canonical Canvas/Slots routes only and explicitly avoid /projects/* aliases in markup and helpers. Ensure language switch continues to derive target path from current pathname so projects context is preserved across locale toggle. Keep route behavior focused on projects/canvas/slots mapping only; do not introduce redirects, aliases, or slots gameplay changes.</action>
  <verify>
    <automated>set -e; ! rg -n "/(en|pt)/projects/(canvas|slots)" src/pages/en/projects/index.astro src/pages/pt/projects/index.astro; rg -n "(/en/canvas/|/en/slots/|/pt/canvas/|/pt/slots/)" src/pages/en/projects/index.astro src/pages/pt/projects/index.astro; rg -n "getLocalizedPath\(|href=\{targetPath\}" src/components/LanguageSwitcher.astro</automated>
  </verify>
  <done>Canonical links are enforced, alias links are absent, and locale switch remains projects-context preserving.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add focused automated regression coverage for projects parity, canonical links, and switch behavior</name>
  <files>tests/projects-hub-delivery.test.mjs</files>
  <behavior>
    - Test fails if EN/PT projects route files diverge from required card presence and status contract.
    - Test fails if projects CTAs drift from canonical /canvas and /slots locale routes.
    - Test fails if language-switch contract no longer preserves /projects/ context.
    - Test fails if language-switch counterpart mapping for /canvas and /slots routes regresses (en<->pt route parity).
  </behavior>
  <action>Create a narrow node:test contract suite that reads source files and asserts: EN/PT projects files exist, both include Canvas and Slots card contracts, canonical hrefs are present, /projects/* alias hrefs are absent, and LanguageSwitcher still uses localized-path mapping that preserves current route context. Add explicit counterpart-switch assertions for /en/projects/↔/pt/projects/, /en/canvas/↔/pt/canvas/, and /en/slots/↔/pt/slots/. Keep scope limited to Phase 10 checks; do not add broad cross-browser compatibility hardening reserved for Phase 12.</action>
  <verify>
    <automated>node --test tests/projects-hub-delivery.test.mjs</automated>
  </verify>
  <done>Focused regression suite passes and protects HUB-02, HUB-03, and I18N-02 from projects/canvas/slots route and locale drift.</done>
</task>

</tasks>

<verification>
- npm run build
- node --test tests/nav-i18n-primitives.test.mjs
- node --test tests/projects-hub-delivery.test.mjs
</verification>

<success_criteria>

- /en/projects/ and /pt/projects/ are implemented as mirrored two-card hero discovery hubs with medium-detail cards and one CTA each.
- Projects card links remain canonical to locale-matched /canvas and /slots routes with no /projects/\* alias routes introduced.
- Language switching from projects routes preserves projects context across EN/PT.
- Focused automated tests cover projects parity, canonical links, and switch behavior and pass consistently.
- No slots gameplay expansion and no broad compatibility hardening work is introduced in this plan.
  </success_criteria>

<output>
After completion, create .planning/phases/10-projects-hub-delivery/10-01-SUMMARY.md
</output>
