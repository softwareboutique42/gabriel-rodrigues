---
phase: 17-slots-runtime-coverage-hardening
plan: 01
type: execute
wave: 1
depends_on:
  - 16-01
files_modified:
  - e2e/compatibility.spec.ts
  - .planning/phases/17-slots-runtime-coverage-hardening/17-01-SUMMARY.md
  - .planning/STATE.md
autonomous: true
requirements: []
---

<objective>
Close remaining v1.3 runtime coverage debt by hardening browser-level Slots assertions for PT runtime flow, insufficient-credit behavior, and localized runtime messaging.

Purpose: convert milestone status from verification-complete-with-tech-debt to stronger runtime confidence without changing shipped gameplay behavior.
Output: updated focused Playwright compatibility coverage and a Phase 17 summary artifact.
</objective>

<scope>
In scope:
- Extend `e2e/compatibility.spec.ts` to exercise PT Slots runtime state transitions beyond static attribute checks.
- Add deterministic insufficient-credit browser-flow coverage that verifies spin-block behavior.
- Add explicit localized runtime message assertions (EN/PT) where stable and practical.
- Preserve canonical route behavior and current deterministic gameplay/economy contracts.

Out of scope:

- New gameplay capabilities, payout rules, or economy model changes.
- Cross-browser expansion beyond focused Chromium compatibility path.
- Route architecture updates or alias exceptions.
- Visual redesign or UX feature additions.
  </scope>

<execution_context>
@/home/gabriel/Documents/gabriel-rodrigues/.claude/get-shit-done/workflows/execute-plan.md
@/home/gabriel/Documents/gabriel-rodrigues/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/v1.3-MILESTONE-AUDIT.md
@.planning/phases/17-slots-runtime-coverage-hardening/17-CONTEXT.md
@.planning/phases/16-milestone-verification-backfill/16-01-SUMMARY.md
@e2e/compatibility.spec.ts
@src/pages/en/slots/index.astro
@src/pages/pt/slots/index.astro
@src/scripts/slots/controller.ts
@src/scripts/slots/economy.ts
@src/i18n/en.json
@src/i18n/pt.json
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Deepen PT runtime Slots journey in compatibility E2E</name>
  <files>e2e/compatibility.spec.ts</files>
  <action>
    Expand the existing compatibility suite so PT coverage executes active gameplay transitions in-browser:
    - assert PT root starts in an allowed initial state,
    - trigger spin action and assert `spinning` intermediate state,
    - assert completion state (`result|insufficient`) and stable outcome attributes,
    - keep checks deterministic and centered on existing machine-readable hooks.
  </action>
  <verify>
    <automated>npx playwright test e2e/compatibility.spec.ts --project=chromium</automated>
  </verify>
  <done>PT runtime coverage includes exercised lifecycle transitions rather than navigation-only checks.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add insufficient-credit browser-flow assertion</name>
  <files>e2e/compatibility.spec.ts</files>
  <action>
    Add a focused insufficient-credit journey using existing controls and state hooks:
    - drive balance/bet to an insufficient condition through supported UI interactions,
    - attempt spin under insufficient condition,
    - assert blocked behavior and expected `data-slots-state`/status semantics.
    Keep implementation resilient to minor copy changes by pairing text checks with state attributes.
  </action>
  <verify>
    <automated>npx playwright test e2e/compatibility.spec.ts --project=chromium</automated>
  </verify>
  <done>Insufficient-credit path is exercised in browser and fails if blocking behavior regresses.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Assert localized runtime messaging in EN/PT</name>
  <files>e2e/compatibility.spec.ts</files>
  <action>
    Add direct EN/PT runtime localization assertions after meaningful state transitions:
    - verify at least one EN runtime status/message string is rendered,
    - verify corresponding PT runtime status/message string is rendered,
    - keep selectors robust and assertions scoped to gameplay state context.
  </action>
  <verify>
    <automated>npx playwright test e2e/compatibility.spec.ts --project=chromium && node --test tests/slots-interaction-guards-contract.test.mjs tests/slots-i18n-parity-contract.test.mjs && npm run build</automated>
  </verify>
  <done>Browser-level localized runtime messaging is directly asserted for EN/PT without widening phase scope.</done>
</task>

</tasks>

<validation_commands>

1. npx playwright test e2e/compatibility.spec.ts --project=chromium
2. node --test tests/slots-interaction-guards-contract.test.mjs tests/slots-i18n-parity-contract.test.mjs
3. npm run build
4. npx playwright test e2e/compatibility.spec.ts --project=chromium && node --test tests/slots-interaction-guards-contract.test.mjs tests/slots-i18n-parity-contract.test.mjs && npm run build

</validation_commands>

<rollback_notes>

- Revert only Phase 17 runtime-hardening test changes if instability appears:
  - e2e/compatibility.spec.ts
  - .planning/phases/17-slots-runtime-coverage-hardening/17-01-SUMMARY.md
  - .planning/STATE.md
- Do not change slots runtime implementation unless a separate fix plan is created.

</rollback_notes>

<success_criteria>

- PT runtime gameplay lifecycle is exercised in Playwright, not only attribute presence after navigation.
- Insufficient-credit block behavior is covered by browser-level assertions.
- Localized runtime messaging is directly asserted in both EN and PT runtime flows.
- Canonical routing and existing gameplay/economy behavior remain unchanged.

</success_criteria>

<output>
After completion, create .planning/phases/17-slots-runtime-coverage-hardening/17-01-SUMMARY.md
</output>
