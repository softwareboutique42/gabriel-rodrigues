---
phase: 01-codebase-stabilization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/scripts/canvas/animations/particles.ts
  - src/scripts/canvas/animations/base.ts
autonomous: true
requirements:
  - FR-1.1
must_haves:
  truths:
    - Particle positions are computed from normalized loop progress and return to start state each 12-second cycle.
    - No per-frame velocity accumulation contributes to particle drift.
  artifacts:
    - path: src/scripts/canvas/animations/particles.ts
      provides: Deterministic particle update formulas driven by progress.
    - path: src/scripts/canvas/animations/base.ts
      provides: Stable loop helper usage for particle math.
  key_links:
    - from: src/scripts/canvas/animations/particles.ts
      to: src/scripts/canvas/animations/base.ts
      via: loopProgress or loopTime driven calculations
      pattern: loopProgress|loopTime
---

<objective>
Harden particle-loop correctness so the style is mathematically deterministic and seam-safe.

Purpose: Remove the primary regression source (unbounded accumulated motion) before wider seam validation.
Output: Deterministic particle update path aligned with FR-1.1 and Decisions D-01 through D-03.
</objective>

<execution_context>
@/home/gabriel/Documents/gabriel-rodrigues/.claude/get-shit-done/workflows/execute-plan.md
@/home/gabriel/Documents/gabriel-rodrigues/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-codebase-stabilization/01-CONTEXT.md
@.planning/phases/01-codebase-stabilization/01-RESEARCH.md
@src/scripts/canvas/animations/base.ts
@src/scripts/canvas/animations/particles.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Refactor particle motion to pure progress-based formulas</name>
  <files>src/scripts/canvas/animations/particles.ts</files>
  <read_first>
    - .planning/REQUIREMENTS.md
    - .planning/phases/01-codebase-stabilization/01-CONTEXT.md
    - src/scripts/canvas/animations/particles.ts
  </read_first>
  <behavior>
    - Given elapsed values that span wrap-around, each particle returns to deterministic position at progress 0 and progress 1.
    - Particle motion no longer includes unbounded velocity accumulation terms.
  </behavior>
  <action>Implement FR-1.1 by replacing velocity accumulation with closed-form sin/cos offsets derived from normalized loop progress; preserve setup-time randomness only (per D-03), and ensure update logic is deterministic at seam boundaries (per D-01 and D-02).</action>
  <verify>
    <automated>npm run lint</automated>
  </verify>
  <acceptance_criteria>Particle update path uses progress-based formulas only; no `* t * 60` style accumulation remains in active motion logic.</acceptance_criteria>
  <done>FR-1.1 behavior is implemented with deterministic seam-safe motion.</done>
</task>

<task type="auto">
  <name>Task 2: Align particle loop math with base timing helpers</name>
  <files>src/scripts/canvas/animations/particles.ts, src/scripts/canvas/animations/base.ts</files>
  <read_first>
    - src/scripts/canvas/animations/base.ts
    - src/scripts/canvas/animations/particles.ts
    - .planning/phases/01-codebase-stabilization/01-RESEARCH.md
  </read_first>
  <action>Ensure particle update logic consistently consumes BaseAnimation loop helpers (`loopTime`/`loopProgress`) and remove any raw elapsed usage that can violate deterministic wrap continuity, honoring D-01 and D-02 seam criteria.</action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>The file compiles and uses normalized loop-time primitives for seam-critical math without introducing non-deterministic time paths.</acceptance_criteria>
  <done>Particles are fully anchored to loop helpers and ready for phase-wide seam pass.</done>
</task>

</tasks>

<verification>
Run lint/build and confirm particle-loop calculations are deterministic by inspection against FR-1.1 constraints.
</verification>

<success_criteria>

- FR-1.1 is satisfied in implementation.
- Decisions D-01 through D-03 are reflected in particle-loop math.
- No Phase 2+ behavior is introduced.
  </success_criteria>

<output>
After completion, create `.planning/phases/01-codebase-stabilization/01-01-SUMMARY.md`
</output>
