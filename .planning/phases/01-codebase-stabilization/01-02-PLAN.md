---
phase: 01-codebase-stabilization
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/scripts/canvas/animations/geometric.ts
autonomous: true
requirements:
  - FR-1.2
must_haves:
  truths:
    - Geometric style reuses shared materials by palette index.
    - Default scene renders at three or fewer draw calls for color buckets.
    - Rotation and transforms remain seam-safe over the 12-second loop.
  artifacts:
    - path: src/scripts/canvas/animations/geometric.ts
      provides: Material pooling plus deterministic update behavior.
  key_links:
    - from: src/scripts/canvas/animations/geometric.ts
      to: THREE.MeshBasicMaterial pool
      via: color-index keyed assignment
      pattern: materialPool|palette|MeshBasicMaterial
---

<objective>
Remove per-shape material churn in geometric animation and enforce deterministic loop-safe transforms.

Purpose: Meet FR-1.2 efficiency requirements while preventing seam regressions from incremental transforms.
Output: Shared material pool implementation with seam-safe geometric updates.
</objective>

<execution_context>
@/home/gabriel/Documents/gabriel-rodrigues/.claude/get-shit-done/workflows/execute-plan.md
@/home/gabriel/Documents/gabriel-rodrigues/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-codebase-stabilization/01-CONTEXT.md
@.planning/phases/01-codebase-stabilization/01-RESEARCH.md
@src/scripts/canvas/animations/geometric.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Introduce shared material pool per palette index</name>
  <files>src/scripts/canvas/animations/geometric.ts</files>
  <read_first>
    - .planning/REQUIREMENTS.md
    - .planning/phases/01-codebase-stabilization/01-RESEARCH.md
    - src/scripts/canvas/animations/geometric.ts
  </read_first>
  <behavior>
    - Geometry creation uses one reusable material per palette color bucket.
    - New shape instances do not allocate one material per mesh.
  </behavior>
  <action>Implement FR-1.2 by creating a color-indexed `MeshBasicMaterial` pool and assigning materials from the pool during shape creation; avoid per-shape material allocation and preserve current visual intent within pooled-material constraints.</action>
  <verify>
    <automated>npm run lint</automated>
  </verify>
  <acceptance_criteria>Geometric shape construction no longer creates a fresh material for every mesh; shared pool logic is explicit in code.</acceptance_criteria>
  <done>Material reuse architecture is in place for draw-call reduction.</done>
</task>

<task type="auto">
  <name>Task 2: Convert seam-sensitive geometric transforms to deterministic formulas</name>
  <files>src/scripts/canvas/animations/geometric.ts</files>
  <read_first>
    - .planning/phases/01-codebase-stabilization/01-CONTEXT.md
    - src/scripts/canvas/animations/geometric.ts
  </read_first>
  <action>Refactor any incremental `+=` style rotation/transform updates into closed-form progress-driven expressions to satisfy D-01, D-02, and D-03 while keeping FR-1.2 performance goals intact.</action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>Geometric update path avoids incremental accumulation for seam-critical transforms and remains build-clean.</acceptance_criteria>
  <done>Geometric animation is both pooled-material efficient and seam-safe.</done>
</task>

</tasks>

<verification>
Confirm lint/build pass and verify code-level evidence of shared material pooling and deterministic geometric transforms.
</verification>

<success_criteria>

- FR-1.2 is satisfied with pooled material architecture.
- D-01 through D-03 are preserved in geometric update logic.
- No feature work outside Phase 1 scope is added.
  </success_criteria>

<output>
After completion, create `.planning/phases/01-codebase-stabilization/01-02-SUMMARY.md`
</output>
