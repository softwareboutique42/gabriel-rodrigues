---
phase: 02-animation-quality
plan: 03
type: execute
wave: 3
depends_on:
  - 02-01
  - 02-02
files_modified:
  - src/scripts/canvas/animations/particles.ts
  - src/scripts/canvas/animations/geometric.ts
autonomous: true
requirements:
  - FR-2.3
  - FR-2.5
  - FR-2.7
  - NFR-9
must_haves:
  truths:
    - ParticlesAnimation shows layered foreground and background motion through z, speed, scale, opacity, and brightness differences.
    - GeometricAnimation shows layered depth and mood-driven motion without breaking Phase 1 seam or material-pool guarantees.
    - Both styles switch to the shared normal-blending profile with opacity 0.7 on light backgrounds.
    - Low-concurrency devices cap particle counts at 400 while preserving deterministic loop behavior.
  artifacts:
    - path: src/scripts/canvas/animations/particles.ts
      provides: Depth-layered particle groups driven by shared mood, render, and particle-budget helpers.
    - path: src/scripts/canvas/animations/geometric.ts
      provides: Depth-layered geometric groups that preserve pooled materials and seam-safe progress math.
  key_links:
    - from: src/scripts/canvas/animations/particles.ts
      to: src/scripts/canvas/quality-profiles.ts
      via: mood preset, render profile, and particle budget helpers
      pattern: getMoodPreset|getRenderProfile|getParticleBudget
    - from: src/scripts/canvas/animations/geometric.ts
      to: src/scripts/canvas/animations/base.ts
      via: seam-safe loop helpers plus shared quality accessors
      pattern: loopProgress|loopTime|getMoodPreset|getRenderProfile
---

<objective>
Raise the premium-motion quality of the abstract styles without undoing the deterministic fixes from Phase 1.

Purpose: FR-2.7 and the mobile/render rules have the highest impact in `ParticlesAnimation` and `GeometricAnimation`, and both files already carry Phase 1 seam-sensitive logic that must be preserved explicitly.
Output: Depth-layered abstract styles that respect mood presets, light-background blending, and mobile particle budgets.
</objective>

<execution_context>
@/home/gabriel/Documents/gabriel-rodrigues/.claude/get-shit-done/workflows/execute-plan.md
@/home/gabriel/Documents/gabriel-rodrigues/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-animation-quality/02-RESEARCH.md
@.planning/phases/01-codebase-stabilization/01-01-SUMMARY.md
@.planning/phases/01-codebase-stabilization/01-02-SUMMARY.md
@.planning/phases/01-codebase-stabilization/01-03-SUMMARY.md
@src/scripts/canvas/animations/base.ts
@src/scripts/canvas/animations/particles.ts
@src/scripts/canvas/animations/geometric.ts

<interfaces>
From src/scripts/canvas/animations/particles.ts:
```ts
export class ParticlesAnimation extends BaseAnimation {
  private basePositions: Float32Array;
  private phaseOffsets: Float32Array;
  private frequencySeeds: Float32Array;
  private amplitudeSeeds: Float32Array;
}
```

From src/scripts/canvas/animations/geometric.ts:

```ts
type GeometricShapeData = {
  baseX: number;
  baseY: number;
  baseRotation: number;
  orbitRadius: number;
  orbitPhase: number;
  rotPhase: number;
  rotAmplitude: number;
  scale: number;
};
```

Phase 1 constraints to preserve exactly:

- Particles derive motion from loop progress with no unbounded accumulation.
- Geometric shapes keep one shared material per palette bucket.
- Seam-critical transforms remain closed-form functions of loop progress.
  </interfaces>
  </context>

<tasks>

<task type="auto">
  <name>Task 1: Add depth layering, mobile budget enforcement, and render-profile switching to ParticlesAnimation</name>
  <files>src/scripts/canvas/animations/particles.ts</files>
  <read_first>
    - .planning/REQUIREMENTS.md
    - .planning/phases/02-animation-quality/02-RESEARCH.md
    - .planning/phases/01-codebase-stabilization/01-01-SUMMARY.md
    - src/scripts/canvas/animations/particles.ts
    - src/scripts/canvas/animations/base.ts
  </read_first>
  <action>Implement FR-2.7 and NFR-9 in `ParticlesAnimation` by splitting particles into at least foreground and background cohorts, with nearer particles moving faster and brighter while farther particles move slower and dimmer. Drive counts through the shared particle-budget helper, derive material blending and opacity from the shared render profile, and tune amplitude or staggering from the shared mood preset. Preserve the Phase 1 closed-form loop formulas and setup-time randomness only; do not reintroduce elapsed-time accumulation or mutable drift.</action>
  <verify>
    <automated>npm run lint</automated>
  </verify>
  <acceptance_criteria>ParticlesAnimation visibly encodes depth, obeys the mobile cap, and still derives motion from seam-safe loop math only.</acceptance_criteria>
  <done>ParticlesAnimation meets the Phase 2 depth and render-budget bar without regressing Phase 1 correctness.</done>
</task>

<task type="auto">
  <name>Task 2: Add mood-driven depth and light-background render behavior to GeometricAnimation</name>
  <files>src/scripts/canvas/animations/geometric.ts</files>
  <read_first>
    - .planning/REQUIREMENTS.md
    - .planning/phases/02-animation-quality/02-RESEARCH.md
    - .planning/phases/01-codebase-stabilization/01-02-SUMMARY.md
    - src/scripts/canvas/animations/geometric.ts
    - src/scripts/canvas/animations/base.ts
  </read_first>
  <action>Implement FR-2.3, FR-2.5, and FR-2.7 in `GeometricAnimation` by introducing foreground and background depth layers through z placement, scale contrast, opacity contrast, and orbit-speed variance while keeping one shared material per palette index. Use the shared mood preset to vary entry and pulse character, and use the shared render profile so light backgrounds switch away from additive blending automatically. Maintain the Phase 1 deterministic transform formulas and do not reintroduce per-shape material allocation.</action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>GeometricAnimation uses shared quality rules, preserves material pooling, and remains seam-safe over the full 12-second loop.</acceptance_criteria>
  <done>Both abstract styles are premium-quality, depth-layered, and aligned with Phase 2 motion/render rules.</done>
</task>

</tasks>

<verification>
Run lint and build, then inspect the two animation files for evidence of depth cohorts, shared helper usage, preserved deterministic math, and unchanged pooled-material architecture.
</verification>

<success_criteria>

- FR-2.7 is implemented in both abstract styles through real layered-depth behavior.
- FR-2.5 and NFR-9 are implemented in both abstract styles through shared helpers.
- Phase 1 seam and pooling guarantees remain intact.
  </success_criteria>

<output>
After completion, create `.planning/phases/02-animation-quality/02-03-SUMMARY.md`
</output>
