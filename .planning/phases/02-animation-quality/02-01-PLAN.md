---
phase: 02-animation-quality
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - workers/company-api/src/index.ts
  - workers/company-api/src/normalize.ts
  - src/scripts/canvas/types.ts
  - src/scripts/canvas/main.ts
  - src/scripts/canvas/versions.ts
  - src/scripts/canvas/style-selector.ts
  - src/scripts/canvas/config-normalization.ts
  - tests/animation-quality-contract.test.mjs
autonomous: true
requirements:
  - FR-2.1
  - FR-2.2
  - FR-2.6
must_haves:
  truths:
    - Worker responses include structured mood, industryCategory, and energyLevel fields for every generated company config.
    - The client derives animationStyle deterministically from version plus industryCategory plus mood and does not trust Claude for final style choice.
    - visualElements longer than 12 characters are truncated before animation setup, checkout, or export handoff.
    - Generate and fetch-by-slug paths both use the same version-aware cache key behavior.
  artifacts:
    - path: workers/company-api/src/index.ts
      provides: Semantic worker prompt plus sanitized config generation and version-aware cache writes.
    - path: src/scripts/canvas/style-selector.ts
      provides: Pure deterministic mapping from version plus industryCategory plus mood to animationStyle.
    - path: src/scripts/canvas/config-normalization.ts
      provides: Boundary normalization for mood, industryCategory, energyLevel, animationStyle, and visualElements.
    - path: tests/animation-quality-contract.test.mjs
      provides: Regression coverage for deterministic selection and normalization rules.
  key_links:
    - from: workers/company-api/src/index.ts
      to: workers/company-api/src/normalize.ts
      via: version-aware cacheKey(name, version) writes and reads
      pattern: cacheKey\(.*version
    - from: src/scripts/canvas/main.ts
      to: src/scripts/canvas/config-normalization.ts
      via: fetched config normalized before currentConfig, renderer startup, and checkout/export usage
      pattern: normalizeCompanyConfig
    - from: src/scripts/canvas/config-normalization.ts
      to: src/scripts/canvas/style-selector.ts
      via: deterministic style overwrite during normalization
      pattern: selectAnimationStyle
---

<objective>
Lock the semantic config contract and deterministic style-routing boundary for Phase 2.

Purpose: FR-2.1, FR-2.2, and FR-2.6 are the foundation for every later motion-quality change; without them, animation polish is built on nondeterministic style selection and untrusted worker payloads.
Output: Version-aware worker schema, typed semantic fields, a pure client-side style selector, and one normalization boundary that downstream plans can rely on.
</objective>

<execution_context>
@/home/gabriel/Documents/gabriel-rodrigues/.claude/get-shit-done/workflows/execute-plan.md
@/home/gabriel/Documents/gabriel-rodrigues/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-animation-quality/02-RESEARCH.md
@.planning/phases/01-codebase-stabilization/01-03-SUMMARY.md
@workers/company-api/src/index.ts
@workers/company-api/src/normalize.ts
@src/scripts/canvas/types.ts
@src/scripts/canvas/main.ts
@src/scripts/canvas/versions.ts

<interfaces>
From src/scripts/canvas/types.ts:
```ts
export interface CompanyConfig {
  companyName: string;
  colors: { primary: string; secondary: string; accent: string; background: string };
  tagline: string;
  industry: string;
  description: string;
  animationStyle: 'particles' | 'flowing' | 'geometric' | 'typographic' | 'narrative' | 'timeline' | 'constellation' | 'spotlight';
  animationParams: { speed: number; density: number; complexity: number };
  visualElements: string[];
  version?: string;
}
```

From src/scripts/canvas/versions.ts:

```ts
export const VERSIONS = [
  { id: 'v1', styles: ['particles', 'flowing', 'geometric', 'typographic'] },
  { id: 'v2', styles: ['narrative', 'timeline', 'constellation', 'spotlight'] },
];
```

From workers/company-api/src/normalize.ts:

```ts
export function cacheKey(name: string, version: string = 'v1'): string {
  return `${version}:${normalizeCompanyName(name)}`;
}
```

Deterministic routing matrix to encode directly in the selector:

- v1: tech -> particles, finance -> geometric, health -> flowing, retail -> typographic, creative -> typographic, food -> flowing, other -> particles
- v2: tech -> constellation, finance -> timeline, health -> timeline, retail -> narrative, creative -> spotlight, food -> narrative, other -> narrative
- Mood resolves ties within the allowed version family only; energyLevel tunes motion intensity and MUST NOT change style identity.
  </interfaces>
  </context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Expand the semantic worker contract and add one normalization boundary</name>
  <files>workers/company-api/src/index.ts, workers/company-api/src/normalize.ts, src/scripts/canvas/types.ts, src/scripts/canvas/config-normalization.ts, tests/animation-quality-contract.test.mjs</files>
  <read_first>
    - .planning/REQUIREMENTS.md
    - .planning/phases/02-animation-quality/02-RESEARCH.md
    - workers/company-api/src/index.ts
    - workers/company-api/src/normalize.ts
    - src/scripts/canvas/types.ts
  </read_first>
  <behavior>
    - Missing or invalid mood values normalize to a safe fallback rather than breaking render startup.
    - Missing or invalid industryCategory values normalize to a safe fallback rather than changing version semantics.
    - energyLevel is clamped into the inclusive 0-1 range.
    - Each visualElement is trimmed and truncated to a maximum of 12 characters before any animation consumes it.
    - POST generate and GET config both cache against the same version-aware key format.
  </behavior>
  <action>Implement FR-2.1 and FR-2.6 by extending CompanyConfig with typed semantic fields, updating the worker prompt to request `mood`, `industryCategory`, and `energyLevel`, sanitizing those values before caching, and creating a single `normalizeCompanyConfig()` helper on the client boundary. That helper must clamp energy, coerce invalid enums to safe fallbacks, truncate `visualElements` to 12 characters, preserve version, and carry forward a worker-supplied `animationStyle` only as a transitional field that Task 2 will overwrite. Also align `handleGenerate()` cache writes with `cacheKey(companyName, version)` so POST and GET obey the same versioned identity.</action>
  <verify>
    <automated>node --test tests/animation-quality-contract.test.mjs</automated>
  </verify>
  <acceptance_criteria>Typed semantic fields exist end to end, invalid external values are recoverable, and long visual-element labels cannot leak past normalization or version-aware cache boundaries.</acceptance_criteria>
  <done>Semantic config generation and client normalization are stable enough for deterministic routing and later animation-quality work.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Replace worker-selected animationStyle with a deterministic client selector</name>
  <files>src/scripts/canvas/style-selector.ts, src/scripts/canvas/main.ts, src/scripts/canvas/versions.ts, tests/animation-quality-contract.test.mjs</files>
  <read_first>
    - .planning/REQUIREMENTS.md
    - .planning/phases/02-animation-quality/02-RESEARCH.md
    - src/scripts/canvas/main.ts
    - src/scripts/canvas/versions.ts
    - src/scripts/canvas/types.ts
  </read_first>
  <behavior>
    - The same version plus industryCategory plus mood always resolves to the same animationStyle.
    - energyLevel changes motion intensity only and never changes the selected style.
    - generate, slug-fetch, and download-return config paths all normalize payloads before updating currentConfig or starting the renderer.
  </behavior>
  <action>Implement FR-2.2 by creating a pure `selectAnimationStyle({ version, industryCategory, mood })` function using the routing matrix in research and wiring `main.ts` so every incoming config is normalized immediately after fetch, then has `animationStyle` overwritten from the deterministic selector before `currentConfig`, `renderInfo()`, `startRenderer()`, checkout, and export paths consume it. Preserve the existing version dropdown contract from `versions.ts`, and do not add randomness, Claude tie-breakers, or energy-based style switching.</action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>The client has one authoritative style-selection function and no live rendering path trusts Claude as the final style chooser.</acceptance_criteria>
  <done>Phase 2 has a deterministic routing layer that all downstream plans can target without ambiguity.</done>
</task>

</tasks>

<verification>
Run the contract tests plus a production build. Confirm the worker generates semantic fields, normalization truncates visualElements to 12 characters, and the client always overwrites animationStyle through the deterministic selector.
</verification>

<success_criteria>

- FR-2.1 is satisfied with typed semantic fields and sane fallbacks.
- FR-2.2 is satisfied with one pure deterministic selector on the client.
- FR-2.6 is satisfied at the normalization boundary rather than ad hoc in individual animations.
  </success_criteria>

<output>
After completion, create `.planning/phases/02-animation-quality/02-01-SUMMARY.md`
</output>
