---
phase: 02-animation-quality
plan: 04
type: execute
wave: 3
depends_on:
  - 02-01
  - 02-02
files_modified:
  - src/scripts/canvas/animations/text-utils.ts
  - src/scripts/canvas/animations/narrative.ts
  - src/scripts/canvas/animations/spotlight.ts
  - e2e/canvas.spec.ts
autonomous: false
requirements:
  - FR-2.3
  - FR-2.4
  - FR-2.5
must_haves:
  truths:
    - NarrativeAnimation and SpotlightAnimation treat the company name as the largest in-scene typographic element.
    - Both text-driven styles reveal the company name with per-character stagger while supporting visualElements stay secondary.
    - Both text-driven styles consume shared mood presets and light-background render rules instead of independent timing constants.
  artifacts:
    - path: src/scripts/canvas/animations/text-utils.ts
      provides: Reusable text-sprite helpers for company-name character reveals and supporting labels.
    - path: src/scripts/canvas/animations/narrative.ts
      provides: Name-first narrative composition with mood-driven staggered reveal.
    - path: src/scripts/canvas/animations/spotlight.ts
      provides: Name-first spotlight composition with mood-driven staggered reveal.
  key_links:
    - from: src/scripts/canvas/animations/narrative.ts
      to: src/scripts/canvas/animations/text-utils.ts
      via: per-character company-name sprite generation and stagger scheduling
      pattern: create.*Character|stagger
    - from: src/scripts/canvas/animations/spotlight.ts
      to: src/scripts/canvas/quality-profiles.ts
      via: mood preset and render profile helpers
      pattern: getMoodPreset|getRenderProfile
    - from: e2e/canvas.spec.ts
      to: src/scripts/canvas/main.ts
      via: canvas generation smoke around deterministic result state and visible overlay text
      pattern: version-select|canvas-form|canvas-result|overlay-name
---

<objective>
Make the story-driven styles visibly premium and ready for final phase review.

Purpose: Narrative and Spotlight carry the strict per-character reveal requirement, so this plan focuses on making those two styles undeniably name-first before the final cross-style audit plan runs.
Output: Name-first text animations in Narrative and Spotlight plus one canvas smoke test update.
</objective>

<execution_context>
@/home/gabriel/Documents/gabriel-rodrigues/.claude/get-shit-done/workflows/execute-plan.md
@/home/gabriel/Documents/gabriel-rodrigues/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-animation-quality/02-RESEARCH.md
@.planning/phases/01-codebase-stabilization/01-03-SUMMARY.md
@src/scripts/canvas/animations/text-utils.ts
@src/scripts/canvas/animations/narrative.ts
@src/scripts/canvas/animations/spotlight.ts
@e2e/canvas.spec.ts

<interfaces>
From src/scripts/canvas/animations/text-utils.ts:
```ts
export function createTextSprite(text: string, color: string, fontSize = 64): THREE.Sprite;
```

Current story-style constraint to change:

```ts
const elements = this.config.visualElements.slice(0, 5);
elements.forEach((word, i) => {
  const sprite = createTextSprite(word.toUpperCase(), colors[i % colors.length], 48);
});
```

Implementation target for this plan:

- Company name is the primary text treatment in-scene.
- visualElements become secondary labels, captions, or satellites only.
- Narrative and Spotlight both use per-character stagger on the company name.
- Mood presets shape reveal cadence, hold time, and pulse character.
  </interfaces>
  </context>

<tasks>

<task type="auto">
  <name>Task 1: Recompose Narrative and Spotlight around a staggered company-name reveal</name>
  <files>src/scripts/canvas/animations/text-utils.ts, src/scripts/canvas/animations/narrative.ts, src/scripts/canvas/animations/spotlight.ts</files>
  <read_first>
    - .planning/REQUIREMENTS.md
    - .planning/phases/02-animation-quality/02-RESEARCH.md
    - src/scripts/canvas/animations/text-utils.ts
    - src/scripts/canvas/animations/narrative.ts
    - src/scripts/canvas/animations/spotlight.ts
  </read_first>
  <action>Implement FR-2.4 by expanding `text-utils.ts` with reusable helpers for character-level sprite creation or grouped letter sequencing, then refactor `NarrativeAnimation` and `SpotlightAnimation` so the company name is the dominant in-scene text element with a per-character staggered reveal. Keep `visualElements` as secondary captions or satellites only, consume the shared mood preset to tune stagger cadence and hold timing, and apply the shared render profile so light backgrounds stop washing out the text-driven scenes.</action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>The company name is the largest scene-level text treatment in both styles and each style reveals it through visible per-character staggering rather than one monolithic fade.</acceptance_criteria>
  <done>Narrative and Spotlight meet the name-first typography requirement and are ready for mood-comparison sign-off.</done>
</task>

<task type="auto">
  <name>Task 2: Add a canvas smoke test for deterministic result rendering after semantic normalization</name>
  <files>e2e/canvas.spec.ts</files>
  <read_first>
    - e2e/canvas.spec.ts
    - src/scripts/canvas/main.ts
    - src/scripts/canvas/types.ts
  </read_first>
  <action>Extend `e2e/canvas.spec.ts` with one focused smoke test that mocks a semantic worker response containing `mood`, `industryCategory`, `energyLevel`, and overlong `visualElements`, then verifies the canvas page reaches result state with visible overlay/company text and deterministic style info after normalization. Keep the test narrow and deterministic; do not attempt visual-frame assertions in Playwright.</action>
  <verify>
    <automated>npx playwright test e2e/canvas.spec.ts</automated>
  </verify>
  <acceptance_criteria>The canvas smoke suite covers the normalized semantic payload path and guards against regressions where result rendering fails after Phase 2 contract changes.</acceptance_criteria>
  <done>Phase 2 text-driven work has an automated browser smoke guard in addition to the manual premium review.</done>
</task>

</tasks>

<verification>
Run build and the focused Playwright canvas smoke, then hand off to the final closeout plan for whole-phase review.
</verification>

<success_criteria>

- FR-2.4 is implemented concretely in Narrative and Spotlight.
- FR-2.5 is implemented in the text-driven styles through shared render rules.
- The text-driven styles are ready for the final phase-wide premium checklist executed after abstract and remaining styles land.
  </success_criteria>

<output>
After completion, create `.planning/phases/02-animation-quality/02-04-SUMMARY.md`
</output>
