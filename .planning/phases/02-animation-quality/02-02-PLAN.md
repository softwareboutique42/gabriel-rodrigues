---
phase: 02-animation-quality
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - src/scripts/canvas/quality-profiles.ts
  - src/scripts/canvas/animations/base.ts
  - src/scripts/canvas/export.ts
  - tests/quality-profiles.test.mjs
autonomous: true
requirements:
  - FR-2.3
  - FR-2.5
  - NFR-9
must_haves:
  truths:
    - Every mood resolves to a named motion preset with distinct timing behavior and reusable parameters.
    - Light backgrounds resolve to normal blending with opacity 0.7 before animation files decide how to build materials.
    - Particle budgets cap at 400 on low-concurrency devices through one shared helper instead of per-file guesses.
    - Export HTML generation uses the same quality-profile constants as live preview so Phase 2 rules do not drift immediately after checkout.
  artifacts:
    - path: src/scripts/canvas/quality-profiles.ts
      provides: Shared mood presets, render-profile detection, and particle-budget logic.
    - path: src/scripts/canvas/animations/base.ts
      provides: Common accessors so animation classes consume quality rules consistently.
    - path: src/scripts/canvas/export.ts
      provides: Export HTML generation seeded from shared Phase 2 quality rules instead of divergent hard-coded constants.
    - path: tests/quality-profiles.test.mjs
      provides: Regression checks for mood presets, light-background render switching, and mobile particle budget caps.
  key_links:
    - from: src/scripts/canvas/animations/base.ts
      to: src/scripts/canvas/quality-profiles.ts
      via: helper methods consumed by animation classes
      pattern: getMoodPreset|getRenderProfile|getParticleBudget
    - from: src/scripts/canvas/export.ts
      to: src/scripts/canvas/quality-profiles.ts
      via: embedded or imported Phase 2 preset and render-profile data
      pattern: qualityProfiles|renderProfile|particleBudget
---

<objective>
Create the shared motion and render-rule contracts that keep Phase 2 quality work consistent across runtime and export paths.

Purpose: FR-2.3, FR-2.5, and NFR-9 are cross-cutting rules; if they are implemented file by file, later plans will drift immediately and executor context will be wasted rediscovering constants.
Output: One quality-profile module, one base-animation access pattern, and one export-parity handoff for Phase 2 rules.
</objective>

<execution_context>
@/home/gabriel/Documents/gabriel-rodrigues/.claude/get-shit-done/workflows/execute-plan.md
@/home/gabriel/Documents/gabriel-rodrigues/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-animation-quality/02-RESEARCH.md
@.planning/phases/01-codebase-stabilization/01-03-SUMMARY.md
@src/scripts/canvas/animations/base.ts
@src/scripts/canvas/export.ts

<interfaces>
From src/scripts/canvas/animations/base.ts:
```ts
export const LOOP_DURATION = 12;

export abstract class BaseAnimation {
protected loopTime(elapsed: number): number;
protected loopProgress(elapsed: number): number;
}

````

Phase 2 quality helpers to define here and reuse everywhere:
```ts
type MoodPreset = {
  entryEasing: string;
  holdRatio: number;
  exitEasing: string;
  overshoot: number;
  intensityScale: number;
  staggerStep: number;
};

type RenderProfile = {
  isLightBackground: boolean;
  blending: 'additive' | 'normal';
  opacity: number;
};
````

Rules to encode exactly from requirements:

- bold -> fast ease-in, hard stop
- elegant -> slow ease-in-out cubic
- playful -> spring or overshoot
- minimal -> subtle linear fade
- dynamic -> fast in, slow out
- background lightness > 50 percent -> NormalBlending with opacity 0.7
- navigator.hardwareConcurrency < 4 -> particle count cap 400
  </interfaces>
  </context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create the shared quality-profile module and regression tests</name>
  <files>src/scripts/canvas/quality-profiles.ts, tests/quality-profiles.test.mjs</files>
  <read_first>
    - .planning/REQUIREMENTS.md
    - .planning/phases/02-animation-quality/02-RESEARCH.md
    - src/scripts/canvas/types.ts
  </read_first>
  <behavior>
    - Each of the five moods resolves to a distinct preset object with different timing characteristics.
    - Background lightness above 50 percent resolves to normal blending and opacity 0.7.
    - Particle counts are capped at 400 whenever hardwareConcurrency is less than 4, without modifying desktop budgets.
  </behavior>
  <action>Create `src/scripts/canvas/quality-profiles.ts` as the single source of truth for Phase 2 cross-cutting rules. Export pure helpers for mood presets, background render profiles, and particle budgets so they can be tested without a Three.js scene. Encode the five required mood behaviors explicitly, compute light-background switching from background hex lightness, and implement the NFR-9 mobile cap exactly as `navigator.hardwareConcurrency < 4 -> min(baseCount, 400)` with a server-safe fallback path for non-browser contexts.</action>
  <verify>
    <automated>node --test tests/quality-profiles.test.mjs</automated>
  </verify>
  <acceptance_criteria>The quality-profile module can be validated in isolation and contains the only authoritative copies of mood, blending, and particle-budget rules.</acceptance_criteria>
  <done>Downstream animation plans have one concrete contract for Phase 2 quality behavior.</done>
</task>

<task type="auto">
  <name>Task 2: Expose shared quality helpers to runtime and export paths</name>
  <files>src/scripts/canvas/quality-profiles.ts, src/scripts/canvas/animations/base.ts, src/scripts/canvas/export.ts</files>
  <read_first>
    - src/scripts/canvas/animations/base.ts
    - src/scripts/canvas/export.ts
    - src/scripts/canvas/quality-profiles.ts
    - .planning/phases/02-animation-quality/02-RESEARCH.md
  </read_first>
  <action>Update `BaseAnimation` to expose convenience accessors for mood preset, render profile, and particle budget so later animation plans can consume shared rules without re-implementing them. Then update `src/scripts/canvas/export.ts` so the generated HTML path is seeded from the same serializable Phase 2 quality-profile data instead of frozen, hand-written additive blending and particle-count constants. Do not add a second copy of mood or blending logic anywhere else after this plan.</action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>Runtime and export code have a common entry point for Phase 2 quality rules, and downstream animation plans can call helpers instead of recreating constants.</acceptance_criteria>
  <done>Shared quality contracts are wired and ready for style-specific implementation work.</done>
</task>

</tasks>

<verification>
Run the isolated quality-profile tests and a production build. Confirm `BaseAnimation` exposes reusable helper accessors and `export.ts` no longer hard-codes divergent Phase 2 blending or particle-budget rules.
</verification>

<success_criteria>

- FR-2.3 has one explicit preset contract for all five moods.
- FR-2.5 has one explicit render-profile contract for light backgrounds.
- NFR-9 has one explicit particle-budget helper used by runtime and export paths.
  </success_criteria>

<output>
After completion, create `.planning/phases/02-animation-quality/02-02-SUMMARY.md`
</output>
