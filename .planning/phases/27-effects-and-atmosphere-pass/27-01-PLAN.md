---
phase: 27-effects-and-atmosphere-pass
plan: 01
type: execute
wave: 1
depends_on:
  - 26-01
files_modified:
  - src/scripts/slots/animation/runtime.ts
  - src/scripts/slots/animation/reel-timeline.ts
  - src/scripts/slots/animation/outcome-feedback.ts
  - src/pages/en/slots/index.astro
  - src/pages/pt/slots/index.astro
  - src/styles/global.css
  - tests/slots-animation-event-sequencing-contract.test.mjs
  - tests/slots-motion-accessibility-contract.test.mjs
  - tests/slots-theme-variants-contract.test.mjs
  - e2e/compatibility.spec.ts
  - .planning/phases/27-effects-and-atmosphere-pass/27-01-SUMMARY.md
autonomous: true
requirements:
  - FX-20
  - FX-21
  - FX-22
must_haves:
  truths:
    - Spin, stop, win, blocked, and idle states gain richer presentation feedback without changing gameplay authority or event sequencing.
    - Reduced-motion and performance guardrails deterministically constrain the richer effect layer across canonical EN/PT Slots routes.
    - At least one coherent theme atmosphere pass feels materially different while remaining presentation-only and parity-safe.
  artifacts:
    - path: src/scripts/slots/animation/runtime.ts
      provides: deterministic projection of richer effect and atmosphere state into machine-readable runtime hooks.
    - path: src/styles/global.css
      provides: theme-aware atmosphere and feedback styling keyed from runtime state.
    - path: tests/slots-motion-accessibility-contract.test.mjs
      provides: reduced-motion-safe assertions for the expanded feedback layer.
    - path: tests/slots-theme-variants-contract.test.mjs
      provides: deterministic theme atmosphere and fallback assertions.
    - path: e2e/compatibility.spec.ts
      provides: browser-level proof that richer feedback remains parity-safe on canonical EN/PT routes.
  key_links:
    - from: src/scripts/slots/animation/runtime.ts
      to: src/scripts/slots/animation/reel-timeline.ts
      via: timeline phase drives spin-up, sustain, stop, and blocked presentation states
      pattern: idle|spin-up|sustain|stop|blocked
    - from: src/scripts/slots/animation/runtime.ts
      to: src/scripts/slots/animation/outcome-feedback.ts
      via: outcome status drives win/loss presentation reinforcement
      pattern: idle|win|loss
    - from: src/styles/global.css
      to: src/pages/en/slots/index.astro and src/pages/pt/slots/index.astro
      via: atmosphere and effect styling keyed from stable shell zones and runtime datasets
      pattern: data-slots-zone|data-slots-anim-|data-slots-shell
    - from: tests/slots-motion-accessibility-contract.test.mjs
      to: src/scripts/slots/animation/runtime.ts
      via: motion intensity and accessibility-safe effect projection
      pattern: full|reduced|minimal|reduced-motion
validation_commands:
  - npm run lint
  - node --test tests/slots-animation-event-sequencing-contract.test.mjs tests/slots-motion-accessibility-contract.test.mjs tests/slots-theme-variants-contract.test.mjs
  - npx playwright test e2e/compatibility.spec.ts --project=chromium --project=mobile-chrome
  - npm run build
  - npm run lint && node --test tests/slots-animation-event-sequencing-contract.test.mjs tests/slots-motion-accessibility-contract.test.mjs tests/slots-theme-variants-contract.test.mjs && npx playwright test e2e/compatibility.spec.ts --project=chromium --project=mobile-chrome && npm run build
---

<objective>
Add richer motion, feedback effects, and atmosphere to the Slots experience so gameplay states feel more satisfying while preserving deterministic presentation boundaries.

Purpose: satisfy FX-20, FX-21, and FX-22 with runtime-driven, reduced-motion-safe, performance-aware effects.
Output: expanded runtime effect state, theme-aware atmosphere styling, and regression coverage for parity-safe visual feedback.
</objective>

<execution_context>
@/home/gabriel/Documents/gabriel-rodrigues/.claude/get-shit-done/workflows/execute-plan.md
@/home/gabriel/Documents/gabriel-rodrigues/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/27-effects-and-atmosphere-pass/27-CONTEXT.md
@.planning/phases/27-effects-and-atmosphere-pass/27-RESEARCH.md
@src/scripts/slots/animation/runtime.ts
@src/scripts/slots/animation/reel-timeline.ts
@src/scripts/slots/animation/outcome-feedback.ts
@src/scripts/slots/animation/idle-motion.ts
@src/scripts/slots/animation/theme-registry.ts
@src/scripts/slots/animation/theme-selection.ts
@src/pages/en/slots/index.astro
@src/pages/pt/slots/index.astro
@src/styles/global.css
@tests/slots-animation-event-sequencing-contract.test.mjs
@tests/slots-motion-accessibility-contract.test.mjs
@tests/slots-theme-variants-contract.test.mjs
@e2e/compatibility.spec.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add red-first contracts for richer effect-state hooks, theme atmosphere, and reduced-motion-safe feedback</name>
  <files>tests/slots-animation-event-sequencing-contract.test.mjs, tests/slots-motion-accessibility-contract.test.mjs, tests/slots-theme-variants-contract.test.mjs</files>
  <behavior>
    - Test 1: contracts fail if richer effect-state hooks drift from deterministic visual-event sequencing.
    - Test 2: contracts fail if reduced-motion/performance modes do not cap or simplify the expanded feedback layer predictably.
    - Test 3: contracts fail if theme atmosphere selection or fallback stops being deterministic across canonical Slots surfaces.
  </behavior>
  <action>
    Extend the runtime-facing contracts before implementation:
    - Add assertions for any new runtime effect/atmosphere datasets or state markers.
    - Keep motion guardrail coverage explicit for `full`, `reduced`, and `minimal` modes.
    - Preserve the existing principle that all new hooks remain presentation-only and machine-readable.
  </action>
  <verify>
    <automated>node --test tests/slots-animation-event-sequencing-contract.test.mjs tests/slots-motion-accessibility-contract.test.mjs tests/slots-theme-variants-contract.test.mjs</automated>
  </verify>
  <done>Contracts can catch sequencing, theme, and motion-policy regressions before the richer effects layer ships.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Project runtime-driven effects and theme-aware atmosphere without touching gameplay authority</name>
  <files>src/scripts/slots/animation/runtime.ts, src/scripts/slots/animation/reel-timeline.ts, src/scripts/slots/animation/outcome-feedback.ts, src/pages/en/slots/index.astro, src/pages/pt/slots/index.astro, src/styles/global.css</files>
  <behavior>
    - Spin acceptance, sustain, stop, blocked, win, and loss states produce stronger visual reinforcement on the refreshed shell.
    - Theme atmosphere feels materially richer while remaining deterministic and presentation-only.
    - Existing gameplay IDs, route behavior, and authority boundaries remain intact.
  </behavior>
  <action>
    Implement the richer feedback layer through runtime projection and CSS-driven atmosphere:
    - Add deterministic runtime hooks for effect-state and atmosphere-state projection.
    - Bind the shell zones and cabinet surfaces to those hooks through shared CSS rather than ad hoc page logic.
    - Ensure reduced-motion and performance overrides visibly simplify effects without hiding gameplay state.
    - Keep EN/PT page structure mirrored and avoid controller/economy changes.
  </action>
  <verify>
    <automated>node --test tests/slots-animation-event-sequencing-contract.test.mjs tests/slots-motion-accessibility-contract.test.mjs tests/slots-theme-variants-contract.test.mjs</automated>
  </verify>
  <done>Richer effects and atmosphere feel intentional, remain deterministic, and degrade cleanly under existing guardrails.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Extend compatibility checks for parity-safe effects and release evidence</name>
  <files>e2e/compatibility.spec.ts</files>
  <behavior>
    - Test 1: canonical EN/PT Slots routes still expose deterministic runtime envelopes after richer effects land.
    - Test 2: browser checks confirm the new atmosphere/effect layer is visible through stable selectors or datasets.
    - Test 3: validation remains timing-agnostic and avoids screenshot-only assertions.
  </behavior>
  <action>
    Extend the compatibility suite to prove the new effect layer is safe to ship:
    - Add stable checks for runtime effect/atmosphere surfaces in EN/PT.
    - Keep existing gameplay, parity, and canonical route checks intact.
    - Use the full lint + contracts + compatibility + build chain as release evidence for Phase 27.
  </action>
  <verify>
    <automated>npx playwright test e2e/compatibility.spec.ts --project=chromium --project=mobile-chrome</automated>
  </verify>
  <done>Canonical EN/PT Slots flows retain deterministic compatibility guarantees with the richer effects layer enabled.</done>
</task>

</tasks>

<validation_commands>

1. npm run lint
2. node --test tests/slots-animation-event-sequencing-contract.test.mjs tests/slots-motion-accessibility-contract.test.mjs tests/slots-theme-variants-contract.test.mjs
3. npx playwright test e2e/compatibility.spec.ts --project=chromium --project=mobile-chrome
4. npm run build
5. npm run lint && node --test tests/slots-animation-event-sequencing-contract.test.mjs tests/slots-motion-accessibility-contract.test.mjs tests/slots-theme-variants-contract.test.mjs && npx playwright test e2e/compatibility.spec.ts --project=chromium --project=mobile-chrome && npm run build

</validation_commands>

<verification>
FX-20, FX-21, and FX-22 are complete when richer Slots feedback and atmosphere are projected through deterministic runtime hooks, remain reduced-motion/performance safe, and the full lint/test/e2e/build chain passes.
</verification>

<success_criteria>

- Spin, stop, win, blocked, and idle states gain materially stronger presentation feedback on the refreshed shell.
- Atmosphere and theme treatment feel richer while remaining deterministic and parity-safe across EN/PT routes.
- Reduced-motion and performance guardrails still produce a readable, intentional experience under degraded modes.
- Compatibility and contract coverage catch effect-layer regressions without relying on subjective screenshot review.
  </success_criteria>

<output>
After completion, create .planning/phases/27-effects-and-atmosphere-pass/27-01-SUMMARY.md
</output>
