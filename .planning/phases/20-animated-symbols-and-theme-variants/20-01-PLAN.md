---
phase: 20-animated-symbols-and-theme-variants
plan: 01
type: execute
wave: 1
depends_on:
  - 19-01
files_modified:
  - src/scripts/slots/animation/symbol-states.ts
  - src/scripts/slots/animation/theme-registry.ts
  - src/scripts/slots/animation/theme-selection.ts
  - src/scripts/slots/animation/runtime.ts
  - src/scripts/slots/main.ts
  - tests/slots-symbol-states-contract.test.mjs
  - tests/slots-theme-variants-contract.test.mjs
  - tests/slots-animation-event-sequencing-contract.test.mjs
  - e2e/compatibility.spec.ts
  - .planning/phases/20-animated-symbols-and-theme-variants/20-01-SUMMARY.md
autonomous: true
requirements:
  - SPRITE-11
  - SPRITE-12
must_haves:
  truths:
    - Core symbols expose deterministic animated states (`idle`, `spin`, `win-react`) without changing payout/evaluation logic.
    - Theme variant selection remains presentation-only and does not branch gameplay authority behavior.
    - Runtime behavior remains SPA-safe and EN/PT consistent across route transitions.
  artifacts:
    - path: src/scripts/slots/animation/symbol-states.ts
      provides: deterministic visual event and runtime phase to symbol-state projection contract.
    - path: src/scripts/slots/animation/theme-registry.ts
      provides: theme schema and deterministic fallback selection behavior.
    - path: tests/slots-theme-variants-contract.test.mjs
      provides: contract coverage for presentation-only theme selection and fallback guarantees.
  key_links:
    - from: src/scripts/slots/animation/events.ts
      to: src/scripts/slots/animation/symbol-states.ts
      via: event-driven symbol-state transitions
      pattern: spin-accepted|spin-resolved|spin-blocked
    - from: src/scripts/slots/animation/runtime.ts
      to: src/scripts/slots/animation/theme-registry.ts
      via: active theme snapshot and deterministic fallback propagation
      pattern: theme|data-slots-anim-
    - from: e2e/compatibility.spec.ts
      to: data-slots-anim-* hooks
      via: stable EN/PT assertions for symbol-state and active-theme observability
      pattern: data-slots-anim-
validation_commands:
  - npm run lint
  - node --test tests/slots-core-determinism-contract.test.mjs tests/slots-interaction-guards-contract.test.mjs tests/slots-i18n-parity-contract.test.mjs tests/slots-animation-event-sequencing-contract.test.mjs tests/slots-sprite-atlas-contract.test.mjs tests/slots-symbol-states-contract.test.mjs tests/slots-theme-variants-contract.test.mjs
  - npx playwright test e2e/compatibility.spec.ts --project=chromium
  - npm run build
  - npm run lint && node --test tests/slots-core-determinism-contract.test.mjs tests/slots-interaction-guards-contract.test.mjs tests/slots-i18n-parity-contract.test.mjs tests/slots-animation-event-sequencing-contract.test.mjs tests/slots-sprite-atlas-contract.test.mjs tests/slots-symbol-states-contract.test.mjs tests/slots-theme-variants-contract.test.mjs && npx playwright test e2e/compatibility.spec.ts --project=chromium && npm run build
---

<objective>
Implement Phase 20 animated symbol states and theme variants over the existing deterministic Slots runtime while preserving gameplay authority boundaries.

Purpose: satisfy SPRITE-11 and SPRITE-12 with presentation-only enhancements.
Output: symbol-state contracts, theme registry/selection wiring, deterministic tests, and compatibility assertions.
</objective>

<execution_context>
@/home/gabriel/Documents/gabriel-rodrigues/.claude/get-shit-done/workflows/execute-plan.md
@/home/gabriel/Documents/gabriel-rodrigues/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/20-animated-symbols-and-theme-variants/20-CONTEXT.md
@.planning/phases/20-animated-symbols-and-theme-variants/20-RESEARCH.md
@.planning/phases/20-animated-symbols-and-theme-variants/20-VALIDATION.md
@.planning/phases/19-sprite-atlas-integration-and-ui-motion/19-01-SUMMARY.md
@.planning/phases/19-sprite-atlas-integration-and-ui-motion/19-01-VERIFICATION.md
@src/scripts/slots/animation/events.ts
@src/scripts/slots/animation/runtime.ts
@src/scripts/slots/animation/symbol-frame-map.ts
@src/scripts/slots/animation/atlas-registry.ts
@src/scripts/slots/main.ts
@tests/slots-sprite-atlas-contract.test.mjs
@tests/slots-animation-event-sequencing-contract.test.mjs
@tests/slots-i18n-parity-contract.test.mjs
@e2e/compatibility.spec.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add deterministic symbol animation-state contracts</name>
  <read_first>
    - .planning/phases/20-animated-symbols-and-theme-variants/20-CONTEXT.md
    - .planning/phases/20-animated-symbols-and-theme-variants/20-RESEARCH.md
    - src/scripts/slots/animation/events.ts
    - src/scripts/slots/animation/runtime.ts
  </read_first>
  <files>src/scripts/slots/animation/symbol-states.ts, src/scripts/slots/animation/runtime.ts</files>
  <action>
    Implement SPRITE-11 symbol-state foundations:
    - Add deterministic adapter(s) mapping runtime/event phases to symbol states (`idle`, `spin`, `win-react`).
    - Keep state derivation presentation-only and independent from payout/economy/engine writes.
    - Expose stable symbol-state snapshots through runtime observability hooks.
  </action>
  <acceptance_criteria>
    - Symbol state transitions are deterministic for identical visual event sequences.
    - No authoritative gameplay mutation path is introduced in symbol-state modules.
  </acceptance_criteria>
  <verify>
    <automated>npm run lint && node --test tests/slots-core-determinism-contract.test.mjs tests/slots-interaction-guards-contract.test.mjs</automated>
  </verify>
  <done>Symbol animation-state contract exists and is integrated into runtime snapshots.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement presentation-only theme registry and selection wiring</name>
  <read_first>
    - src/scripts/slots/animation/runtime.ts
    - src/scripts/slots/main.ts
    - .planning/phases/20-animated-symbols-and-theme-variants/20-RESEARCH.md
  </read_first>
  <files>src/scripts/slots/animation/theme-registry.ts, src/scripts/slots/animation/theme-selection.ts, src/scripts/slots/animation/runtime.ts, src/scripts/slots/main.ts</files>
  <action>
    Implement SPRITE-12 theme variant foundations:
    - Add a deterministic theme registry with default theme + at least one additional variant.
    - Add selection/fallback wiring that is presentation-only and lifecycle-safe across SPA navigation.
    - Extend runtime hooks with active theme snapshot for deterministic observability.
  </action>
  <acceptance_criteria>
    - Theme selection can switch visual presentation without touching gameplay rules.
    - Missing/invalid theme selection falls back deterministically to default.
    - EN/PT routes keep equivalent functional behavior under any supported theme.
  </acceptance_criteria>
  <verify>
    <automated>node --test tests/slots-core-determinism-contract.test.mjs tests/slots-i18n-parity-contract.test.mjs && npm run build</automated>
  </verify>
  <done>Theme variants are wired as presentation-only runtime overlays with deterministic fallback.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add symbol-state/theme contracts and compatibility assertions</name>
  <read_first>
    - tests/slots-animation-event-sequencing-contract.test.mjs
    - tests/slots-sprite-atlas-contract.test.mjs
    - e2e/compatibility.spec.ts
    - .planning/phases/20-animated-symbols-and-theme-variants/20-VALIDATION.md
  </read_first>
  <files>tests/slots-symbol-states-contract.test.mjs, tests/slots-theme-variants-contract.test.mjs, tests/slots-animation-event-sequencing-contract.test.mjs, e2e/compatibility.spec.ts</files>
  <action>
    Close Phase 20 validation coverage:
    - Add contract tests for symbol-state determinism and theme-variant fallback guarantees.
    - Extend compatibility E2E with stable EN/PT assertions for active theme and symbol-state hooks.
    - Keep checks deterministic and timing-agnostic.
  </action>
  <acceptance_criteria>
    - Contracts fail on symbol-state mapping drift or theme fallback regressions.
    - E2E validates theme/symbol-state observability in canonical EN/PT flows.
    - Existing determinism, guard, and i18n parity suites remain green.
  </acceptance_criteria>
  <verify>
    <automated>node --test tests/slots-core-determinism-contract.test.mjs tests/slots-interaction-guards-contract.test.mjs tests/slots-i18n-parity-contract.test.mjs tests/slots-animation-event-sequencing-contract.test.mjs tests/slots-sprite-atlas-contract.test.mjs tests/slots-symbol-states-contract.test.mjs tests/slots-theme-variants-contract.test.mjs</automated>
  </verify>
  <done>Phase 20 has deterministic symbol-state/theme coverage in contracts and compatibility checks.</done>
</task>

</tasks>

<validation_commands>

1. npm run lint
2. node --test tests/slots-core-determinism-contract.test.mjs tests/slots-interaction-guards-contract.test.mjs tests/slots-i18n-parity-contract.test.mjs tests/slots-animation-event-sequencing-contract.test.mjs tests/slots-sprite-atlas-contract.test.mjs tests/slots-symbol-states-contract.test.mjs tests/slots-theme-variants-contract.test.mjs
3. npx playwright test e2e/compatibility.spec.ts --project=chromium
4. npm run build
5. npm run lint && node --test tests/slots-core-determinism-contract.test.mjs tests/slots-interaction-guards-contract.test.mjs tests/slots-i18n-parity-contract.test.mjs tests/slots-animation-event-sequencing-contract.test.mjs tests/slots-sprite-atlas-contract.test.mjs tests/slots-symbol-states-contract.test.mjs tests/slots-theme-variants-contract.test.mjs && npx playwright test e2e/compatibility.spec.ts --project=chromium && npm run build

</validation_commands>

<verification>
Phase 20 is complete when SPRITE-11 and SPRITE-12 are satisfied via presentation-only symbol-state/theme wiring and all validation commands pass without authority regressions.
</verification>

<success_criteria>

- Core symbols support deterministic animated states (`idle`, `spin`, `win-react`) via shared contracts.
- At least one additional theme variant is selectable with deterministic fallback behavior.
- Theme and symbol-state presentation does not alter deterministic payout/state outcomes.
- Determinism, i18n parity, sequencing, compatibility, and build validation remain green.
  </success_criteria>

<output>
After completion, create .planning/phases/20-animated-symbols-and-theme-variants/20-01-SUMMARY.md
</output>
