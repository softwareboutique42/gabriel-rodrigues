---
phase: 34-learning-loop-clarity-and-bounded-progression-ux
plan: 01
type: execute
wave: 1
depends_on:
  - 33-01
files_modified:
  - src/scripts/casinocraftz/tutorial/types.ts
  - src/scripts/casinocraftz/tutorial/dialogue.ts
  - src/scripts/casinocraftz/tutorial/main.ts
  - src/i18n/en.json
  - src/i18n/pt.json
  - tests/casinocraftz-tutorial-contract.test.mjs
  - tests/slots-i18n-parity-contract.test.mjs
  - e2e/compatibility.spec.ts
autonomous: true
requirements:
  - LEARN-50
  - LEARN-51
  - PROG-50
validation_commands:
  - node --test tests/casinocraftz-tutorial-contract.test.mjs tests/slots-i18n-parity-contract.test.mjs
  - npx playwright test e2e/compatibility.spec.ts --project=chromium --workers=1 --grep "play-and-observe step advances to probability-reveal after 3 spins via postMessage bridge - EN|play-and-observe step advances to probability-reveal after 3 spins via postMessage bridge - PT|casinocraftz tutorial system"
  - npm run lint
  - npm run build
must_haves:
  truths:
    - Users can immediately read why the tutorial moved to probability-reveal after the spin-triggered transition.
    - Users can replay the current step explanation in-shell without resetting spins, cards, or essence.
    - Users can open a compact in-shell recap explaining the spin-triggered transition condition.
    - Card progression is bounded and transparent with visible LOCKED/UNLOCKED status on each starter card.
    - EN/PT surfaces preserve parity for replay/recap/card-status copy and behavior.
    - No new gameplay mechanics, no runtime authority mutation paths, and no new routes are introduced.
  artifacts:
    - path: src/scripts/casinocraftz/tutorial/dialogue.ts
      provides: DIALOGUE_REGISTRY causality-first copy updates for spin-triggered transitions
      exports: getDialogue()
    - path: src/scripts/casinocraftz/tutorial/main.ts
      provides: replay control, spin-triggered recap disclosure, and card lock/unlock badge rendering inside existing zones
      exports: mountTutorial(), parseSpinSettledBridgeEvent()
    - path: src/scripts/casinocraftz/tutorial/types.ts
      provides: minimal tutorial state extension to mark spin-triggered transition context without authority coupling
      exports: TutorialState
    - path: src/i18n/en.json
      provides: EN copy keys for replay CTA, recap disclosure, and LOCKED/UNLOCKED card status labels
    - path: src/i18n/pt.json
      provides: PT copy keys for replay CTA, recap disclosure, and LOCKED/UNLOCKED card status labels
    - path: tests/casinocraftz-tutorial-contract.test.mjs
      provides: source contracts for scope boundaries, replay/recap rendering hooks, and card badge semantics
      min_lines: 80
    - path: tests/slots-i18n-parity-contract.test.mjs
      provides: EN/PT namespace parity contracts for all new tutorial/cards keys
      min_lines: 50
    - path: e2e/compatibility.spec.ts
      provides: deterministic browser verification of spin-triggered explanation visibility and bounded card progression UX
      min_lines: 300
  key_links:
    - from: src/scripts/casinocraftz/tutorial/engine.ts
      to: src/scripts/casinocraftz/tutorial/main.ts
      via: recordSpin(state) step transition detection gates recap visibility
      pattern: "recordSpin|currentStep"
    - from: src/scripts/casinocraftz/tutorial/dialogue.ts
      to: src/scripts/casinocraftz/tutorial/main.ts
      via: renderDialogue() consumes causality-first copy for spin-triggered step
      pattern: "getDialogue|probability-reveal"
    - from: src/scripts/casinocraftz/tutorial/main.ts
      to: src/i18n/en.json
      via: dataset-backed localized labels for replay/recap/card-status UI
      pattern: "data-casinocraftz.*replay|data-casinocraftz.*locked|data-casinocraftz.*unlocked"
    - from: src/scripts/casinocraftz/tutorial/main.ts
      to: src/i18n/pt.json
      via: parity-equivalent localized labels for replay/recap/card-status UI
      pattern: "tutorial\.cta|cards\."
---

<objective>
Implement deterministic explanatory UX inside existing Casinocraftz tutorial and cards surfaces so users understand spin-driven transitions and bounded card progression state without changing gameplay authority behavior.

Purpose: Close LEARN-50, LEARN-51, and PROG-50 while honoring strict scope boundaries from Phase 34 context (no new mechanics, no Slots runtime authority mutation, EN/PT parity preserved).

Output: Updated dialogue causality lines, replay + recap controls in the existing tutorial zone, lock/unlock status badges in existing cards zone, parity-safe copy keys, and deterministic source/browser validation coverage.
</objective>

<execution_context>
@/home/gabriel/Documents/gabriel-rodrigues/.claude/get-shit-done/workflows/execute-plan.md
@/home/gabriel/Documents/gabriel-rodrigues/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/34-learning-loop-clarity-and-bounded-progression-ux/34-CONTEXT.md
@.planning/phases/33-bridge-and-authority-hardening/33-01-SUMMARY.md
@src/scripts/casinocraftz/tutorial/types.ts
@src/scripts/casinocraftz/tutorial/engine.ts
@src/scripts/casinocraftz/tutorial/dialogue.ts
@src/scripts/casinocraftz/tutorial/main.ts
@src/pages/en/casinocraftz/index.astro
@src/pages/pt/casinocraftz/index.astro
@src/i18n/en.json
@src/i18n/pt.json
@tests/casinocraftz-tutorial-contract.test.mjs
@tests/slots-i18n-parity-contract.test.mjs
@e2e/compatibility.spec.ts

<interfaces>
From src/scripts/casinocraftz/tutorial/engine.ts:
- export function recordSpin(state: TutorialState): TutorialState;
- export function advanceTutorialStep(state: TutorialState): TutorialState;

From src/scripts/casinocraftz/tutorial/dialogue.ts:

- export const DIALOGUE_REGISTRY: Record<TutorialStepId, Record<'en' | 'pt', DialogueMessage[]>>;
- export function getDialogue(stepId: TutorialStepId, lang: 'en' | 'pt'): DialogueMessage[];

From src/scripts/casinocraftz/tutorial/main.ts:

- renderDialogue(zone, stepId, lang) currently performs deterministic full re-render of message list.
- renderCards(root, cardsZone, state, onActivate) currently reads state.cardsUnlocked to gate activation.
- onSpinMessage(event) currently derives transitions only from parseSpinSettledBridgeEvent -> recordSpin.

From src/scripts/casinocraftz/tutorial/types.ts:

- export interface TutorialState { currentStep; completedSteps; spinsObserved; essenceBalance; cardsUnlocked; activeCard; }
  </interfaces>
  </context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add deterministic causality messaging and spin-triggered recap/replay controls inside existing tutorial zone</name>
  <files>src/scripts/casinocraftz/tutorial/types.ts, src/scripts/casinocraftz/tutorial/dialogue.ts, src/scripts/casinocraftz/tutorial/main.ts, src/i18n/en.json, src/i18n/pt.json</files>
  <behavior>
    - Spin-triggered transition into probability-reveal shows first-line causality explanation in dialogue copy.
    - Replay CTA is visible from house-edge-intro onward and replays current step copy without mutating spins/cards/essence.
    - A collapsible "Why did this happen?" recap appears only after the spin-triggered transition path.
    - Button-driven transitions do not show spin-trigger recap state unless transition was spin-caused.
    - EN/PT replay and recap copy remains parity-locked.
  </behavior>
  <action>
    Implement LEARN-50 and LEARN-51 within existing tutorial zone boundaries only:

    1. Update `src/scripts/casinocraftz/tutorial/dialogue.ts` per D-01 and D-02:
       - Add causality-first lead message for `probability-reveal` in EN/PT when entered via spin-triggered transition.
       - Do not add causality copy to button-driven-only transitions.
       - Keep dialogue tone consistent with existing narrator/system voice.

    2. Extend `src/scripts/casinocraftz/tutorial/types.ts` minimally to track transition context for recap rendering in presentation layer only:
       - Add a small, deterministic field (for example `lastTransitionTrigger: 'spin' | 'ui' | null`) to TutorialState.
       - Do not add any field that can influence Slots authority/economy/RNG behavior.

    3. Update `src/scripts/casinocraftz/tutorial/main.ts` per D-03 and D-04:
       - In `onSpinMessage`, detect step change after `recordSpin` and mark recap trigger as spin-driven when transition occurs.
       - In `proceedStep`, mark transition trigger as UI-driven.
       - In `renderDialogue`, append a localized replay button for steps from `house-edge-intro` onward that only re-renders current step dialogue.
       - Add a localized `<details>/<summary>` recap block that renders only when state indicates spin-triggered transition into `probability-reveal`.
       - Render all new controls inside existing `[data-casinocraftz-zone="tutorial"]` content flow; do not introduce new zones/routes.

    4. Add i18n keys in `src/i18n/en.json` and `src/i18n/pt.json` for replay CTA and recap disclosure text.

    Guardrails:
    - No new tutorial steps, no progression logic changes, no new gameplay mechanics.
    - No mutation paths into Slots runtime authority surfaces (D-04 scope + Phase 33 boundary).

  </action>
  <verify>
    <automated>node --test tests/casinocraftz-tutorial-contract.test.mjs --test-name-pattern="tutorial|Bridge Versioning|Authority Isolation"</automated>
  </verify>
  <done>
    - LEARN-50 satisfied: spin-triggered transition has deterministic causal explanation visible in dialogue flow.
    - LEARN-51 satisfied (part 1): replay CTA is present and non-mutating from house-edge-intro onward.
    - LEARN-51 satisfied (part 2): recap disclosure appears only for spin-triggered probability-reveal path.
    - All additions remain in existing tutorial surface and preserve authority isolation.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Render bounded LOCKED/UNLOCKED card progression badges with EN/PT parity inside existing cards surface</name>
  <files>src/scripts/casinocraftz/tutorial/main.ts, src/i18n/en.json, src/i18n/pt.json, tests/casinocraftz-tutorial-contract.test.mjs, tests/slots-i18n-parity-contract.test.mjs</files>
  <behavior>
    - Every starter card shows a visible status badge in card header: LOCKED or UNLOCKED.
    - Badge state is computed at render-time from state.cardsUnlocked and updates deterministically.
    - Badge copy and behavior are parity-locked in EN/PT.
    - Badge is presentation-only and does not alter card unlock rules or authority logic.
  </behavior>
  <action>
    Implement PROG-50 per D-05 and D-06 without changing unlock mechanics:

    1. Update `renderCards()` in `src/scripts/casinocraftz/tutorial/main.ts`:
       - Add header row layout for card label + status badge chip.
       - Compute badge state dynamically from `state.cardsUnlocked.includes(card.id)`.
       - Render localized LOCKED/UNLOCKED badge text with existing token styles (`font-mono`, compact uppercase chip treatment).
       - Keep button enable/disable logic exactly as-is; badge is visual transparency only.

    2. Add i18n keys in `src/i18n/en.json` and `src/i18n/pt.json` for card status labels.

    3. Extend `tests/casinocraftz-tutorial-contract.test.mjs` and `tests/slots-i18n-parity-contract.test.mjs`:
       - Assert source contains deterministic badge rendering path based on `state.cardsUnlocked`.
       - Assert EN/PT parity for added card status keys.

    Guardrails:
    - Do not introduce card tiers, new card types, or new progression mechanics.
    - Do not alter `unlockCards()` or step order in `engine.ts`.

  </action>
  <verify>
    <automated>node --test tests/casinocraftz-tutorial-contract.test.mjs tests/slots-i18n-parity-contract.test.mjs</automated>
  </verify>
  <done>
    - PROG-50 satisfied: each card has deterministic lock/unlock visibility.
    - Badge rendering is fully dynamic (no hardcoded per-card lock states).
    - EN/PT localization parity for status labels is enforced by automated tests.
    - No gameplay/progression mechanics changed.
  </done>
</task>

<task type="auto">
  <name>Task 3: Add deterministic browser verification for explanatory UX and bounded progression while preserving scope limits</name>
  <files>e2e/compatibility.spec.ts</files>
  <action>
    Extend existing compatibility coverage to validate LEARN-50, LEARN-51, and PROG-50 in EN/PT and embedded host mode:

    1. In `e2e/compatibility.spec.ts`, add or refine tutorial-system checks that assert:
       - After 3 embedded spins, `data-casinocraftz-tutorial-step` reaches `probability-reveal` and causality explanation text is visible (D-01, D-02).
       - Replay control is visible from `house-edge-intro` onward and replay action does not reset essence/spin/card datasets (D-03).
       - Recap disclosure is visible only for spin-triggered transition path and absent for purely button-driven transitions (D-03).
       - Cards zone shows LOCKED/UNLOCKED badges and badge state reflects progression after unlock step (D-05, D-06).

    2. Keep checks deterministic:
       - Reuse existing stable selectors/dataset attributes.
       - Avoid screenshot assertions and non-deterministic timing patterns.

    3. Keep strict scope guards explicit in test intent:
       - No assertions for new mechanics or new routes.
       - No assertions that require Slots authority mutation from tutorial layer.

  </action>
  <verify>
    <automated>npx playwright test e2e/compatibility.spec.ts --project=chromium --workers=1 --grep "casinocraftz tutorial system|play-and-observe step advances to probability-reveal after 3 spins via postMessage bridge - EN|play-and-observe step advances to probability-reveal after 3 spins via postMessage bridge - PT"</automated>
  </verify>
  <done>
    - Browser tests deterministically prove explanatory transition clarity and bounded progression visibility in EN/PT.
    - Existing host-mode compatibility coverage remains green.
    - Phase scope remains bounded to existing tutorial/cards surfaces.
  </done>
</task>

</tasks>

<verification>
- Run all commands in `validation_commands` exactly as listed.
- Confirm source tests validate scope boundaries, i18n parity, and deterministic UI state hooks.
- Confirm Playwright coverage validates spin-triggered causality, recap/replay visibility, and card lock/unlock badges in EN/PT.
- Confirm no files outside listed `files_modified` are changed for feature scope.
- Confirm no additions to Slots authority internals, no new mechanics, no new routes.
</verification>

<success_criteria>

- LEARN-50: Users can see explicit, deterministic causality explanation when spin-triggered tutorial transition occurs.
- LEARN-51: Users can replay step copy and access spin-trigger recap prompts in-shell without state reset or mechanics changes.
- PROG-50: Users can read bounded lock/unlock status for starter cards directly in the cards surface.
- EN/PT parity remains intact for copy and behavior.
- Authority boundaries from Phase 33 remain intact: tutorial/cards layer stays presentation-only.
  </success_criteria>

<output>
After completion, create `.planning/phases/34-learning-loop-clarity-and-bounded-progression-ux/34-01-SUMMARY.md`.
</output>
