---
phase: 18-slots-animation-runtime-foundation
plan: 01
type: execute
wave: 1
depends_on:
  - 17-01
files_modified:
  - src/scripts/slots/controller.ts
  - src/scripts/slots/main.ts
  - src/scripts/slots/animation/events.ts
  - src/scripts/slots/animation/reel-timeline.ts
  - src/scripts/slots/animation/outcome-feedback.ts
  - src/scripts/slots/animation/runtime.ts
  - tests/slots-animation-event-sequencing-contract.test.mjs
  - e2e/compatibility.spec.ts
  - .planning/phases/18-slots-animation-runtime-foundation/18-01-SUMMARY.md
autonomous: true
requirements:
  - ANIM-10
  - ANIM-11
must_haves:
  truths:
    - User-visible reel lifecycle progresses through spin-up, sustained spin, and deterministic stop after an accepted spin.
    - Win/loss feedback appears only after resolved outcomes and never changes payout, balance, or authoritative round state.
    - Runtime behavior remains stable across Astro SPA navigations with no duplicate listener side effects.
  artifacts:
    - path: src/scripts/slots/animation/events.ts
      provides: Typed presentation event contract emitted from controller transitions.
    - path: src/scripts/slots/animation/runtime.ts
      provides: SPA-safe animation mount/dispose orchestration with AbortController cleanup.
    - path: tests/slots-animation-event-sequencing-contract.test.mjs
      provides: Deterministic sequencing contract coverage for presentation events.
  key_links:
    - from: src/scripts/slots/controller.ts
      to: src/scripts/slots/animation/events.ts
      via: deterministic visual event emission on accepted spin, resolved spin, and blocked spin paths
      pattern: emit.*visual
    - from: src/scripts/slots/main.ts
      to: src/scripts/slots/animation/runtime.ts
      via: astro:page-load mount and astro:before-swap dispose
      pattern: astro:page-load|astro:before-swap
    - from: e2e/compatibility.spec.ts
      to: data-slots-anim-* hooks
      via: stable runtime observability assertions without frame-perfect timing checks
      pattern: data-slots-anim-
validation_commands:
  - npm run lint
  - node --test tests/slots-core-determinism-contract.test.mjs tests/slots-interaction-guards-contract.test.mjs tests/slots-i18n-parity-contract.test.mjs tests/slots-animation-event-sequencing-contract.test.mjs
  - npx playwright test e2e/compatibility.spec.ts --project=chromium
  - npm run build
  - npm run lint && node --test tests/slots-core-determinism-contract.test.mjs tests/slots-interaction-guards-contract.test.mjs tests/slots-i18n-parity-contract.test.mjs tests/slots-animation-event-sequencing-contract.test.mjs && npx playwright test e2e/compatibility.spec.ts --project=chromium && npm run build
---

<objective>
Implement the Phase 18 runtime foundation for deterministic reel lifecycle choreography and outcome feedback wiring while preserving gameplay authority boundaries.

Purpose: satisfy ANIM-10 and ANIM-11 with presentation-only runtime additions that consume existing state transitions.
Output: typed animation event contracts, runtime orchestration modules, deterministic sequencing tests, and compatibility coverage updates.
</objective>

<execution_context>
@/home/gabriel/Documents/gabriel-rodrigues/.claude/get-shit-done/workflows/execute-plan.md
@/home/gabriel/Documents/gabriel-rodrigues/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/18-slots-animation-runtime-foundation/18-CONTEXT.md
@.planning/phases/18-slots-animation-runtime-foundation/18-RESEARCH.md
@.planning/phases/18-slots-animation-runtime-foundation/18-VALIDATION.md
@.planning/phases/17-slots-runtime-coverage-hardening/17-01-SUMMARY.md
@src/scripts/slots/controller.ts
@src/scripts/slots/main.ts
@src/scripts/slots/engine/state-machine.ts
@src/scripts/slots/engine/round.ts
@tests/slots-core-determinism-contract.test.mjs
@tests/slots-interaction-guards-contract.test.mjs
@tests/slots-i18n-parity-contract.test.mjs
@e2e/compatibility.spec.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define deterministic presentation event contract and emit from controller transitions</name>
  <read_first>
    - .planning/phases/18-slots-animation-runtime-foundation/18-CONTEXT.md
    - .planning/phases/18-slots-animation-runtime-foundation/18-RESEARCH.md
    - src/scripts/slots/controller.ts
    - src/scripts/slots/engine/state-machine.ts
    - src/scripts/slots/engine/round.ts
  </read_first>
  <files>src/scripts/slots/animation/events.ts, src/scripts/slots/controller.ts</files>
  <action>
    Implement ANIM-10/ANIM-11 event foundations per locked decision "deterministic authority separation":
    - Create a typed presentation event contract in `src/scripts/slots/animation/events.ts` for accepted spin start, resolved spin, and blocked spin outcomes.
    - Update `src/scripts/slots/controller.ts` to emit immutable visual payload snapshots at authoritative checkpoints only.
    - Keep gameplay authority untouched: do not move or add any state-machine/economy mutation outside existing controller authority paths.
    - Ensure events are deterministic and indexable by round/spin identifiers for contract tests.
  </action>
  <acceptance_criteria>
    - Controller emits deterministic visual events for accepted, resolved, and blocked spin branches.
    - Event payloads are presentation-only snapshots and cannot mutate engine/economy state.
    - Existing authoritative transitions and payout/balance writes remain in current gameplay modules.
  </acceptance_criteria>
  <verify>
    <automated>node --test tests/slots-core-determinism-contract.test.mjs tests/slots-interaction-guards-contract.test.mjs</automated>
  </verify>
  <done>Typed event contract exists and controller emission wiring is present without gameplay authority drift.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement SPA-safe reel lifecycle and outcome feedback animation runtime wiring</name>
  <read_first>
    - src/scripts/slots/main.ts
    - src/scripts/slots/controller.ts
    - .planning/phases/18-slots-animation-runtime-foundation/18-RESEARCH.md
  </read_first>
  <files>src/scripts/slots/animation/reel-timeline.ts, src/scripts/slots/animation/outcome-feedback.ts, src/scripts/slots/animation/runtime.ts, src/scripts/slots/main.ts</files>
  <action>
    Build deterministic presentation orchestration for ANIM-10 and ANIM-11, keeping authority read-only:
    - Add `reel-timeline.ts` for spin-up/sustain/stop choreography driven strictly from emitted visual events.
    - Add `outcome-feedback.ts` to trigger win/loss emphasis only from resolved outcome payloads.
    - Add `runtime.ts` mount/dispose orchestration with idempotent subscriptions and AbortController-safe cleanup.
    - Wire runtime mount/dispose into `src/scripts/slots/main.ts` using existing `astro:page-load` and re-navigation cleanup patterns.
    - Preserve machine-readable contracts by adding presentation-only `data-slots-anim-*` hooks (no mutation of authoritative `data-slots-*` gameplay values).
  </action>
  <acceptance_criteria>
    - Reel lifecycle phases map to deterministic visual runtime states from controller event flow.
    - Outcome feedback runs only after resolved outcomes and never gates or alters authoritative transitions.
    - SPA re-navigation does not duplicate runtime listeners or animation triggers.
  </acceptance_criteria>
  <verify>
    <automated>node --test tests/slots-core-determinism-contract.test.mjs tests/slots-interaction-guards-contract.test.mjs && npm run build</automated>
  </verify>
  <done>Animation runtime modules are wired into slots mount lifecycle and remain presentation-only.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add deterministic sequencing contracts and compatibility assertions for runtime observability</name>
  <read_first>
    - tests/slots-core-determinism-contract.test.mjs
    - tests/slots-interaction-guards-contract.test.mjs
    - tests/slots-i18n-parity-contract.test.mjs
    - e2e/compatibility.spec.ts
    - .planning/phases/18-slots-animation-runtime-foundation/18-VALIDATION.md
  </read_first>
  <files>tests/slots-animation-event-sequencing-contract.test.mjs, e2e/compatibility.spec.ts</files>
  <action>
    Close validation gap for deterministic animation sequencing without visual flake:
    - Add `tests/slots-animation-event-sequencing-contract.test.mjs` covering deterministic event order and no-authority-mutation constraints.
    - Extend `e2e/compatibility.spec.ts` to assert stable animation observability hooks (`data-slots-anim-*`) during runtime flow on canonical routes.
    - Keep assertions deterministic and state-driven; avoid frame-perfect or timing-fragile checks.
    - Confirm EN/PT runtime parity remains intact while animations are active.
  </action>
  <acceptance_criteria>
    - Contract tests fail on sequencing regressions or authority-coupling leaks.
    - Compatibility E2E verifies animation runtime observability without brittle timing assertions.
    - Existing determinism, guard, and i18n parity checks remain green.
  </acceptance_criteria>
  <verify>
    <automated>node --test tests/slots-core-determinism-contract.test.mjs tests/slots-interaction-guards-contract.test.mjs tests/slots-i18n-parity-contract.test.mjs tests/slots-animation-event-sequencing-contract.test.mjs</automated>
  </verify>
  <done>Phase 18 runtime validation covers deterministic sequencing and presentation-only behavior in contracts and browser checks.</done>
</task>

</tasks>

<validation_commands>

1. npm run lint
2. node --test tests/slots-core-determinism-contract.test.mjs tests/slots-interaction-guards-contract.test.mjs tests/slots-i18n-parity-contract.test.mjs tests/slots-animation-event-sequencing-contract.test.mjs
3. npx playwright test e2e/compatibility.spec.ts --project=chromium
4. npm run build
5. npm run lint && node --test tests/slots-core-determinism-contract.test.mjs tests/slots-interaction-guards-contract.test.mjs tests/slots-i18n-parity-contract.test.mjs tests/slots-animation-event-sequencing-contract.test.mjs && npx playwright test e2e/compatibility.spec.ts --project=chromium && npm run build

</validation_commands>

<verification>
Phase 18 is complete when ANIM-10 and ANIM-11 are satisfied via presentation-only runtime wiring and all validation commands pass without gameplay authority regressions.
</verification>

<success_criteria>

- Deterministic reel lifecycle choreography is event-driven and aligned with authoritative state transitions.
- Outcome feedback animation triggers only from resolved outcomes and leaves engine/economy authority unchanged.
- SPA lifecycle remains idempotent and listener-safe during route transitions.
- Determinism, interaction guards, i18n parity, and compatibility validation remain green.
  </success_criteria>

<output>
After completion, create .planning/phases/18-slots-animation-runtime-foundation/18-01-SUMMARY.md
</output>
