---
phase: 07-export-funnel-conversion-uplift
plan: 01
type: execute
wave: 2
depends_on:
  - 06-01
files_modified:
  - src/pages/en/canvas/index.astro
  - src/pages/pt/canvas/index.astro
  - src/i18n/en.json
  - src/i18n/pt.json
  - src/scripts/canvas/main.ts
  - tests/export-contract.test.mjs
  - e2e/canvas-export.spec.ts
autonomous: true
requirements:
  - CONV-01
  - CONV-02
  - CONV-03
must_haves:
  truths:
    - User sees concise premium value framing before payment in both EN and PT.
    - User follows one primary CTA path from generate to checkout to download without competing branches.
    - After payment return, user immediately sees success confirmation plus visible export progress and next-step outcome.
  artifacts:
    - path: src/pages/en/canvas/index.astro
      provides: EN export modal and processing-state markup for value cues and primary CTA flow labels
    - path: src/pages/pt/canvas/index.astro
      provides: PT export modal and processing-state parity with EN structure
    - path: src/scripts/canvas/main.ts
      provides: Checkout transition and post-payment success/progress orchestration in a single flow
    - path: src/i18n/en.json
      provides: EN copy for premium framing, CTA transitions, and success/progress states
    - path: src/i18n/pt.json
      provides: PT copy parity for premium framing, CTA transitions, and success/progress states
    - path: e2e/canvas-export.spec.ts
      provides: Executable flow assertions for pre-checkout cues and post-payment status behavior
  key_links:
    - from: src/pages/en/canvas/index.astro
      to: src/scripts/canvas/main.ts
      via: export modal element IDs and data-* copy hooks consumed by event handlers
      pattern: canvas-export-modal|export-modal-confirm|canvas-download-processing
    - from: src/i18n/en.json
      to: src/pages/en/canvas/index.astro
      via: translation keys bound to value framing and CTA labels
      pattern: canvas\.exportModal|canvas\.download
    - from: src/i18n/pt.json
      to: src/pages/pt/canvas/index.astro
      via: translation keys bound to PT-equivalent value framing and CTA labels
      pattern: canvas\.exportModal|canvas\.download
    - from: src/scripts/canvas/main.ts
      to: e2e/canvas-export.spec.ts
      via: checkout/download status transitions asserted in return-flow tests
      pattern: handleDownload|handlePaymentReturn|download-status
---

<objective>
Increase paid export conversion by reducing funnel ambiguity while preserving the existing browser-first export architecture.

Purpose: Implement CONV-01/02/03 with concise pre-payment confidence cues, one dominant CTA per stage, and immediate post-payment success/progress feedback.
Output: EN/PT funnel copy and UI updates, tightened checkout transition handling, and automated regression coverage for conversion-critical flow states.
</objective>

<scope>
In scope:
- Add concise premium value framing near export confirmation before checkout (minimal copy, no large marketing panel).
- Enforce single dominant CTA progression: Generate -> Export Options -> Continue to Checkout -> Download.
- Clarify post-payment success state and keep export progress/next-step visibility in-place (no separate success page).
- Preserve EN/PT parity for all user-facing text and flow labels.
- Extend automated tests for conversion-critical states.

Out of scope:

- New payment provider, billing model redesign, or Stripe architecture changes.
- New export formats, server-side rendering pipeline, or analytics dashboard.
- Alternate or dual funnel branches in this phase.
  </scope>

<dependencies>
- Plan dependency: 06-01 (preset routing and normalization foundation must remain unchanged).
- Task dependency chain: Task 1 -> Task 2 -> Task 3.
- Shared file dependency: `src/scripts/canvas/main.ts` changes in Task 2 are required before final E2E assertions in Task 3.
</dependencies>

<execution_context>
@/home/gabriel/Documents/gabriel-rodrigues/.claude/get-shit-done/workflows/execute-plan.md
@/home/gabriel/Documents/gabriel-rodrigues/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/07-export-funnel-conversion-uplift/07-CONTEXT.md
@.planning/phases/06-vertical-style-pack-foundation/06-01-SUMMARY.md
@src/pages/en/canvas/index.astro
@src/pages/pt/canvas/index.astro
@src/i18n/en.json
@src/i18n/pt.json
@src/scripts/canvas/main.ts
@tests/export-contract.test.mjs
@e2e/canvas-export.spec.ts

<interfaces>
From src/pages/en/canvas/index.astro and src/pages/pt/canvas/index.astro:
- `#canvas-download` opens `#canvas-export-modal`.
- `#export-modal-confirm` triggers checkout continuation.
- `#canvas-download-processing` stores data-* status text consumed by JS.

From src/scripts/canvas/main.ts:

- `handleDownload(config, settings)` controls checkout transition.
- `handlePaymentReturn(signal?)` controls post-payment success/progress feedback.
- `setDownloadStatus(...)` is the central status-text update path.

From e2e/canvas-export.spec.ts:

- Existing tests already assert return-flow progress and fallback behavior, making it the right place to lock CONV post-payment guarantees.
  </interfaces>
  </context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add concise pre-payment value framing and CTA language parity (CONV-01, CONV-02)</name>
  <files>src/pages/en/canvas/index.astro, src/pages/pt/canvas/index.astro, src/i18n/en.json, src/i18n/pt.json</files>
  <behavior>
    - Test 1: export modal presents concise value framing before payment in EN and PT.
    - Test 2: one dominant CTA label is used for checkout continuation in both locales.
    - Test 3: EN/PT keys remain structurally equivalent for all newly added funnel strings.
  </behavior>
  <action>
    Implement locked decision to keep value framing minimal: add short, high-confidence value cues adjacent to export confirmation controls without introducing a large marketing/comparison section.
    Standardize CTA copy so each stage has one primary action and no dual-branch labels.
    Update i18n keys in EN/PT together and wire them to existing modal and processing elements.
    Do not add new UI steps, new page routes, or alternate checkout paths.
  </action>
  <verify>
    <automated>node --test tests/export-contract.test.mjs</automated>
  </verify>
  <done>
    Both locales show concise pre-payment value framing with one dominant CTA label path and no added branch complexity.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Tighten checkout transition and post-payment success/progress orchestration (CONV-02, CONV-03)</name>
  <files>src/scripts/canvas/main.ts, src/i18n/en.json, src/i18n/pt.json</files>
  <behavior>
    - Test 1: checkout transition preserves a clear primary next action and avoids ambiguous interim states.
    - Test 2: session return immediately shows explicit success confirmation before/with export progress visibility.
    - Test 3: success/progress/fallback status wording maps to localized keys in EN/PT without hardcoded copy.
  </behavior>
  <action>
    Refine `handleDownload` and `handlePaymentReturn` sequencing so the user sees immediate successful-payment confirmation and an explicit next-step outcome while export begins.
    Keep status updates centralized through `setDownloadStatus` and data-* text sources to avoid copy drift.
    Preserve existing safeguards and browser capability fallbacks; do not introduce redirect-to-success-page behavior.
    Keep logic compatible with Astro SPA navigation and existing abort/cleanup patterns.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>
    Post-payment UX shows immediate confirmation and visible progress/next-action outcome, with localized and non-duplicated status copy.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Lock conversion-critical behavior with E2E regression assertions (CONV-01, CONV-02, CONV-03)</name>
  <files>e2e/canvas-export.spec.ts, tests/export-contract.test.mjs, .planning/phases/07-export-funnel-conversion-uplift/07-01-SUMMARY.md</files>
  <behavior>
    - Test 1: pre-payment export modal exposes value framing and single dominant checkout CTA in EN and PT.
    - Test 2: return flow shows success confirmation and export progress states in supported environments.
    - Test 3: unsupported capability path still shows clear next action/outcome messaging without dead-end states.
  </behavior>
  <action>
    Extend Playwright coverage in `e2e/canvas-export.spec.ts` for the conversion path, including pre-checkout cue visibility and post-payment status transitions.
    Add/extend contract checks in `tests/export-contract.test.mjs` for required localization hooks and status key usage to prevent regressions.
    Run validation commands and document outcomes, residual risk, and rollback triggers in the phase summary.
  </action>
  <verify>
    <automated>npm run test -- e2e/canvas-export.spec.ts && node --test tests/export-contract.test.mjs && npm run build</automated>
  </verify>
  <done>
    Conversion-critical funnel behavior is protected by automated tests and execution results are documented for handoff.
  </done>
</task>

</tasks>

<risks>
- Risk R1: EN/PT copy drift from asymmetric key updates.
  Mitigation: modify both locale files in the same tasks and add contract checks for key parity.
- Risk R2: Conversion copy changes accidentally introduce ambiguous dual CTAs.
  Mitigation: lock one-primary-action assertions in E2E and keep modal/button semantics unchanged.
- Risk R3: Post-payment status sequencing regresses fallback behavior.
  Mitigation: preserve capability-gated paths and assert both supported and unsupported flows in tests.
- Risk R4: Over-expansion of value framing bloats UX and conflicts with locked decisions.
  Mitigation: constrain implementation to concise cues only; no expanded marketing panel.
</risks>

<rollback_strategy>
Trigger rollback if conversion flow tests fail, locale parity breaks, or return-flow status becomes ambiguous.

Rollback steps:

1. Revert modal/value-copy updates in `src/pages/en/canvas/index.astro`, `src/pages/pt/canvas/index.astro`, `src/i18n/en.json`, and `src/i18n/pt.json`.
2. Revert post-payment sequencing changes in `src/scripts/canvas/main.ts` to the last known passing behavior.
3. Re-run `npm run test -- e2e/canvas-export.spec.ts`, `node --test tests/export-contract.test.mjs`, and `npm run build` to confirm baseline restoration.
   </rollback_strategy>

<verification>
- node --test tests/export-contract.test.mjs
- npm run test -- e2e/canvas-export.spec.ts
- npm run build
</verification>

<success_criteria>

- CONV-01: export funnel shows concise premium value framing before payment in EN and PT.
- CONV-02: CTA copy and stage transitions are simplified into one dominant path without alternate branches.
- CONV-03: post-payment state confirms success immediately and keeps export progress/next action visible.
- No architecture drift: Astro static setup, Stripe integration pattern, and browser-first export flow remain intact.
  </success_criteria>

<output>
After completion, create `.planning/phases/07-export-funnel-conversion-uplift/07-01-SUMMARY.md`
</output>
