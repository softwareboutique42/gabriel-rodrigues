---
phase: 52-lobby-simplification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/i18n/en.json
  - src/i18n/pt.json
  - src/styles/global.css
autonomous: true
requirements:
  - LBY-01
  - LBY-02

must_haves:
  truths:
    - "The lobby renders in a single column with no sidebar gap"
    - "Chamber cards have i18n keys for all three metric explanations"
    - "Both EN and PT translation files contain the 4 new keys"
  artifacts:
    - path: "src/i18n/en.json"
      provides: "EN translation keys for info popovers"
      contains: "lobby.telemetry.houseEdge.explain"
    - path: "src/i18n/pt.json"
      provides: "PT translation keys for info popovers"
      contains: "lobby.telemetry.houseEdge.explain"
    - path: "src/styles/global.css"
      provides: "Single-column lobby layout"
      contains: "ccz-lobby-grid"
  key_links:
    - from: "src/i18n/en.json"
      to: "src/pages/en/casinocraftz/index.astro"
      via: "t('lobby.telemetry.houseEdge.explain')"
      pattern: "lobby\\.telemetry\\.houseEdge\\.explain"
    - from: "src/styles/global.css"
      to: ".ccz-lobby-grid"
      via: "grid-template-columns"
      pattern: "ccz-lobby-grid"
---

<objective>
Add 4 new i18n keys for info button popovers to both EN and PT translation files, then convert the lobby grid from two-column to single-column by editing global.css and removing the analyzer-panel CSS rules.

Purpose: Establish the string and layout foundation that plans 52-02 and 52-03 depend on. These changes are non-breaking — the existing markup continues to render, just reflowed into a single column.

Output: Updated en.json, pt.json, and global.css. No HTML changes in this plan.
</objective>

<execution_context>
@/home/gabriel/Documents/gabriel-rodrigues/.claude/get-shit-done/workflows/execute-plan.md
@/home/gabriel/Documents/gabriel-rodrigues/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add 4 new i18n keys to en.json and pt.json</name>
  <files>src/i18n/en.json, src/i18n/pt.json</files>
  <action>
In `src/i18n/en.json`, insert the following 4 lines immediately after line 92
(`"lobby.telemetry.impulse": "IMPULSE",`), before the `"tutorial.step.welcome.label"` entry:

```json
  "lobby.telemetry.houseEdge.explain": "The house edge is the mathematical advantage the casino holds on every bet. A 5.6% edge means the house earns 5.6 cents for every dollar wagered over time.",
  "lobby.telemetry.signal.explain": "Signal tracks the RNG output pattern across recent spins. Animated bars visualise entropy — they carry no predictive value.",
  "lobby.telemetry.impulse.explain": "Impulse counts simulated bets per minute and reflects session pacing. Higher impulse increases exposure to the house edge per unit of time.",
  "lobby.telemetry.info.ariaLabel": "Show explanation",
```

In `src/i18n/pt.json`, insert the following 4 lines immediately after line 92
(`"lobby.telemetry.impulse": "IMPULSO",`), before the `"tutorial.step.welcome.label"` entry:

```json
  "lobby.telemetry.houseEdge.explain": "O house edge e a vantagem matematica que o cassino tem em cada aposta. Um edge de 5,6% significa que a casa ganha 5,6 centavos para cada real apostado ao longo do tempo.",
  "lobby.telemetry.signal.explain": "O sinal acompanha o padrao de saida do RNG nas rodadas recentes. As barras animadas visualizam entropia — nao possuem valor preditivo.",
  "lobby.telemetry.impulse.explain": "O impulso conta apostas simuladas por minuto e reflete o ritmo da sessao. Impulso mais alto aumenta a exposicao ao house edge por unidade de tempo.",
  "lobby.telemetry.info.ariaLabel": "Exibir explicacao",
```

After editing, verify both files remain valid JSON:
```bash
node -e "JSON.parse(require('fs').readFileSync('src/i18n/en.json','utf8')); console.log('en.json OK')"
node -e "JSON.parse(require('fs').readFileSync('src/i18n/pt.json','utf8')); console.log('pt.json OK')"
```
  </action>
  <verify>
    <automated>node -e "const e=JSON.parse(require('fs').readFileSync('src/i18n/en.json','utf8')); const p=JSON.parse(require('fs').readFileSync('src/i18n/pt.json','utf8')); ['lobby.telemetry.houseEdge.explain','lobby.telemetry.signal.explain','lobby.telemetry.impulse.explain','lobby.telemetry.info.ariaLabel'].forEach(k=>{if(!e[k])throw new Error('Missing EN: '+k); if(!p[k])throw new Error('Missing PT: '+k)}); console.log('OK: all 4 keys present in both locales')"</automated>
  </verify>
  <done>All 4 keys exist in both en.json and pt.json with non-empty string values. Both files parse as valid JSON.</done>
</task>

<task type="auto">
  <name>Task 2: Convert ccz-lobby-grid to single-column and remove analyzer-panel CSS</name>
  <files>src/styles/global.css</files>
  <action>
In `src/styles/global.css`, the current block starting at line 161 reads:

```css
/* === CASINOCRAFTZ LOBBY LAYOUT === */
.ccz-lobby-grid {
  display: grid;
  grid-template-columns: 260px 1fr;
  gap: 1.5rem;
  align-items: start;
}

@media (max-width: 639px) {
  .ccz-lobby-grid {
    display: block;
  }
}

.ccz-analyzer-panel {
  position: sticky;
  top: 1.5rem;
}

@media (max-width: 639px) {
  .ccz-analyzer-panel {
    position: static;
  }
  .ccz-analyzer-panel [data-ccz-analyzer-drawer] {
    display: none;
  }
  .ccz-analyzer-panel [data-ccz-analyzer-drawer]:not([hidden]) {
    display: block;
  }
}


@media (min-width: 640px) {
  .ccz-analyzer-panel [data-ccz-analyzer-drawer] {
    display: block;
  }
}
```

Replace that entire block (lines 161–197, from the `/* === CASINOCRAFTZ LOBBY LAYOUT === */` comment through the closing `}` of the last `@media (min-width: 640px)` block) with:

```css
/* === CASINOCRAFTZ LOBBY LAYOUT === */
.ccz-lobby-grid {
  display: block;
}
```

This removes:
- The `grid-template-columns: 260px 1fr` two-column rule
- The mobile `display: block` override (now the default)
- All `.ccz-analyzer-panel` rules (sticky positioning, drawer show/hide logic)
- The `@media (min-width: 640px)` analyzer-drawer rule

The `.ccz-lobby-grid` class is kept (not deleted) so the HTML does not need to change yet — it is now simply a block container.
  </action>
  <verify>
    <automated>node -e "const css=require('fs').readFileSync('src/styles/global.css','utf8'); if(css.includes('260px'))throw new Error('260px column still present'); if(css.includes('ccz-analyzer-panel'))throw new Error('ccz-analyzer-panel still present'); if(!css.includes('ccz-lobby-grid'))throw new Error('ccz-lobby-grid missing'); console.log('OK: single-column layout in place, analyzer-panel rules removed')"</automated>
  </verify>
  <done>
- `.ccz-lobby-grid` exists in global.css with only `display: block`
- `grid-template-columns: 260px 1fr` is gone
- All `.ccz-analyzer-panel` selectors are gone
- `npm run build` produces no CSS errors
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| i18n string → DOM | Translation strings are build-time static; no user input crosses this boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-52-01 | Tampering | en.json / pt.json | accept | Files are build-time assets; no runtime write path exists |
| T-52-02 | Information Disclosure | global.css | accept | CSS is public; no secrets in layout rules |
</threat_model>

<verification>
After both tasks complete, run:

```bash
npm run build
```

The build must succeed with no errors. Then open the dev server (`npm run dev`) and visit `/en/casinocraftz/` — the page should render as a single column with header, wallet HUD, and chamber cards occupying full width (no 260px sidebar gap on left).
</verification>

<success_criteria>
- en.json and pt.json each contain exactly 4 new keys: `lobby.telemetry.houseEdge.explain`, `lobby.telemetry.signal.explain`, `lobby.telemetry.impulse.explain`, `lobby.telemetry.info.ariaLabel`
- `src/styles/global.css` no longer contains `260px` or any `.ccz-analyzer-panel` selector
- `npm run build` exits 0
</success_criteria>

<output>
After completion, create `.planning/phases/52-lobby-simplification/52-01-SUMMARY.md` following the summary template.
</output>
