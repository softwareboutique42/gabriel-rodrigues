---
phase: 35-en-pt-and-host-mode-parity-matrix-lock
plan: 01
type: execute
wave: 1
depends_on:
  - 34-01
files_modified:
  - tests/slots-i18n-parity-contract.test.mjs
  - tests/casinocraftz-tutorial-contract.test.mjs
  - e2e/compatibility.spec.ts
autonomous: true
requirements:
  - PAR-50
  - PAR-51
validation_commands:
  - node --test tests/casinocraftz-tutorial-contract.test.mjs tests/slots-i18n-parity-contract.test.mjs
  - npx playwright test e2e/compatibility.spec.ts --project=chromium --workers=1 --grep "casinocraftz tutorial system|embedded host parity|slots runtime compatibility"
  - npm run lint
  - npm run build
must_haves:
  truths:
    - EN/PT copy and machine-readable datasets remain parity-locked for affected tutorial and slots surfaces.
    - Embedded host and standalone host matrix checks are explicit and deterministic.
    - No locale-specific logic alters authority behavior.
    - No new routes or gameplay mechanics are introduced.
  artifacts:
    - path: tests/slots-i18n-parity-contract.test.mjs
      provides: parity assertions for EN/PT keys and host-mode behavior invariants
    - path: tests/casinocraftz-tutorial-contract.test.mjs
      provides: tutorial parity and boundary source contracts
    - path: e2e/compatibility.spec.ts
      provides: deterministic browser-level matrix verification for EN/PT and host-mode slices
---

<objective>
Lock PAR-50 and PAR-51 by enforcing EN/PT copy+dataset parity and embedded/standalone host-mode matrix checks across v1.8 learning-loop surfaces.
</objective>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lock EN/PT source parity contracts for tutorial and slots surfaces</name>
  <files>tests/slots-i18n-parity-contract.test.mjs, tests/casinocraftz-tutorial-contract.test.mjs</files>
  <action>
    Add or tighten source-reading contracts to ensure parity keys, dataset anchors, and behavior hooks remain aligned between EN and PT routes.
  </action>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Lock host-mode matrix compatibility checks in browser tests</name>
  <files>e2e/compatibility.spec.ts</files>
  <action>
    Ensure deterministic compatibility coverage explicitly validates embedded host mode and standalone mode parity across EN/PT.
  </action>
</task>

<task type="auto">
  <name>Task 3: Run full validation chain and publish summary evidence</name>
  <files>.planning/phases/35-en-pt-and-host-mode-parity-matrix-lock/35-01-SUMMARY.md</files>
  <action>
    Run all validation commands, confirm PAR-50/PAR-51 outcomes, and create summary artifact.
  </action>
</task>

</tasks>
