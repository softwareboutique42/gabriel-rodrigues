---
phase: 03-video-export
plan: 04
type: execute
wave: 4
depends_on:
  - 03-02
  - 03-03
files_modified:
  - package.json
  - package-lock.json
  - src/scripts/canvas/export-encoders.ts
  - src/scripts/canvas/export-support.ts
  - src/scripts/canvas/export-controller.ts
  - src/scripts/canvas/main.ts
  - tests/export-contract.test.mjs
autonomous: true
requirements:
  - FR-3.10
  - NFR-7
must_haves:
  truths:
    - Chrome and Edge can select an MP4 export path when WebCodecs VideoEncoder is available.
    - VideoFrame resources are closed after encode submission to prevent GPU memory growth.
    - Non-Chromium browsers continue using WebM or fallback flow without regression.
    - MP4 path remains browser-side and does not introduce server encoding dependency.
  artifacts:
    - path: src/scripts/canvas/export-encoders.ts
      provides: WebCodecs plus mp4-muxer encoder implementation behind adapter boundary.
    - path: src/scripts/canvas/export-support.ts
      provides: Runtime path-selection logic preferring MP4 only when WebCodecs support is present.
    - path: src/scripts/canvas/export-controller.ts
      provides: Controller integration that routes capture frames to MP4 encoder path.
    - path: tests/export-contract.test.mjs
      provides: Contract tests for MP4 path gating and VideoFrame cleanup expectations.
  key_links:
    - from: src/scripts/canvas/export-support.ts
      to: src/scripts/canvas/export-controller.ts
      via: preferred export format decision consumed during controller start
      pattern: selectBestExportPath
    - from: src/scripts/canvas/export-controller.ts
      to: src/scripts/canvas/export-encoders.ts
      via: encoder factory selection for webm or mp4
      pattern: create.*Encoder|webcodecs
    - from: src/scripts/canvas/export-encoders.ts
      to: package.json
      via: mp4-muxer dependency used only in WebCodecs implementation
      pattern: mp4-muxer
---

<objective>
Add the Chromium MP4 enhancement path with safe resource cleanup and strict capability gating.

Purpose: FR-3.10 is a SHOULD requirement that improves download compatibility while preserving the mandatory WebM path.
Output: mp4-muxer-backed WebCodecs encoder integration, runtime gating, and regression tests for memory-safe frame handling.
</objective>

<execution_context>
@/home/gabriel/Documents/gabriel-rodrigues/.claude/get-shit-done/workflows/execute-plan.md
@/home/gabriel/Documents/gabriel-rodrigues/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-video-export/03-RESEARCH.md
@.planning/phases/03-video-export/03-02-PLAN.md
@.planning/phases/03-video-export/03-03-PLAN.md
@src/scripts/canvas/export-encoders.ts
@src/scripts/canvas/export-controller.ts
@src/scripts/canvas/export-support.ts
@src/scripts/canvas/main.ts

<interfaces>
From Plan 03-02 output contract:
```ts
type ExportControllerOptions = {
  width: number;
  height: number;
  fps: number;
  durationSeconds: number;
  onProgress?: (frame: number, totalFrames: number) => void;
};
```

From requirements FR-3.10:

- MP4 path should be used when VideoEncoder is available.
- VideoFrame objects must be closed after encoding.
  </interfaces>
  </context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add WebCodecs plus mp4-muxer encoder implementation behind existing adapter</name>
  <files>package.json, package-lock.json, src/scripts/canvas/export-encoders.ts, src/scripts/canvas/export-support.ts, src/scripts/canvas/export-controller.ts, src/scripts/canvas/main.ts</files>
  <behavior>
    - MP4 encoder initializes only when VideoEncoder is available.
    - WebM remains default when MP4 capability is unavailable.
    - Frame objects are closed consistently after encode submission.
  </behavior>
  <action>Install `mp4-muxer`, then implement `WebCodecsMp4Encoder` in `export-encoders.ts` and route selection logic through `export-support.ts` and `export-controller.ts`. Keep MP4 path capability-gated to Chromium-class environments and preserve existing WebM/fallback behavior in `main.ts`. Ensure every created VideoFrame is closed to satisfy FR-3.10 memory-safety requirement.</action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>MP4 enhancement path exists behind runtime gate with no regression to mandatory export flows.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend export contract tests for MP4 path gating and resource cleanup</name>
  <files>tests/export-contract.test.mjs</files>
  <behavior>
    - Tests verify MP4 path selection only when VideoEncoder support is present.
    - Tests assert VideoFrame close calls occur for encoded frames.
    - Tests confirm unsupported environments route to WebM/fallback paths.
  </behavior>
  <action>Expand `tests/export-contract.test.mjs` with targeted unit/integration-contract coverage for WebCodecs MP4 behavior. Use mocks/spies for VideoEncoder and VideoFrame lifecycle calls so cleanup guarantees are asserted without relying on full browser encoding execution in Node.</action>
  <verify>
    <automated>node --test tests/export-contract.test.mjs</automated>
  </verify>
  <done>FR-3.10 enhancement logic is guarded by regression tests and safe cleanup contracts.</done>
</task>

</tasks>

<verification>
Run build and export contract tests. Confirm MP4 path is only available under WebCodecs support and VideoFrame cleanup is explicitly tested.
</verification>

<success_criteria>

- FR-3.10 is implemented as a capability-gated enhancement.
- Browser-only architecture is maintained with no server encode path (NFR-7).
- Existing WebM and fallback flows remain unchanged for unsupported MP4 environments.
  </success_criteria>

<output>
After completion, create `.planning/phases/03-video-export/03-04-SUMMARY.md`
</output>
