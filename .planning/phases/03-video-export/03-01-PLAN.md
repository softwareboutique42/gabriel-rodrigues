---
phase: 03-video-export
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/scripts/canvas/renderer.ts
  - src/scripts/canvas/quality-profiles.ts
  - src/scripts/canvas/export-support.ts
  - src/scripts/canvas/export-encoders.ts
  - tests/export-contract.test.mjs
  - tests/quality-profiles.test.mjs
autonomous: true
requirements:
  - FR-3.3
  - FR-3.6
  - FR-3.9
  - FR-3.11
  - NFR-7
must_haves:
  truths:
    - Export contracts define one default profile: 1920x1080, 30 fps, and 12-second duration.
    - Renderer supports deterministic export stepping with manual elapsed input instead of wall-clock time.
    - Capability detection determines whether WebM export is available before any export flow starts.
    - Export mode particle budgets are reduced by 30 percent without breaking existing mobile caps.
  artifacts:
    - path: src/scripts/canvas/renderer.ts
      provides: Export-compatible renderer API with deterministic render-frame entrypoint.
    - path: src/scripts/canvas/export-support.ts
      provides: Capability and export-path detection helpers for WebM and fallback states.
    - path: src/scripts/canvas/export-encoders.ts
      provides: Export encoder interfaces and baseline WebM encoder contract used by later plans.
    - path: tests/export-contract.test.mjs
      provides: Contract coverage for defaults, capability gating, and deterministic export semantics.
    - path: tests/quality-profiles.test.mjs
      provides: Regression coverage for 30 percent export particle reduction behavior.
  key_links:
    - from: src/scripts/canvas/export-support.ts
      to: src/scripts/canvas/main.ts
      via: capability gate consumed before payment-return export execution
      pattern: detectExportCapabilities|selectBestExportPath
    - from: src/scripts/canvas/renderer.ts
      to: src/scripts/canvas/export-controller.ts
      via: deterministic manual elapsed frame stepping
      pattern: renderFrame|manualElapsed|exportMode
    - from: src/scripts/canvas/quality-profiles.ts
      to: src/scripts/canvas/animations/particles.ts
      via: export particle budget helper used for FR-3.11
      pattern: getExportParticleBudget
---

<objective>
Define Phase 3 export contracts and deterministic renderer primitives before flow wiring.

Purpose: FR-3.3, FR-3.6, FR-3.9, and FR-3.11 are foundational requirements that downstream plans must consume as stable contracts.
Output: export capability helpers, encoder interfaces, renderer deterministic frame API, and baseline tests proving contract behavior.
</objective>

<execution_context>
@/home/gabriel/Documents/gabriel-rodrigues/.claude/get-shit-done/workflows/execute-plan.md
@/home/gabriel/Documents/gabriel-rodrigues/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-video-export/03-RESEARCH.md
@.planning/phases/02-animation-quality/02-05-SUMMARY.md
@src/scripts/canvas/renderer.ts
@src/scripts/canvas/quality-profiles.ts
@src/scripts/canvas/types.ts

<interfaces>
From src/scripts/canvas/renderer.ts:
```ts
export class CanvasRenderer {
  init(canvas: HTMLCanvasElement, config: CompanyConfig): void;
  start(): void;
  stop(): void;
  dispose(): void;
}
```

From src/scripts/canvas/quality-profiles.ts:

```ts
export function getParticleBudget(baseCount: number): number;
```

From src/scripts/canvas/types.ts:

```ts
export interface CompanyConfig {
  companyName: string;
  colors: { primary: string; secondary: string; accent: string; background: string };
  mood: CompanyMood;
  industryCategory: IndustryCategory;
  animationStyle: AnimationStyle;
  version?: string;
}
```

Export defaults that must be encoded in constants for tests and downstream plans:

- width: 1920
- height: 1080
- fps: 30
- durationSeconds: 12
- totalFrames: 360
  </interfaces>
  </context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add export contract test scaffolding before implementation</name>
  <files>tests/export-contract.test.mjs, tests/quality-profiles.test.mjs</files>
  <behavior>
    - Export defaults resolve to 1920x1080 at 30 fps for 12 seconds and 360 frames.
    - Capability detection reports unsupported when MediaRecorder or captureStream is missing.
    - Renderer deterministic export path accepts manual elapsed values and bypasses wall-clock semantics.
    - Export particle budget applies a 30 percent reduction before mobile caps are enforced.
  </behavior>
  <action>Create `tests/export-contract.test.mjs` with contract-level tests for export defaults, capability gating, and deterministic frame stepping API expectations. Extend `tests/quality-profiles.test.mjs` with a new export-budget test that validates FR-3.11 behavior while preserving existing mobile-cap semantics. Keep test names explicit for FR traceability.</action>
  <verify>
    <automated>node --test tests/export-contract.test.mjs tests/quality-profiles.test.mjs</automated>
  </verify>
  <done>Wave 0 gaps for export contract coverage are closed and provide guardrails for implementation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement deterministic renderer and export capability foundations</name>
  <files>src/scripts/canvas/renderer.ts, src/scripts/canvas/quality-profiles.ts, src/scripts/canvas/export-support.ts, src/scripts/canvas/export-encoders.ts</files>
  <behavior>
    - Renderer init accepts export options including exportMode and preserveDrawingBuffer toggles.
    - Renderer exposes renderFrame(elapsed, delta) for controller-driven deterministic export loops.
    - Export support helpers return capability and preferred path metadata with no user-agent sniffing.
    - Encoder boundary defines start, appendFrame, finish, and cancel contracts for WebM and MP4 paths.
  </behavior>
  <action>Implement the Wave 0 contract layer by extending `CanvasRenderer` to support deterministic export-frame rendering (manual elapsed) and export-time renderer options, adding `getExportParticleBudget` in `quality-profiles.ts`, and creating new files `export-support.ts` and `export-encoders.ts`. Use feature detection and MediaRecorder.isTypeSupported checks for FR-3.9 capability gating. Keep this plan browser-only (NFR-7) and avoid wiring UI or worker routes yet.</action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>Core export interfaces and deterministic renderer behavior exist and pass baseline tests for downstream implementation plans.</done>
</task>

</tasks>

<verification>
Run contract tests and build. Confirm export defaults, capability helper outputs, deterministic renderer API, and 30 percent export particle reduction behavior are all covered before moving to flow integration.
</verification>

<success_criteria>

- FR-3.3 is satisfied with deterministic renderer contracts.
- FR-3.6 defaults are encoded in constants and tested.
- FR-3.9 has capability-gating helpers ready for integration.
- FR-3.11 reduction logic is implemented and tested without breaking existing mobile budget behavior.
- NFR-7 is preserved by keeping all implementation browser-side.
  </success_criteria>

<output>
After completion, create `.planning/phases/03-video-export/03-01-SUMMARY.md`
</output>
