---
phase: 03-video-export
plan: 05
type: execute
wave: 5
depends_on:
  - 03-03
  - 03-04
files_modified:
  - tests/export-contract.test.mjs
  - e2e/canvas-export.spec.ts
  - .planning/phases/03-video-export/03-BENCHMARK.md
autonomous: false
requirements:
  - NFR-2
  - NFR-3
  - FR-3.1
  - FR-3.5
must_haves:
  truths:
    - Default 12-second 1080p export completes within 36 seconds on reference desktop hardware.
    - Default WebM output size remains at or below 10 MB for acceptance fixture.
    - Exported files are produced by browser pipeline and remain playable in browser video element.
    - Phase gate captures pass/fail evidence for time and file-size requirements.
  artifacts:
    - path: tests/export-contract.test.mjs
      provides: Assertions for default export profile constants and size-guard helper behavior.
    - path: e2e/canvas-export.spec.ts
      provides: Browser flow coverage for successful export and preview continuity.
    - path: .planning/phases/03-video-export/03-BENCHMARK.md
      provides: Recorded benchmark evidence for NFR-2 and NFR-3 sign-off.
  key_links:
    - from: e2e/canvas-export.spec.ts
      to: src/scripts/canvas/export-controller.ts
      via: capture flow timing and completion checks
      pattern: progress|download|complete
    - from: .planning/phases/03-video-export/03-BENCHMARK.md
      to: .planning/phases/03-video-export/03-VALIDATION.md
      via: benchmark evidence referenced by final phase gate
      pattern: NFR-2|NFR-3
---

<objective>
Close Phase 3 with measurable performance and file-size evidence plus final execution gate.

Purpose: NFR-2 and NFR-3 are release-critical and require explicit evidence capture before Phase 3 sign-off.
Output: benchmark record, final targeted automation updates, and a blocking human verification checkpoint.
</objective>

<execution_context>
@/home/gabriel/Documents/gabriel-rodrigues/.claude/get-shit-done/workflows/execute-plan.md
@/home/gabriel/Documents/gabriel-rodrigues/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-video-export/03-VALIDATION.md
@.planning/phases/03-video-export/03-03-PLAN.md
@.planning/phases/03-video-export/03-04-PLAN.md
@tests/export-contract.test.mjs
@e2e/canvas-export.spec.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Finalize automated guards for default export profile and browser export completion</name>
  <files>tests/export-contract.test.mjs, e2e/canvas-export.spec.ts</files>
  <behavior>
    - Contract tests lock default export profile values used for NFR-2 and NFR-3 benchmarking.
    - Browser E2E test verifies export flow reaches completion with preview still active.
    - Tests remain deterministic and fast enough for repeated pre-merge execution.
  </behavior>
  <action>Tighten `tests/export-contract.test.mjs` and `e2e/canvas-export.spec.ts` to lock the default profile constants and successful completion-state behavior required by Phase 3 gate criteria. Keep tests focused on state transitions and measurable outputs rather than media-quality subjective checks.</action>
  <verify>
    <automated>node --test tests/export-contract.test.mjs && npx playwright test e2e/canvas-export.spec.ts --project=chromium</automated>
  </verify>
  <done>Phase-level automated gates for export defaults and completion flow are reliable and reusable.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Capture benchmark and file-size evidence for Phase 3 sign-off</name>
  <files>.planning/phases/03-video-export/03-BENCHMARK.md</files>
  <action>Create `.planning/phases/03-video-export/03-BENCHMARK.md` and record at least three default-export runs (1920x1080, 30 fps, 12 s) with elapsed export time and file size for each run. Include one browser video playback confirmation and one external player confirmation (VLC or QuickTime) as evidence. Mark each NFR as pass/fail with notes.</action>
  <how-to-verify>
    1) Run `npm run dev`.
    2) Open `/en/canvas/` in Chromium-based browser and complete paid-return export flow using test payment setup.
    3) Export three times with default settings and record elapsed time from start to download completion.
    4) Record file size for each artifact and compare against the 10 MB cap.
    5) Confirm playback in browser `<video>` and one external player.
    6) Save results in `03-BENCHMARK.md` with pass/fail for NFR-2 and NFR-3.
  </how-to-verify>
  <verify>
    <automated>npm run lint</automated>
  </verify>
  <done>Phase 3 has concrete benchmark evidence and a clear sign-off outcome for performance and file-size constraints.</done>
  <resume-signal>Type "approved" after benchmark evidence is recorded, or list failing metric runs for tuning follow-up.</resume-signal>
</task>

</tasks>

<verification>
Run export contract tests, targeted export E2E, and lint before the human benchmark checkpoint. Phase closes only when benchmark evidence confirms NFR-2 and NFR-3.
</verification>

<success_criteria>

- NFR-2 is evidenced with <=36-second export completion across benchmark runs.
- NFR-3 is evidenced with <=10 MB default WebM outputs.
- FR-3.1 and FR-3.5 remain valid under benchmark conditions.
  </success_criteria>

<output>
After completion, create `.planning/phases/03-video-export/03-05-SUMMARY.md`
</output>
