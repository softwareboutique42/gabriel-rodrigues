---
phase: 03-video-export
plan: 03
type: execute
wave: 3
depends_on:
  - 03-02
files_modified:
  - src/pages/en/canvas/index.astro
  - src/pages/pt/canvas/index.astro
  - src/i18n/en.json
  - src/i18n/pt.json
  - src/scripts/canvas/main.ts
  - src/scripts/canvas/export-support.ts
  - e2e/canvas-export.spec.ts
autonomous: true
requirements:
  - FR-3.8
  - FR-3.9
  - NFR-5
must_haves:
  truths:
    - Export UI communicates progress, keep-tab-active warning, and completion/error states during capture.
    - Unsupported browsers receive a clear fallback message rather than failing silently.
    - Export UX copy is complete in both English and Portuguese.
    - SPA navigation does not duplicate export listeners or leave stale export state.
  artifacts:
    - path: src/pages/en/canvas/index.astro
      provides: Export progress and fallback UI placeholders for EN route.
    - path: src/pages/pt/canvas/index.astro
      provides: Mirrored export progress and fallback UI placeholders for PT route.
    - path: src/i18n/en.json
      provides: Export progress, fallback, and warning strings in English.
    - path: src/i18n/pt.json
      provides: Export progress, fallback, and warning strings in Portuguese.
    - path: e2e/canvas-export.spec.ts
      provides: Browser-level regression coverage for progress and unsupported fallback behavior.
  key_links:
    - from: src/scripts/canvas/main.ts
      to: src/i18n/utils.ts
      via: translated status strings consumed in export status updates
      pattern: useTranslations|download-status|export
    - from: src/pages/en/canvas/index.astro
      to: src/scripts/canvas/main.ts
      via: export status and fallback element IDs bound by client script
      pattern: canvas-download-processing|download-status
    - from: e2e/canvas-export.spec.ts
      to: src/scripts/canvas/export-support.ts
      via: mocked capability matrix drives fallback and supported flows
      pattern: detectExportCapabilities|MediaRecorder
---

<objective>
Deliver user-facing export progress and unsupported-browser UX with EN/PT parity.

Purpose: FR-3.8 and FR-3.9 require visible, localized UX states that survive SPA navigation behavior.
Output: bilingual export status UI, fallback messaging, and focused browser automation for progress/fallback flows.
</objective>

<execution_context>
@/home/gabriel/Documents/gabriel-rodrigues/.claude/get-shit-done/workflows/execute-plan.md
@/home/gabriel/Documents/gabriel-rodrigues/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-video-export/03-RESEARCH.md
@.planning/phases/03-video-export/03-02-PLAN.md
@src/pages/en/canvas/index.astro
@src/pages/pt/canvas/index.astro
@src/i18n/en.json
@src/i18n/pt.json
@src/scripts/canvas/main.ts
@src/scripts/canvas/export-support.ts
@e2e/canvas.spec.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add bilingual export progress and fallback UI states</name>
  <files>src/pages/en/canvas/index.astro, src/pages/pt/canvas/index.astro, src/i18n/en.json, src/i18n/pt.json, src/scripts/canvas/main.ts, src/scripts/canvas/export-support.ts</files>
  <behavior>
    - Progress status updates include frame progress and completion/error messages.
    - Unsupported browser messaging explicitly guides users to Chrome/Firefox or HTML fallback.
    - Keep-tab-active warning displays while export is in progress.
    - Status and warning text are sourced from locale dictionaries for both EN and PT.
  </behavior>
  <action>Update both canvas pages and locale JSON files with export UI elements and copy required for FR-3.8 and FR-3.9. Wire status updates in `main.ts` to capability checks from `export-support.ts`, ensuring lifecycle-safe listener behavior per NFR-5. Preserve existing layout structure and do not add server dependencies.</action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>User-facing export states are visible, localized, and capability-aware across both languages.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add focused E2E coverage for export progress and unsupported fallback</name>
  <files>e2e/canvas-export.spec.ts</files>
  <behavior>
    - Supported path test verifies progress indicator updates while export runs.
    - Unsupported path test verifies fallback guidance instead of silent failure.
    - Preview canvas remains present while export status changes occur.
  </behavior>
  <action>Create `e2e/canvas-export.spec.ts` with deterministic browser-level scenarios for supported and unsupported export environments. Use capability mocks where needed and keep assertions targeted to state transitions and visible status text rather than pixel-level video validation.</action>
  <verify>
    <automated>npx playwright test e2e/canvas-export.spec.ts --project=chromium</automated>
  </verify>
  <done>Phase 3 UX regressions for progress and fallback behavior are covered by automation.</done>
</task>

</tasks>

<verification>
Run build and the targeted export E2E suite. Confirm EN/PT copy renders, unsupported browsers show clear guidance, and progress state updates are visible during supported export flow.
</verification>

<success_criteria>

- FR-3.8 is satisfied with visible progress updates.
- FR-3.9 is satisfied with explicit capability gating and fallback messaging.
- NFR-5 is preserved by keeping export listeners lifecycle-safe in SPA navigation.
  </success_criteria>

<output>
After completion, create `.planning/phases/03-video-export/03-03-SUMMARY.md`
</output>
