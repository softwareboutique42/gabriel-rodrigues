---
phase: 03-video-export
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - workers/company-api/src/index.ts
  - workers/company-api/src/stripe.ts
  - src/scripts/canvas/export-controller.ts
  - src/scripts/canvas/export-encoders.ts
  - src/scripts/canvas/main.ts
  - tests/export-contract.test.mjs
autonomous: true
requirements:
  - FR-3.1
  - FR-3.2
  - FR-3.4
  - FR-3.5
  - FR-3.7
  - FR-3.8
  - NFR-5
must_haves:
  truths:
    - Paid users can complete checkout and trigger browser-side video export instead of immediate HTML-only download.
    - Export capture runs through a dedicated offscreen renderer so live preview remains active.
    - Export starts after a deterministic 100 ms warm-up and reports frame progress during capture.
    - WebM output is generated via MediaRecorder path with deterministic frame stepping.
  artifacts:
    - path: src/scripts/canvas/export-controller.ts
      provides: End-to-end export orchestration with offscreen renderer, warm-up delay, frame loop, and progress events.
    - path: src/scripts/canvas/main.ts
      provides: Stripe return handling and export invocation wired with astro lifecycle-safe listeners.
    - path: workers/company-api/src/stripe.ts
      provides: Optional export intent metadata persisted in checkout session.
    - path: workers/company-api/src/index.ts
      provides: Non-breaking checkout/download route extension for export intent flow.
    - path: tests/export-contract.test.mjs
      provides: Regression tests for warm-up and offscreen flow contracts.
  key_links:
    - from: src/scripts/canvas/main.ts
      to: src/scripts/canvas/export-controller.ts
      via: session_id return branch invoking video export pipeline
      pattern: handlePaymentReturn|startVideoExport
    - from: src/scripts/canvas/export-controller.ts
      to: src/scripts/canvas/renderer.ts
      via: export renderer init with preserveDrawingBuffer and manual elapsed frame stepping
      pattern: init\(|renderFrame
    - from: workers/company-api/src/index.ts
      to: workers/company-api/src/stripe.ts
      via: checkout metadata for export intent persisted and returned without breaking existing contract
      pattern: export_type|createCheckoutSession
---

<objective>
Implement the mandatory paid WebM export path with deterministic offscreen capture.

Purpose: FR-3.1, FR-3.2, FR-3.4, FR-3.5, FR-3.7, and FR-3.8 form the core user outcome for Phase 3.
Output: offscreen export controller, Stripe intent extension, and main-flow wiring that preserves SPA lifecycle safety.
</objective>

<execution_context>
@/home/gabriel/Documents/gabriel-rodrigues/.claude/get-shit-done/workflows/execute-plan.md
@/home/gabriel/Documents/gabriel-rodrigues/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-video-export/03-RESEARCH.md
@.planning/phases/03-video-export/03-01-PLAN.md
@workers/company-api/src/index.ts
@workers/company-api/src/stripe.ts
@src/scripts/canvas/main.ts
@src/scripts/canvas/export.ts
@src/scripts/canvas/renderer.ts
@src/scripts/canvas/export-support.ts
@src/scripts/canvas/export-encoders.ts

<interfaces>
From workers/company-api/src/stripe.ts:
```ts
export async function createCheckoutSession(apiKey: string, checkout: CheckoutConfig): Promise<{ url: string }>;
export async function verifyAndGetConfig(apiKey: string, sessionId: string): Promise<{ paid: boolean; config: unknown; version: string }>;
```

From src/scripts/canvas/main.ts:

```ts
async function handleDownload(config: CompanyConfig): Promise<void>;
async function handlePaymentReturn(): Promise<void>;
```

From Plan 03-01 contracts:

```ts
type ExportEncoder = {
  start(): Promise<void>;
  appendFrame(input: HTMLCanvasElement | VideoFrame, timestampUs: number): Promise<void>;
  finish(): Promise<Blob>;
  cancel(reason?: string): Promise<void>;
};
```

  </interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend Stripe checkout/download flow with video export intent metadata</name>
  <files>workers/company-api/src/index.ts, workers/company-api/src/stripe.ts, tests/export-contract.test.mjs</files>
  <behavior>
    - Checkout accepts optional export intent metadata without breaking existing payloads.
    - Download verification returns the same config/version contract while preserving compatibility.
    - Existing HTML export fallback remains available when export intent is absent or unsupported.
  </behavior>
  <action>Update worker checkout and Stripe helper code to carry an optional `export_type=video` metadata field for FR-3.2 while preserving backward compatibility. Keep route contracts non-breaking and extend `tests/export-contract.test.mjs` to cover metadata pass-through behavior at contract level. Do not add any server-side encoding path (NFR-7).</action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>Stripe gate is extended for video intent with no regression in existing checkout/download flow.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build WebM export controller with offscreen deterministic capture and warm-up</name>
  <files>src/scripts/canvas/export-controller.ts, src/scripts/canvas/export-encoders.ts, tests/export-contract.test.mjs</files>
  <behavior>
    - Export controller creates a second canvas and renderer independent from the preview canvas.
    - Controller waits 100 ms before starting recording and captures exactly 360 frames at 30 fps.
    - Progress callback emits frame index and percentage through capture lifecycle.
    - WebM blob generation uses MediaRecorder chunks and resolves filename metadata.
  </behavior>
  <action>Create `export-controller.ts` and implement the mandatory WebM path in `export-encoders.ts`. Configure export renderer with preserveDrawingBuffer during capture (FR-3.4), step frames using deterministic elapsed progression (FR-3.5), enforce warm-up delay (FR-3.7), and emit progress state (FR-3.8). Add/update tests in `tests/export-contract.test.mjs` for deterministic frame count and warm-up timing contracts.</action>
  <verify>
    <automated>node --test tests/export-contract.test.mjs</automated>
  </verify>
  <done>Core browser WebM export pipeline is operational behind Stripe-gated flow prerequisites.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Wire main canvas payment-return flow to export controller with lifecycle-safe cleanup</name>
  <files>src/scripts/canvas/main.ts</files>
  <behavior>
    - Download button initiates checkout intent for video export.
    - Returning with session_id triggers video export flow and status updates.
    - Event listeners and long-running tasks are bound to AbortController for SPA re-navigation safety.
  </behavior>
  <action>Update `main.ts` to invoke the export controller after successful paid return, while preserving `export.ts` HTML fallback for unsupported or failed cases. Keep all listener wiring inside the existing `initCanvas()` lifecycle and cleanup patterns to satisfy NFR-5. Ensure preview renderer remains active and unaffected during export capture.</action>
  <verify>
    <automated>npm run lint</automated>
  </verify>
  <done>Paid return path launches browser video capture safely and remains compatible with Astro SPA lifecycle constraints.</done>
</task>

</tasks>

<verification>
Run build, export contract tests, and lint. Confirm paid return path invokes video export, progress updates are emitted, and preview renderer remains active while offscreen capture runs.
</verification>

<success_criteria>

- FR-3.1 and FR-3.2 are implemented end to end with Stripe-gated browser export.
- FR-3.4, FR-3.5, and FR-3.7 are implemented in the export controller.
- FR-3.8 has progress hooks wired into user-facing status updates.
- NFR-5 listener and cleanup patterns are preserved in main lifecycle wiring.
  </success_criteria>

<output>
After completion, create `.planning/phases/03-video-export/03-02-SUMMARY.md`
</output>
