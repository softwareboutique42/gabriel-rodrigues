---
phase: 22-regression-and-runtime-confidence-lock
plan: 01
type: execute
wave: 1
depends_on:
  - 21-01
files_modified:
  - tests/slots-animation-event-sequencing-contract.test.mjs
  - tests/slots-sprite-atlas-contract.test.mjs
  - tests/slots-symbol-states-contract.test.mjs
  - tests/slots-i18n-parity-contract.test.mjs
  - e2e/compatibility.spec.ts
  - .planning/phases/22-regression-and-runtime-confidence-lock/22-01-SUMMARY.md
autonomous: true
requirements:
  - QA-20
must_haves:
  truths:
    - Fixed-seed spins keep deterministic runtime sequencing and presentation snapshots across repeated runs.
    - EN and PT slots routes expose parity-safe runtime behavior through stable data-slots-anim-* hooks.
    - Browser regression checks fail on selector drift, locale mismatch, or runtime event-order regressions.
  artifacts:
    - path: tests/slots-animation-event-sequencing-contract.test.mjs
      provides: deterministic sequencing and immutable event snapshot contracts.
    - path: tests/slots-sprite-atlas-contract.test.mjs
      provides: deterministic atlas/symbol runtime observability contracts.
    - path: tests/slots-i18n-parity-contract.test.mjs
      provides: EN/PT parity contract for slots runtime labels/keys and route parity invariants.
    - path: e2e/compatibility.spec.ts
      provides: Playwright parity and deterministic runtime assertions on canonical EN/PT slots flows.
  key_links:
    - from: tests/slots-animation-event-sequencing-contract.test.mjs
      to: src/scripts/slots/animation/runtime.ts
      via: runtime mount/snapshot assertions on data-slots-anim-* state and sequence hooks
      pattern: data-slots-anim-(state|outcome|seq|symbol-states|performance)
    - from: tests/slots-i18n-parity-contract.test.mjs
      to: src/pages/en/slots/index.astro
      via: translation key and runtime label parity enforcement
      pattern: slots\.gameplay\.
    - from: e2e/compatibility.spec.ts
      to: src/pages/pt/slots/index.astro
      via: canonical EN/PT slot-route and machine-readable runtime parity assertions
      pattern: /en/slots/|/pt/slots/|data-slots-anim-
validation_commands:
  - npm run lint
  - node --test tests/slots-core-determinism-contract.test.mjs tests/slots-animation-event-sequencing-contract.test.mjs tests/slots-sprite-atlas-contract.test.mjs tests/slots-symbol-states-contract.test.mjs tests/slots-i18n-parity-contract.test.mjs tests/slots-motion-accessibility-contract.test.mjs tests/slots-performance-guardrail-contract.test.mjs
  - npx playwright test e2e/compatibility.spec.ts --project=chromium --project=mobile-chrome
  - npm run build
  - npm run lint && node --test tests/slots-core-determinism-contract.test.mjs tests/slots-animation-event-sequencing-contract.test.mjs tests/slots-sprite-atlas-contract.test.mjs tests/slots-symbol-states-contract.test.mjs tests/slots-i18n-parity-contract.test.mjs tests/slots-motion-accessibility-contract.test.mjs tests/slots-performance-guardrail-contract.test.mjs && npx playwright test e2e/compatibility.spec.ts --project=chromium --project=mobile-chrome && npm run build
---

<objective>
Lock v1.4 runtime confidence by hardening deterministic contract and Playwright compatibility coverage for animation/sprite sequencing and EN/PT parity.

Purpose: complete QA-20 with deterministic runtime sequencing guarantees and locale parity release gates.
Output: strengthened contract tests, hardened compatibility E2E, and a validation chain suitable for milestone closeout.
</objective>

<execution_context>
@/home/gabriel/Documents/gabriel-rodrigues/.claude/get-shit-done/workflows/execute-plan.md
@/home/gabriel/Documents/gabriel-rodrigues/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/22-regression-and-runtime-confidence-lock/22-CONTEXT.md
@.planning/phases/21-accessibility-and-performance-hardening/21-01-SUMMARY.md
@tests/slots-animation-event-sequencing-contract.test.mjs
@tests/slots-sprite-atlas-contract.test.mjs
@tests/slots-symbol-states-contract.test.mjs
@tests/slots-i18n-parity-contract.test.mjs
@e2e/compatibility.spec.ts

<interfaces>
From src/scripts/slots/animation/runtime.ts runtime datasets consumed by contracts and E2E:
- data-slots-anim-state
- data-slots-anim-outcome
- data-slots-anim-atlas
- data-slots-anim-atlas-id
- data-slots-anim-theme
- data-slots-anim-reduced-motion
- data-slots-anim-intensity-requested
- data-slots-anim-intensity
- data-slots-anim-performance
- data-slots-anim-symbol-states
- data-slots-anim-idle
- data-slots-anim-seq
- data-slots-anim-blocked-reason

From e2e/compatibility.spec.ts existing runtime helpers:

- expectSlotsState(root, state)
- expectSlotsStatus(page, text)
- spinAndWaitForResolution(page, root, statusText)
- setMaximumBet(page)
  </interfaces>
  </context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Harden deterministic contract coverage for runtime sequencing, sprite snapshots, and EN/PT parity</name>
  <files>tests/slots-animation-event-sequencing-contract.test.mjs, tests/slots-sprite-atlas-contract.test.mjs, tests/slots-symbol-states-contract.test.mjs, tests/slots-i18n-parity-contract.test.mjs</files>
  <behavior>
    - Test 1: fixed-seed runtime event sequences remain monotonic and immutable across repeated runs (per deterministic sequencing lock in 22-CONTEXT).
    - Test 2: sprite/symbol runtime snapshots stay presentation-only and deterministic under idle, spin, resolved, and blocked paths.
    - Test 3: EN/PT parity contracts fail when canonical gameplay labels/keys or runtime parity assumptions diverge.
  </behavior>
  <action>
    Expand QA-20 contract guards by strengthening existing suites without introducing timing-fragile assertions:
    - Extend sequencing contracts to assert monotonic seq transitions and blocked-event non-overwrite invariants under repeated mount/dispose cycles.
    - Extend sprite/symbol contracts to verify deterministic symbol-state and atlas observability snapshots remain stable for canonical event transitions.
    - Extend i18n parity contracts for slots runtime labels/hooks used by browser checks, preserving canonical EN/PT route assumptions and parity safety.
    - Keep checks machine-readable and hook-based; do not add screenshot/pixel-diff assertions.
  </action>
  <verify>
    <automated>node --test tests/slots-core-determinism-contract.test.mjs tests/slots-animation-event-sequencing-contract.test.mjs tests/slots-sprite-atlas-contract.test.mjs tests/slots-symbol-states-contract.test.mjs tests/slots-i18n-parity-contract.test.mjs tests/slots-motion-accessibility-contract.test.mjs tests/slots-performance-guardrail-contract.test.mjs</automated>
  </verify>
  <done>Contract suite fails on deterministic sequencing drift, sprite snapshot drift, or EN/PT parity drift and remains timing-agnostic.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Harden Playwright compatibility coverage for deterministic EN/PT runtime parity</name>
  <files>e2e/compatibility.spec.ts</files>
  <behavior>
    - Test 1: EN/PT slots journeys keep canonical routing and deterministic runtime dataset expectations with stable selectors.
    - Test 2: Runtime sequencing snapshots increment predictably through spin and insufficient-credit blocked flows in both locales.
    - Test 3: Compatibility suite fails immediately on selector drift or localized runtime-state mismatches.
  </behavior>
  <action>
    Strengthen browser-level QA-20 confidence lock in compatibility E2E:
    - Consolidate runtime dataset assertions into reusable helper expectations to avoid assertion drift between EN/PT checks.
    - Add explicit parity checks for deterministic animation/sprite runtime hooks and sequence progression on canonical slots routes.
    - Preserve stable selectors and data-slots-anim-* hooks as primary oracle; keep assertions timing-agnostic and copy-resilient where possible.
    - Run coverage on existing Playwright projects (chromium and mobile-chrome) to lock desktop/mobile sequencing behavior.
  </action>
  <verify>
    <automated>npx playwright test e2e/compatibility.spec.ts --project=chromium --project=mobile-chrome</automated>
  </verify>
  <done>Compatibility E2E reliably catches EN/PT runtime parity and sequencing regressions using deterministic hook assertions.</done>
</task>

</tasks>

<validation_commands>

1. npm run lint
2. node --test tests/slots-core-determinism-contract.test.mjs tests/slots-animation-event-sequencing-contract.test.mjs tests/slots-sprite-atlas-contract.test.mjs tests/slots-symbol-states-contract.test.mjs tests/slots-i18n-parity-contract.test.mjs tests/slots-motion-accessibility-contract.test.mjs tests/slots-performance-guardrail-contract.test.mjs
3. npx playwright test e2e/compatibility.spec.ts --project=chromium --project=mobile-chrome
4. npm run build
5. npm run lint && node --test tests/slots-core-determinism-contract.test.mjs tests/slots-animation-event-sequencing-contract.test.mjs tests/slots-sprite-atlas-contract.test.mjs tests/slots-symbol-states-contract.test.mjs tests/slots-i18n-parity-contract.test.mjs tests/slots-motion-accessibility-contract.test.mjs tests/slots-performance-guardrail-contract.test.mjs && npx playwright test e2e/compatibility.spec.ts --project=chromium --project=mobile-chrome && npm run build

</validation_commands>

<verification>
QA-20 is complete when deterministic contract suites and compatibility E2E both enforce fixed-seed runtime sequencing plus EN/PT parity, and the full lint/test/build chain passes without flaky timing-dependent checks.
</verification>

<success_criteria>

- Contract tests detect any regression in animation/sprite event sequencing determinism for fixed-seed flows.
- Contract tests detect runtime snapshot regressions across atlas, symbol-state, accessibility, and performance datasets.
- Playwright compatibility validates EN/PT parity on canonical slots routes using stable selectors and data hooks.
- Full verification chain (lint, targeted contracts, compatibility E2E, build) passes as release evidence for QA-20 closure.
  </success_criteria>

<output>
After completion, create .planning/phases/22-regression-and-runtime-confidence-lock/22-01-SUMMARY.md
</output>
